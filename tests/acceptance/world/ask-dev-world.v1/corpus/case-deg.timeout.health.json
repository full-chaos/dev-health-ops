{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.timeout.health",
  "family": "deg",
  "group": 9,
  "status": "declared-blocked",
  "blocked_by": "CHAOS-3455",
  "proves": [
    "group 9 family deg.timeout (pipeline-stage timeout matrix -- health-evaluation instance)"
  ],
  "question": "Is the health evaluation for the repo \"meridian/web-app\" hanging?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": "subject.exact.meridian-web-app",
  "invariants": [
    {
      "category": "declared-blocked-placeholder",
      "check": "no_internal_error",
      "args": {},
      "assert": "Placeholder ONLY -- this case is status=declared-blocked (nothing runs against real production code/fixtures yet, see blocked_by). Lane 2a runner code (read directly, 2026-08-06) has no declared-blocked concept: case_schema.py requires every case file, unconditionally, to carry a non-empty invariants[] list, or the WHOLE corpus load raises (not a per-file skip). This entry exists purely to satisfy that mechanical requirement so the corpus stays loadable; it must NOT be read as an executed assertion about the blocked scenario.",
      "executable_today": false
    }
  ],
  "resolution_profile_ref": null,
  "fault_ref": null,
  "notes": "Checked contracts.py's ToolID enum directly: its 9 members are resolve_scope.v1, list_metrics.v1, query_metric.v1, status_snapshot.v1, change_summary.v1, work_graph_neighbors.v1, search_evidence.v1, get_evidence.v1, data_health.v1 -- there is no health-evaluation tool. Checked operational_deficiency_service.py directly: 'health evaluation' (health_profile_synthesis.synthesize_health_profile, invoked through ProjectHealthService/TeamHealthService's PlanExecutorRuntime seam) is server-side SYNTHESIS composed from the results of other tools (status_snapshot.v1/query_metric.v1/data_health.v1 -- each already independently covered by deg.timeout.status/metric/data-health respectively), not itself a provider-requestable ToolID the scripted decision sequence can request and then delay a following round on, the way deg.timeout.graph/metric/evidence/data-health each do for their own genuinely provider-requestable tool. Unlike those four siblings, there is no plausible tool_call to anchor a pre_fault_decisions + slow-response sequence to -- the delay would have to land on the SAME round that already produced status_snapshot/query_metric/data_health, duplicating one of the other cases rather than isolating a distinct 'health evaluation' stage. Declared-blocked rather than force a strained reuse of an existing case's mechanism onto a different label. question/org_alias/user_alias/subject_class/subject_ref kept as documentation of the intended (currently-blocked) scenario. [blocked_by WIRED to CHAOS-3455 during Lane 2b ticket-filing pass, 2026-08-06 -- original free-text reason preserved verbatim here: 'runner: no per-stage timeout fault type scripted yet'.]"
}
