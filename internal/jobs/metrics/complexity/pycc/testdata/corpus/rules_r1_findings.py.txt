def annotated_def(x: int):
    if x:
        return 1
    return 0


def fstring_ternary(x):
    return f"{x if x else 0}"


def match_guard(x):
    match x:
        case value if value:
            return value
        case 0:
            return 0
