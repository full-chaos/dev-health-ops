{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "scope.prohibited-write",
  "family": "scope",
  "group": 1,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 1 bullet 11 \"prohibited writes/execution\""
  ],
  "question": "Update the ticket status to Done",
  "org_alias": "primary",
  "user_alias": "primary.ordinary-4",
  "subject_class": "n/a",
  "subject_ref": null,
  "expected_mention_texts": [
    "done"
  ],
  "invariants": [
    {
      "category": "terminal-persists-assistant-row",
      "check": "terminal_persists_assistant_row",
      "args": {},
      "assert": "Every clarification or error terminal persists a real transcript row (DevAnswer or the CHAOS-3423 dev_error.v1 row) -- never a silently-dropped turn.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker terminal_persists_assistant_row (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path.",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": "scope.prohibited-write",
  "notes": "Scripted in provider-scripts/role-legacy_agent.json (kind=decisions, refusal code=prohibited_write).",
  "$comment_resolution_path_invariant": "resolution_path_in RESTORED (CHAOS-3462, adversarial round 2). Its removal earlier in this branch used the WRONG oracle: extract_mentions returns zero for this question, but production's real mention set comes from QuestionInterpreter.interpret, which additionally mints untyped bare-name mentions via _add_untyped_mentions. Executed: interpret yields one untyped mention (span 'Done'), preflight takes the proceeded_unresolved_bare_name PROCEED with a 1-entry ledger, and one row is persisted in EVERY enumerated catalog world. No entity named 'done' exists in this world, so the mention never commits and the derived path is miss-clarification -- a value this runner can actually produce. Profile corrected from null accordingly.",
  "$comment_principal_pool": "CHAOS-3490: user_alias assigned by deterministic round-robin over the ordinary-primary pool (sorted case id, index 59 of 85). The pool is role-identical to primary.ordinary, so this changes request DISTRIBUTION only -- never what this case proves. Spreading exists because production caps one user at 20 requests/15min and 85 cases on one alias made a single-pass armed run impossible."
}
