{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.timeout.status",
  "family": "deg",
  "group": 9,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 9 family deg.timeout (pipeline-stage timeout matrix -- status-snapshot instance)"
  ],
  "question": "Is the status snapshot for meridian/web-app ready yet?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary-7",
  "subject_class": "exact",
  "subject_ref": "subject.exact.meridian-web-app",
  "invariants": [
    {
      "category": "terminal-persists-assistant-row",
      "check": "terminal_persists_assistant_row",
      "args": {},
      "assert": "Every clarification or error terminal persists a real transcript row (DevAnswer or the CHAOS-3423 dev_error.v1 row) -- never a silently-dropped turn. Applies here even though the run is deliberately slow (an injected 4000ms delay before the only round's final_answer): a slow-but-successful terminal must persist exactly like a fast one.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker terminal_persists_assistant_row (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    },
    {
      "category": "terminal-outcome-matches-profile",
      "check": "public_outcome_in",
      "args": {
        "from_profile": "expected_public_outcome"
      },
      "assert": "The run reaches the terminal PublicOutcome its resolution profile declares -- not merely a non-crash, and not merely a persisted row. Wired CHAOS-3219 Phase 3 (P3-1).",
      "executable_today": true,
      "$comment": "CHAOS-3219 Phase 3 P3-1: the profile ALREADY declared expected_public_outcome for this case and nothing asserted it. Every active group-8 case was in that state, so the whole adversarial family's security claims reduced to 'the run did not crash' plus 'a row was persisted' -- a product that stopped refusing, or answered where it should have failed, passed all of them. The checker and the expected value both already existed; only the wire between them was missing."
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": "deg.timeout.status",
  "notes": "Question text and fault shape copied VERBATIM from provider-scripts/role-legacy_agent.json's pre-existing `cases[\"deg.timeout.status\"]` entry (Lane 1b), per this batch's explicit instruction -- not reworded, not re-scripted. Fault: kind=fault, type=slow-response, fires_from_round=0, delay_ms=4000, decision=final_answer{status:degraded}, no pre_fault_decisions (the single round itself is the delayed one). Honesty flag (found while grounding this case, not present in the pre-existing script's own documentation): the question text 'Is the status snapshot for meridian/web-app ready yet?' names 'meridian/web-app' as a BARE, unquoted, lowercase slug with no adjacent closed-kind noun ('repo'/'project'/etc.) immediately beside it -- per question_interpreter.py's own module docstring ('a lowercase slug or a noun-less name ... produces no mention, so the run degrades to today's organization-wide behaviour') and this batch's CRITICAL LESSON, extract_mentions recognizes ZERO mentions in this literal text despite it conceptually referring to subject.exact.meridian-web-app. subject_class/subject_ref above document the INTENDED subject (matching the fault's own conceptual target and the fixture this fault is grounded against), but the resolution-profiles/deterministic-v1.json entry for this id correctly asserts resolution_path=null and expected_scope_resolution_outcome=organization_fallback, not exact -- this is a real, pre-existing property of the inherited script text, not a defect introduced here, and is called out explicitly rather than silently reconciled. This id's fixture/fault content is otherwise unrelated to the deg.timeout.metric/evidence/graph/data-health siblings authored alongside it (those use a freshly quoted 'the repo \"meridian/web-app\"' phrasing specifically to avoid repeating this gap).",
  "$comment_resolution_path_invariant": "resolution_path_in REMOVED (CHAOS-3462 B4). This case's question names ZERO mentions that production's extract_mentions recognizes -- verified by executing the REAL producer -- QuestionInterpreter.interpret plus SubjectPreflight.run with this runner's exact request shape -- against this question text. NOTE (adversarial round 2): an earlier version of this comment cited extract_mentions, which is the WRONG oracle: production additionally mints untyped bare-name mentions via _add_untyped_mentions, and three cases were mis-dispositioned on that basis. This case's zero-mention conclusion was re-confirmed against the real interpreter. CASE-SCHEMA.v1.md's own 'practical extraction gotcha' is the cause: a bare, unquoted, lowercase slug is not a mention. So no dev_run_resolutions row is ever written, derive_resolution_path returns None BY CONTRACT, and resolution_path_in deliberately never matches None -- the check failed by construction on every run, on every stack. Same trap and same disposition as the four scope_resolution_outcome_in cases in CASE-SCHEMA.v1.md S1: drop the unpassable check, keep the narrower floor the remaining invariants give, and say so out loud rather than claim enforcement this case never had. The profile's null resolution_path is CORRECT and stays.",
  "$comment_principal_pool": "CHAOS-3490: user_alias assigned by deterministic round-robin over the ordinary-primary pool (sorted case id, index 34 of 85). The pool is role-identical to primary.ordinary, so this changes request DISTRIBUTION only -- never what this case proves. Spreading exists because production caps one user at 20 requests/15min and 85 cases on one alias made a single-pass armed run impossible."
}
