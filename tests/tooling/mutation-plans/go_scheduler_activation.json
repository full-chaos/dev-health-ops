{
  "schema_version": 1,
  "name": "Go scheduler source activation",
  "$comment": "CHAOS-3128: Go now owns scheduled marker mutation. The plan proves the checked-in production wrapper cannot silently return to the dormant branch, while the scheduler runtime suite proves product and fixed-loop construction/readiness with the reviewed role split.",
  "build": [
    "env",
    "GOTOOLCHAIN=go1.25.9",
    "GOWORK=off",
    "GOCACHE=/tmp/go-scheduler-activation-mutation-cache",
    "go",
    "test",
    "-run",
    "^$",
    "./cmd/dev-health-scheduler/"
  ],
  "mutations": [
    {
      "id": "GSA01-checked-in-marker-ownership",
      "file": "cmd/dev-health-scheduler/main.go",
      "find": "var checkedInSchedulerActivation = schedulerActivation{goOwnsMarkers: true}",
      "replace": "var checkedInSchedulerActivation = schedulerActivation{goOwnsMarkers: false}",
      "expect_occurrences": 1,
      "rationale": "The production dependency wrapper must reach the reviewed scheduler factory instead of returning the dormant unavailable checks.",
      "proof": [
        [
          "env",
          "GOTOOLCHAIN=go1.25.9",
          "GOWORK=off",
          "GOCACHE=/tmp/go-scheduler-activation-mutation-cache",
          "go",
          "test",
          "-count=1",
          "-run",
          "^TestProductionSchedulerConfigurationBuildsTheReviewedLoop$",
          "./cmd/dev-health-scheduler/"
        ]
      ]
    }
  ]
}
