{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.source-state.no-data",
  "family": "deg",
  "group": 9,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 9 family deg.source-state (sources.json per-source-class terminal-state matrix, state=no-data)"
  ],
  "question": "How many deployments has the repo \"probe/source-no-data\" recorded recently?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": null,
  "invariants": [
    {
      "category": "no-silent-widening",
      "check": "scope_resolution_outcome_in",
      "args": {
        "from_profile": "expected_scope_resolution_outcome"
      },
      "assert": "A named subject mention that misses its intended target never silently widens to org-wide/broader scope; widening is only ever a disclosed, terminal answered_with_gaps/needs_clarification outcome.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker scope_resolution_outcome_in against this case's own resolution-profile expected_scope_resolution_outcome (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Grounded against sources.json's dedicated 'no-data' matrix row (repo_full_name probe/source-no-data, org primary): repo entity exists but is generated with zero commits/PRs/reviews/deployments/work items -- watermark is genuinely NULL for every source (DataHealthState.NO_DATA), distinct from measured-zero (deg.acr-unavailable-native-usable's sibling registry doc string covers that distinction; not re-litigated here). Question asks specifically about deployments, one of the two sources sources.json explicitly asserts NO_DATA on for this repo. Repo referenced directly by name, subject_ref intentionally null (no dedicated subjects.json entry)."
}
