{
  "description": "The golden corpus for the jwks.v1 wire contract. This manifest is the SINGLE inventory of the corpus: the Go runner and the Python runner both read THIS file over THESE fixtures, so a third copy of the list cannot drift out of step with the first two (G-70).\n\nThe consumer named throughout is dev-health-go's the `Keys()` method of dev-health-go's `Ed25519JWKSVerifier`, which is what actually reads this document in production. The Go runner drives the REAL consumer against every fixture, so `accept` means \"the consumer accepts it\" as well as \"the schema validates it\" -- the two directions that together stop the schema drifting either looser or stricter than the code it describes.",
  "accept": [
    {
      "file": "valid-single-signing-key.json",
      "why": "The ordinary steady state: one key, `use` omitted. The minimal document the consumer accepts."
    },
    {
      "file": "valid-single-key-with-use-sig.json",
      "why": "`use` present and `sig`. Means exactly what its absence means; both forms are on the wire today because the producer emits it and the consumer does not require it."
    },
    {
      "file": "valid-two-keys-during-rotation.json",
      "why": "A rotation in progress. Two DISTINCT kids, so envelopes signed by the outgoing key still verify while they remain within their TTL. This is why `keys` is an array and not an object."
    },
    {
      "file": "valid-kid-at-maximum-length.json",
      "why": "`kid` at exactly the consumer's 256-byte ceiling. An upper bound with no at-the-bound case is a bound nobody has checked: a future off-by-one to 255 would break no other fixture."
    }
  ],
  "reject": [
    {
      "file": "invalid-keys-absent.json",
      "expect_instance_location": "/",
      "expect_keyword": "required",
      "why": "No `keys` at all. The consumer decodes to a nil slice and refuses on len==0, so the schema must not treat the member as optional."
    },
    {
      "file": "invalid-keys-empty.json",
      "expect_instance_location": "/keys",
      "expect_keyword": "minItems",
      "why": "A syntactically valid document that yields no key material. The consumer refuses it rather than reporting a healthy-but-empty keyset -- otherwise every verification fails as a kid lookup miss, which is indistinguishable from an unknown kid and sends the reader to the wrong cause."
    },
    {
      "file": "invalid-unknown-top-level-member.json",
      "expect_instance_location": "/",
      "expect_keyword": "additionalProperties",
      "why": "An extra top-level member. The consumer decodes with DisallowUnknownFields and rejects the WHOLE document, so a plausible-looking addition like `issuer` is not forward-compatible here -- it is an outage."
    },
    {
      "file": "invalid-unknown-key-member.json",
      "expect_instance_location": "/keys/0",
      "expect_keyword": "additionalProperties",
      "why": "A standard RFC 7517 member the consumer does not know. RFC 7517 says ignore unrecognised JWK members; the consumer refuses them. The schema follows the CONSUMER, because a schema that permitted `x5c` would certify a document that cannot be read."
    },
    {
      "file": "invalid-private-key-material-present.json",
      "expect_instance_location": "/keys/0",
      "expect_keyword": "additionalProperties",
      "why": "The private scalar in a document whose entire purpose is to publish the PUBLIC half. Refused by `additionalProperties` rather than by a rule naming `d`: the subtractive contract makes the leak unrepresentable instead of prohibited, so `p`, `q` and every other private member are closed by the same clause without being enumerated."
    },
    {
      "file": "invalid-kty-rsa.json",
      "expect_instance_location": "/keys/0/kty",
      "expect_keyword": "const",
      "why": "Wrong key type. Refused before any key material is looked at."
    },
    {
      "file": "invalid-crv-x25519.json",
      "expect_instance_location": "/keys/0/crv",
      "expect_keyword": "const",
      "why": "The same key TYPE (OKP) on the key-agreement curve. X25519 never signs anything, so accepting it here would defer a certain failure to a confusing signature mismatch later."
    },
    {
      "file": "invalid-alg-absent.json",
      "expect_instance_location": "/keys/0",
      "expect_keyword": "required",
      "why": "`alg` is optional in RFC 7517 and REQUIRED here, because the consumer compares it literally -- an absent `alg` reads as the empty string and the document is refused. The schema states the requirement rather than letting it surface as a mystery rejection."
    },
    {
      "file": "invalid-alg-lowercase.json",
      "expect_instance_location": "/keys/0/alg",
      "expect_keyword": "const",
      "why": "Case matters. The consumer's comparison is byte equality, not a case-folded match."
    },
    {
      "file": "invalid-use-not-sig.json",
      "expect_instance_location": "/keys/0/use",
      "expect_keyword": "const",
      "why": "An encryption key offered where a signing key is required."
    },
    {
      "file": "invalid-kid-absent.json",
      "expect_instance_location": "/keys/0",
      "expect_keyword": "required",
      "why": "No `kid`. The consumer reads the missing member as \"\", which fails its non-empty check, and an envelope could never be matched to this key anyway."
    },
    {
      "file": "invalid-kid-empty.json",
      "expect_instance_location": "/keys/0/kid",
      "expect_keyword": "pattern",
      "why": "An empty `kid`. Refused by the pattern rather than by `minLength`, so that this case and the whitespace-only case below trip the SAME keyword -- they are one rule in the consumer (TrimSpace then non-empty) and are kept one rule here."
    },
    {
      "file": "invalid-kid-whitespace-only.json",
      "expect_instance_location": "/keys/0/kid",
      "expect_keyword": "pattern",
      "why": "A `kid` of three spaces. `minLength: 1` ACCEPTS this -- length 3 -- and the consumer REFUSES it, because it trims before testing for empty. Excluding space from the alphabet is what closes the gap; executed against the consumer's own validKeyID, not argued."
    },
    {
      "file": "invalid-kid-non-ascii.json",
      "expect_instance_location": "/keys/0/kid",
      "expect_keyword": "pattern",
      "why": "256 code points and 512 UTF-8 BYTES. `maxLength: 256` accepts it and the consumer refuses it, because JSON Schema counts code points and Go's len() counts bytes. Restricting the alphabet to one-byte characters makes the two counts the same number; this fixture is what fails if someone \"simplifies\" the pattern back to a maxLength."
    },
    {
      "file": "invalid-kid-over-maximum-length.json",
      "expect_instance_location": "/keys/0/kid",
      "expect_keyword": "pattern",
      "why": "One character past the consumer's ceiling. Paired with valid-kid-at-maximum-length.json so the boundary is pinned from both sides."
    },
    {
      "file": "invalid-x-absent.json",
      "expect_instance_location": "/keys/0",
      "expect_keyword": "required",
      "why": "No key material."
    },
    {
      "file": "invalid-x-base64-padded.json",
      "expect_instance_location": "/keys/0/x",
      "expect_keyword": "pattern",
      "why": "Padded base64url. RFC 7515 section 2 forbids the padding, the consumer's RawURLEncoding rejects `=`, and Python's urlsafe_b64decode ACCEPTS it -- so a Python-side reader written later would disagree with the consumer unless the wire form is pinned here."
    },
    {
      "file": "invalid-x-standard-base64-alphabet.json",
      "expect_instance_location": "/keys/0/x",
      "expect_keyword": "pattern",
      "why": "The standard base64 alphabet. `+` and `/` are rejected by the consumer's RawURLEncoding and silently ACCEPTED by Python's urlsafe_b64decode, which translates them before decoding. Same split as the padding case, different cause."
    },
    {
      "file": "invalid-x-decodes-too-short.json",
      "expect_instance_location": "/keys/0/x",
      "expect_keyword": "pattern",
      "why": "42 characters, which decodes to 31 bytes. BOTH base64 decoders accept it happily -- the length is not checked anywhere in the decode. ed25519.Verify PANICS on a 31-byte public key rather than returning an error, BUT THE CONSUMER GUARDS THIS ITSELF: Keys() compares the decoded length against ed25519.PublicKeySize before returning a key, so the panic is not reachable through it. An earlier version of this note called the schema the only guard -- false, and written after reading the very function that disproves it. The pattern is defence-in-depth and a statement of the wire form, not the thing preventing a crash."
    },
    {
      "file": "invalid-x-decodes-too-long.json",
      "expect_instance_location": "/keys/0/x",
      "expect_keyword": "pattern",
      "why": "44 characters, which decodes to 33 bytes. Same panic class as the short case; kept as its own fixture because a bound checked on one side only is not a bound. The consumer already refuses both lengths; see the short-case entry for the sole-guard claim that was corrected."
    }
  ],
  "reject_by_client": [
    {
      "file": "client-rejected-duplicate-kid.json",
      "why": "Two keys sharing one `kid` and carrying DIFFERENT key material. The schema accepts it and the consumer refuses it. `uniqueItems` cannot help: it compares whole array items, and these items differ in `x`, so they are unique by that test while being ambiguous by the only measure that matters -- which key does an envelope stamped with this kid verify against? Executed both directions rather than reasoned."
    }
  ],
  "reject_by_client_note": "Valid against the schema, refused by the consumer. The category exists for rules JSON Schema cannot express at all, never for rules that were merely inconvenient to write.",
  "regex_dialect_note": "Both patterns here use explicit character RANGES and no shorthand class. `\\d`, `\\w`, `\\s` and `\\b` are Unicode-aware in Python's re and ASCII-only in RE2 and ECMA-262, so `[!-~]` and `[A-Za-z0-9_-]` are used instead.\n\nTHE ANCHORS ARE NOT ENGINE-IDENTICAL, and an earlier version of this note said they were. Executed: Python's `$` matches BEFORE a final newline, so bare `re.match('^[!-~]{1,256}$', 'safe\\n')` is True, while ECMA-262 returns false and Go RE2's `$` is end-of-text. So a trailing-newline `kid` is a genuine three-engine split at the pattern level.\n\nIT DOES NOT REACH A CALLER IN THIS REPOSITORY, which is why it is a documentation correction rather than a defect. authclient/contracts.py compiles every schema pattern through `strictly_anchored`, which rewrites an unescaped trailing `$` so it means end-of-input as the other two engines do. Verified through the real validator: a kid of 'safe\\n' is REFUSED with `pattern: 'safe\\n' does not match '^[!-~]{1,256}$'`. This is divergence #4 in the surface README, and it is enforced by the CLIENT rather than by anything in the schema \u2014 so a new language binding that compiles these patterns naively reopens it.",
  "known_gap_duplicate_json_members": "A repeated JSON member inside one key object -- two `kid` members, say -- is NOT refused by any current reader. Every parser keeps the LAST value, so the earlier one never reaches validation. error.v1 refuses this at the raw-bytes boundary before decoding, but that check lives in a client this repository owns; the JWKS consumer is dev-health-go, and adding a shadow reader here to catch it would create exactly the second implementation this programme exists to remove. Recorded as a gap for a dev-health-go change, not papered over with a local wrapper.\n\nFILED AS CHAOS-4951 (dev-health-go: refuse duplicate JSON members in a JWKS before decode, raw-text red-first fixture, positive control, no reliance on last-wins).",
  "narrower_than_consumer": [
    {
      "file": "narrower-case-variant-member-names.json",
      "why": "Go's encoding/json matches struct field names CASE-INSENSITIVELY, and DisallowUnknownFields compares AFTER that match -- so the consumer reads {\"KEYS\":[{\"KTY\":...}]} as an ordinary document and accepts it. The schema refuses it.\n\nWHY THE SCHEMA REFUSES IT, corrected after round 2 falsified the first reason given. That reason was \"admitting any casing would make additionalProperties:false unenforceable\", and it is FALSE -- executed: a schema declaring both `keys` and `KEYS` still rejects an undeclared `issuer` by additionalProperties. The keyword keeps working.\n\nThe true reason is that JSON Schema matches property names EXACTLY, so admitting case variants means ENUMERATING them: every casing of every member in `properties`, and `required` rewritten as a disjunction over all of them. That is combinatorial in the number of members and it leaves the wire form with no canonical spelling for a producer to target. Refusing is not a limitation of the keyword; it is a choice to have one spelling."
    },
    {
      "file": "narrower-use-null.json",
      "why": "JSON null decodes into Go's string field as the zero value, so the consumer sees an empty use and accepts it. const:sig refuses null. Absence is how this contract says nothing."
    },
    {
      "file": "narrower-kid-with-surrounding-space.json",
      "why": "The consumer TrimSpaces before testing for empty, so a padded kid trims to something non-empty and is accepted -- and an envelope's kid header would then have to match the UNTRIMMED value to resolve, a silent mismatch waiting to happen. The schema refuses the padding outright."
    },
    {
      "file": "narrower-kid-containing-nul.json",
      "why": "unicode.IsSpace(NUL) is FALSE, so TrimSpace does not strip it and the consumer accepts a kid carrying an embedded NUL. The schema's printable-ASCII class refuses it. Stored as a JSON backslash-u-0000 escape, the only way a NUL can appear in a JSON document."
    },
    {
      "file": "narrower-use-empty-string.json",
      "why": "The consumer's test is `use != \"\" && use != \"sig\"`, so an EMPTY use is tolerated alongside sig; `const: sig` refuses it. Absence is how this contract says nothing, and a field present-and-blank carries no information its omission does not.\n\nTHIS FIXTURE EXISTS BECAUSE ITS ABSENCE WAS THE ROUND-2 BLOCK. The narrowing was named in the schema description and exempted by a predicate in the differential harness, and had no fixture asserting both halves -- so it was a SENTENCE, in the very commit whose message was that narrowings must stop being sentences. Round 1 blocked this contract for four prose-only narrowings; round 2 blocked it for a fifth, added while fixing the first four."
    },
    {
      "file": "narrower-unicode-folded-member-name.json",
      "why": "Go's encoding/json does not match field names by ASCII case alone -- it uses UNICODE SIMPLE FOLDING. U+017F LATIN SMALL LETTER LONG S folds with `s`, so the member `key\\u017f` binds to the `Keys` field and the consumer reads this as an ordinary document. Executed against the real Keys(): ACCEPTS. U+212A KELVIN SIGN folds with `k` the same way.\n\nTHIS IS THE SIXTH NARROWING AND THE THIRD BLOCK IN A ROW ON THIS CATEGORY. It is not a new kind of defect; it is the case-variant narrowing, whose PREDICATE was wrong. That predicate asked `name != strings.ToLower(name)`, which is FALSE for `key\\u017f` because the name is already lowercase -- so the exemption did not fire, the differential would have reported a genuine undeclared narrowing, and the only reason it stayed green is that the generator emitted ASCII upper/title variants and could not produce this shape. A wrong predicate and a blind generator cancelled out.\n\nThe predicate now tests what the decoder tests: a member name that is not byte-equal to a declared name but is strings.EqualFold to one. Verified as exactly as wide as the decoder rather than wider -- ASCII upper, ASCII title, long-s and Kelvin all agree between EqualFold and the real consumer, so this does not repeat round 2's predicate-broader-than-the-narrowing finding."
    },
    {
      "file": "narrower-kid-short-non-ascii.json",
      "why": "A `kid` of a single non-ASCII letter. The consumer accepts it \u2014 2 UTF-8 bytes, not whitespace, non-empty after TrimSpace \u2014 and the schema's printable-ASCII class refuses it. Executed against the real Keys(): ACCEPTS.\n\nFOUND BY ROUND 4, AND IT IS THE PREDICATE-BROADER-THAN-ITS-FIXTURES SHAPE. The `kid` predicate exempts ANY character outside printable non-space ASCII, and the fixtures documented only NUL and surrounding whitespace \u2014 both of which read as malformed. This one does not: `\u00e9` is a perfectly reasonable key identifier that the consumer would accept in production, and the schema's refusal of it is a real narrowing that nothing recorded. The predicate was right and its documentation was a subset of what it excused."
    },
    {
      "file": "narrower-kid-embedded-tab.json",
      "why": "An embedded TAB. The consumer accepts it -- TrimSpace only strips the ENDS, so an interior control character survives and the value is non-empty.\n\nADDED AFTER ROUND 4's RULING. The kid exemption used to be a CHARACTER CLASS -- any character outside printable non-space ASCII -- which excused this whole family on the strength of three fixtures, and mixed narrowings together with cases where both sides refuse. A class predicate that exempts members the consumer ACCEPTS is the predicate-broader-than-its-fixtures finding at a coarser grain. The exemption is now keyed to the documented kids themselves, so an undocumented one fails naming the input."
    },
    {
      "file": "narrower-kid-embedded-newline.json",
      "why": "An embedded newline, same mechanism as the tab. Worth its own fixture because a newline in an identifier is what makes a log line ambiguous, and the consumer will take it.\n\nADDED AFTER ROUND 4's RULING. The kid exemption used to be a CHARACTER CLASS -- any character outside printable non-space ASCII -- which excused this whole family on the strength of three fixtures, and mixed narrowings together with cases where both sides refuse. A class predicate that exempts members the consumer ACCEPTS is the predicate-broader-than-its-fixtures finding at a coarser grain. The exemption is now keyed to the documented kids themselves, so an undocumented one fails naming the input."
    },
    {
      "file": "narrower-kid-embedded-ideographic-space.json",
      "why": "U+3000 IDEOGRAPHIC SPACE in the INTERIOR. The consumer accepts it, and this is the pair to the reject-side case where a kid of U+3000 ALONE is refused by both -- TrimSpace treats it as space at the ends and not in the middle. Same character, two verdicts, decided by position.\n\nADDED AFTER ROUND 4's RULING. The kid exemption used to be a CHARACTER CLASS -- any character outside printable non-space ASCII -- which excused this whole family on the strength of three fixtures, and mixed narrowings together with cases where both sides refuse. A class predicate that exempts members the consumer ACCEPTS is the predicate-broader-than-its-fixtures finding at a coarser grain. The exemption is now keyed to the documented kids themselves, so an undocumented one fails naming the input."
    },
    {
      "file": "narrower-kid-cjk.json",
      "why": "A single CJK character. Not whitespace, not a control, 3 UTF-8 bytes: the consumer accepts it without reservation. The most ordinary identifier in this list and the schema refuses it.\n\nADDED AFTER ROUND 4's RULING. The kid exemption used to be a CHARACTER CLASS -- any character outside printable non-space ASCII -- which excused this whole family on the strength of three fixtures, and mixed narrowings together with cases where both sides refuse. A class predicate that exempts members the consumer ACCEPTS is the predicate-broader-than-its-fixtures finding at a coarser grain. The exemption is now keyed to the documented kids themselves, so an undocumented one fails naming the input."
    },
    {
      "file": "narrower-kid-combining-mark.json",
      "why": "ASCII 'a' followed by U+0301 COMBINING ACUTE ACCENT. Two code points that render as one glyph, which is why a byte bound and a visual reading disagree about this value.\n\nADDED AFTER ROUND 4's RULING. The kid exemption used to be a CHARACTER CLASS -- any character outside printable non-space ASCII -- which excused this whole family on the strength of three fixtures, and mixed narrowings together with cases where both sides refuse. A class predicate that exempts members the consumer ACCEPTS is the predicate-broader-than-its-fixtures finding at a coarser grain. The exemption is now keyed to the documented kids themselves, so an undocumented one fails naming the input."
    }
  ],
  "narrower_than_consumer_note": "THE CATEGORY THIS CORPUS WAS MISSING, added after codex round 1 blocked the first version.\n\nThese documents are ACCEPTED by the real consumer and REFUSED by the schema. Each is a deliberate narrowing, and each is here because the first version STATED its narrowings in prose and got the list wrong: it declared two, and there were five.\n\nThey cannot be reject entries. TestRejectedJWKSFixturesAreAlsoRefusedByTheRealConsumer asserts every reject fixture is refused by the consumer too -- the check that stops the schema drifting stricter than the code it describes. Filing a narrowing there would force that test to be weakened, and it is the most valuable test in the file.\n\nWHY PROSE WAS NOT ENOUGH, stated plainly because it is the lesson: that test PASSED on the blocked version. It passed because not one of the 20 reject fixtures happened to exercise any undeclared narrowing. A correct instrument fed a corpus that cannot trip it reports coverage it does not have. Each row below turns one sentence of prose into an assertion executed in both directions.\n\nROUND 2 BLOCKED THIS CATEGORY FOR BEING INCOMPLETE, which is the round-1 defect committed inside its own fix. `use: \"\"` was listed in the schema description and exempted by a harness predicate, and had no fixture. A predicate without a fixture is worse than prose alone: prose merely fails to test the narrowing, while a predicate actively EXCUSES the disagreement, so the differential stays green over an asymmetry nothing asserts. The invariant is now enforced mechanically -- every predicate must have a fixture and every predicate must be exercised -- rather than by remembering to add both.\n\nROUND 3 BLOCKED ON A SIXTH, and the lesson is about PREDICATES rather than fixtures. The predicate/fixture correspondence test added after round 2 passed, because every predicate did have a fixture. What it cannot check is whether a predicate says what it MEANS: the case-variant predicate tested ASCII lowercasing while the consumer folds Unicode, so it named a narrower class than the one it was exempting. Two independent errors cancelled -- the predicate was too narrow AND the generator could not produce the shape -- and either alone would have shown up. Where a predicate mirrors a runtime behaviour, it must be checked against that runtime and not against its own description.\n\nTHE KID EXEMPTION IS ORACLE-DERIVED, NOT A CHARACTER CLASS. Ruled after round 4. The rule is stated once and has no alphabet in it: schema refuses AND consumer refuses is an AGREEMENT and needs nothing; schema refuses AND consumer ACCEPTS must match a documented narrowing fixture, and otherwise fails naming the input. The previous character-class predicate excused an entire family -- tabs, newlines, interior ideographic space, CJK, combining marks -- that no fixture demonstrated, while also covering cases where both sides refuse and the exemption could never fire. One predicate standing for a class whose members behave differently is how a seventh narrowing hid behind three fixtures.",
  "known_gap_trailing_content": "A mounted JWKS may carry a SECOND JSON value after a valid document and the consumer silently ignores it: Keys() decodes with a streaming json.Decoder, which reads exactly one value and never inspects the remainder. Executed -- a valid document followed by null, and a file holding two whole JWKS documents, are BOTH accepted, yielding only the first document's keys.\n\nThis corpus cannot express it: every fixture here is a single JSON document and the schema never sees bytes. Not hypothetical for rotation either -- cmd/query-api's CheckJWKS carries a comment describing exactly this, where a two-document file with kid old then kid new makes verification fail on new while a naive health check reports the instance healthy. Found by codex round 1. FOLDED INTO CHAOS-4951, which CF retitled to cover the raw-bytes boundary as a whole -- refuse duplicate members AND trailing content before decode, requiring EOF after the single document. The case-insensitive field match and the use:null acceptance are recorded there too, as a decision the Go fix must make explicitly: refuse, or document with a test. One ticket, one manifest reference.",
  "differential_harness_note": "The fixture lists below are NOT the whole check. TestTheSchemaAndTheRealConsumerAgreeExceptOnDeclaredNarrowings generates 2000 documents by mutating a valid baseline and asserts the schema and the real consumer reach the same verdict, except where a declared narrowing or a client-enforced rule explains the difference. Both exemption lists are PREDICATES in that file, so a new member of a known class passes and a genuinely new class fails.\n\nIt exists because the hand-written corpus could not catch what round 1 caught: a corpus contains only the cases someone thought of, and the test policing that direction was green while four narrowings went undeclared. Borrowed from lane-auth-wave1, who built the same shape with PostgreSQL as the oracle after their own corpus missed a case for the identical reason.\n\nMutation-proven in both directions, each with the mutation itself verified as applied: deleting the case-insensitivity predicate reports 148 undeclared narrowings; widening the x pattern to admit a 42-character key reports documents the consumer refuses. The first attempt at the former was a silent no-op -- the edit did not match after formatting -- and a mutation control that did not mutate proves nothing, so the applied change is now asserted before the run."
}
