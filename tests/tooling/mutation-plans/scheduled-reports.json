{
  "schema_version": 1,
  "name": "scheduled-reports",
  "build": [
    "go",
    "build",
    "-tags",
    "integration",
    "./..."
  ],
  "mutations": [
    {
      "id": "M16",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "   AND report.org_id = job.org_id\nWHERE job.job_type",
      "replace": "\nWHERE job.job_type",
      "rationale": "Tenant isolation. reports.execution_trigger._require_schedule rejects report.org_id != job.org_id, and the schema enforces only the schedule_id foreign key, so legacy data can point one tenant's job at another tenant's report. The occurrence is filed under the JOB's organization while the global report worker executes the REPORT's tenant data, so dropping this condition crosses tenants.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestCrossTenantJobAndReportAreNeverPaired",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M17",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tif err := refuseAmbiguousReportSchedules(ctx, tx); err != nil {\n\t\treturn Outcome{}, err\n\t}",
      "replace": "\tif false {\n\t\treturn Outcome{}, nil\n\t}",
      "rationale": "The ambiguity guard must be unlocked. Checking the post-SKIP-LOCKED candidate set instead lets a concurrent on-demand trigger hide a sibling report, so the sweep sees one row, passes, and materializes an arbitrary sibling while advancing the shared job's next_run_at.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestAmbiguousScheduleIsRefusedEvenWhenOneReportIsLocked",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M18",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tcase handoffStatus == nil:\n\t\treturn *linkedRunID, true, nil",
      "replace": "\tcase handoffStatus == nil:\n\t\treturn \"\", false, nil",
      "rationale": "Restores the original crash-window stall. A pending run with neither an outbox row nor an abandonment fact was never published, so refusing to re-arm leaves it permanently undelivered behind a healthy-looking schedule. A retained abandonment is a different state and must never reach this arm.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestReplayRearmsAPendingRunWhoseHandoffWasNeverPublished",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M19",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "handoffStatus != nil && *handoffStatus == outboxDeadStatus",
      "replace": "handoffStatus != nil && *handoffStatus == outboxDeadStatus && false",
      "rationale": "A spent delivery budget must be reported, not re-armed. Re-publishing collapses on the outbox dedupe key, so it delivers nothing while reporting success \u2014 a false pass worse than the stall it tries to fix.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestSpentHandoffIsReportedAsDegradedNotSkippedSilently",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M20",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tif *runStatus != pendingReportRunStatus {\n\t\treturn \"\", false, nil\n\t}",
      "replace": "\tif *runStatus != pendingReportRunStatus && false {\n\t\treturn \"\", false, nil\n\t}",
      "rationale": "Running, succeeded, failed and canceled are the report handler's states. Re-arming any of them duplicates execution or resurrects work an operator cancelled.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestReplayIgnoresARunThatIsNoLongerPending",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M21",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tdefault:\n\t\treturn \"\", false, nil\n\t}\n}",
      "replace": "\tdefault:\n\t\treturn *linkedRunID, true, nil\n\t}\n}",
      "rationale": "A live handoff is delivery in flight and the relay owns it. Re-arming adds a second delivery path for work that is already guaranteed.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestReplayLeavesALiveHandoffAlone",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M23",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tcase handoffAbandoned || (handoffStatus != nil && *handoffStatus == outboxDeadStatus):\n\t\treturn \"\", false, fmt.Errorf(\n\t\t\t\"%w: report run %s is pending but its handoff exhausted its delivery budget\",\n\t\t\tErrScheduledReportUndeliverable, *linkedRunID,\n\t\t)\n\tcase handoffStatus == nil:\n\t\treturn *linkedRunID, true, nil",
      "replace": "\tcase handoffAbandoned || (handoffStatus != nil && *handoffStatus == outboxDeadStatus):\n\t\treturn *linkedRunID, true, nil\n\tcase handoffStatus == nil:\n\t\treturn \"\", false, fmt.Errorf(\n\t\t\t\"%w: report run %s is pending but its handoff exhausted its delivery budget\",\n\t\t\tErrScheduledReportUndeliverable, *linkedRunID,\n\t\t)",
      "rationale": "Highest-value mutation on the replay path: it keeps both arms live and exchanges them. Never-published handoffs must re-arm, while both live dead rows and retained abandonments must stay degraded; swapping the arms inverts all three states.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestReplayRearmsAPendingRunWhoseHandoffWasNeverPublished|TestSpentHandoffIsReportedAsDegradedNotSkippedSilently|TestDeadHandoffRemainsUndeliverableAfterTerminalRetention",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M26",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\t\trequest, rearm, err := producer.replayExisting(ctx, tx, entry.candidate, entry.scheduledFor)\n\t\tif err != nil {",
      "replace": "\t\trequest, rearm, err := producer.replayExisting(ctx, tx, entry.candidate, entry.scheduledFor)\n\t\tif err != nil && false {",
      "rationale": "Swallows the undeliverable error instead of surfacing it, so a spent handoff is silently skipped with no degraded reason \u2014 the stranded run becomes invisible and the schedule looks idle. This pins the DETECTION half of the H1 fix. The isolation half is M32: these are separate properties and a single mutation cannot pin both, which the kill site is what revealed.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestOneTenantsStrandedRunDoesNotBlockAnotherTenant",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "CHAOS-3160-M1",
      "file": "internal/joboutbox/repository.go",
      "find": "\t\t\tFROM expired\n\t\t\tWHERE status = 'dead'\n\t\t\tON CONFLICT (dedupe_key) DO NOTHING",
      "replace": "\t\t\tFROM expired\n\t\t\tWHERE status = 'dead' AND false\n\t\t\tON CONFLICT (dedupe_key) DO NOTHING",
      "rationale": "Dead-row retention must record the durable abandonment fact before deleting full delivery state. Disabling the insert must make retention preserve the dead row rather than erase the only evidence, so the exact lifecycle test fails at the retention boundary.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "^TestDeadHandoffRemainsUndeliverableAfterTerminalRetention$",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "CHAOS-3160-M2",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tcase handoffAbandoned || (handoffStatus != nil && *handoffStatus == outboxDeadStatus):",
      "replace": "\tcase false || (handoffStatus != nil && *handoffStatus == outboxDeadStatus):",
      "rationale": "Report replay must consult the retained abandonment fact after the full dead outbox row is gone. Disabling only that clause leaves the live-dead guard intact but incorrectly treats retained abandonment as never-published work and re-arms a fresh delivery budget.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "^TestDeadHandoffRemainsUndeliverableAfterTerminalRetention$",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M27",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tfor _, entry := range replays {",
      "replace": "\tfor _, entry := range append([]dueEntry(nil), dueEntries...) {",
      "rationale": "Proves the two-pass ordering is load-bearing. Replaying every due entry rather than only the unclaimed ones lets replays compete with new claims for the budget AND re-arms occurrences pass one just claimed, publishing the same run id twice from one occurrence. The duplicate-handoff invariant is what detects it: no count or total-runs assertion can, because the durable graph is unchanged either way. Only a MIXED sweep with budget left over can detect it: when pass one fills the budget, every pass-two iteration short-circuits and the mutation changes nothing.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "12m",
          "-run",
          "TestMixedNewAndReplayedSweepPublishesEachRunOnce",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M24",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "ON CONFLICT DO NOTHING\nRETURNING occurrence_id",
      "replace": "RETURNING occurrence_id",
      "rationale": "The occurrence claim must be an atomic insert-or-verify. Without ON CONFLICT the loser of a race raises a unique violation instead of reporting a clean duplicate. This is the mutation the ORIGINAL concurrency test survived: FOR UPDATE OF job, report SKIP LOCKED serializes the two sweeps so the loser never reaches the INSERT, which is why the focused claim test exists alongside the full-sweep one. Do not collapse them.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestOccurrenceClaimIsAtomicUnderTrueContention",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "C1",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tif linkedRunID == nil || runStatus == nil {",
      "replace": "\tif runStatus == nil {",
      "rationale": "Clause-level mutation of the replay guard. Compound predicates are mutated clause by clause because a unit mutation of this very condition reported KILLED while an individual clause inside it was both unasserted and wrong (see C3).",
      "expected_survivor_reason": "Genuine redundancy, not a coverage gap. A NULL report_run_id makes the LEFT JOIN yield a NULL run status too, so the runStatus clause alone covers every reachable state and there is nothing left to assert. The clause is KEPT because it guards the *linkedRunID dereference below, which would nil-panic if the join were ever narrowed to an inner join. C1 and C2 together are the reference instance of the third survivor class: a mutation that removes real redundancy, distinct from an invalid mutation and from a missing test.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestReplayFailsWhenTheOccurrenceNoLongerLinksItsRun|TestPythonAuthoredOccurrenceIsRecognisedNotRejected",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "C2",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tif linkedRunID == nil || runStatus == nil {",
      "replace": "\tif linkedRunID == nil {",
      "rationale": "The mirror of C1: the other clause of the same compound guard, mutated independently.",
      "expected_survivor_reason": "Redundant for the mirror reason: the foreign key makes 'links a run that does not exist' unreachable, so the linkedRunID clause alone covers every reachable state. KEPT because it would still hold if the foreign key were relaxed.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestReplayFailsWhenTheOccurrenceNoLongerLinksItsRun|TestPythonAuthoredOccurrenceIsRecognisedNotRejected",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "C3",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tif linkedRunID == nil || runStatus == nil {",
      "replace": "\tif linkedRunID == nil || runStatus == nil || *linkedRunID != scheduledReportRunID(occurrenceID) {",
      "rationale": "Restores the coexistence bug, decision D14. Python allocates ReportRun.id with uuid4, so a Python-authored occurrence links an id the Go derivation cannot produce. Requiring equality failed every Go sweep that met one, and because last_run_at does not advance while a run is pending, the same due time recurs \u2014 closing the schedule's readiness for as long as such an occurrence existed. Run identity is derived to CREATE, never to RECOGNISE.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestPythonAuthoredOccurrenceIsRecognisedNotRejected",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "C4",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tcase handoffStatus == nil:\n\t\treturn *linkedRunID, true, nil",
      "replace": "\tcase handoffStatus == nil:\n\t\treturn scheduledReportRunID(occurrenceID), true, nil",
      "rationale": "The other half of D14: the re-armed handoff must carry the LINKED run's id so its outbox dedupe key matches whatever the other runtime enqueued. Publishing the derived id would name a run that does not exist, turning a scheduler-visible fault into a permanent worker failure.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestPythonAuthoredOccurrenceIsRecognisedNotRejected",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M28",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\t\treturn entries[first].scheduledFor.Before(entries[second].scheduledFor)",
      "replace": "\t\treturn entries[second].scheduledFor.Before(entries[first].scheduledFor)",
      "rationale": "Inverts fairness to least-overdue-first. The two-pass split guarantees only AGGREGATE progress; PER-REPORT progress comes from most-overdue-first ordering. Reversed, a carried-over report is displaced by every newer arrival and starves indefinitely \u2014 the same starvation as the budget-refill bug, one level up, and the reason the bound must depend on population rather than arrival rate.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "12m",
          "-run",
          "TestCarriedOverReportLandsUnderContinuousArrival",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M29",
      "file": "internal/scheduler/fixed/loop.go",
      "find": "\t\tif schedule.Evaluated {\n\t\t\tstate.degraded = schedule.Degraded\n\t\t}",
      "replace": "\t\tstate.degraded = schedule.Degraded",
      "rationale": "Restores the unconditional overwrite. The loop polls far more often than any schedule is due, so a window that never ran the producer cleared a live degraded reason on the very next poll \u2014 making a permanent fault visible for one poll interval and invisible to any realistic scrape. This is what made the whole per-report degraded fix unobservable in practice, so the fix and its signal must be mutated separately. Must be proved through the loop's exported metric, not the engine's returned ScheduleResult: a test that only inspects Step's return never reaches Loop.record.",
      "proof": [
        [
          "go",
          "test",
          "-count=1",
          "-timeout",
          "5m",
          "-run",
          "TestStoredDegradedReasonSurvivesNonEvaluatingWindows",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M30",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\t\treturn entries[first].candidate.ReportID < entries[second].candidate.ReportID",
      "replace": "\t\treturn false",
      "rationale": "Removes the tie-break, leaving the order stable rather than total. Ties are the common case, not an edge case \u2014 reports sharing a cron and last run all have the same due instant. Without the tie-break the order depends on arrival sequence, and two schedulers do NOT read the same rows because SKIP LOCKED gives each a different subset, so replica agreement would rest on a clause in another function.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "12m",
          "-run",
          "TestFairnessOrderIsTotalAndReplicaStable",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M31",
      "file": "internal/scheduler/fixed/engine.go",
      "find": "\t\tif evaluated {\n\t\t\toutcome.Evaluated = true",
      "replace": "\t\tif evaluated || true {\n\t\t\toutcome.Evaluated = true",
      "rationale": "Claims every window evaluated the schedule, including duplicate claims and cold-start baselines that never ran the producer. That reintroduces the M6 defect from the engine side rather than the loop side: a window with no verdict would assert one, clearing a live degraded reason. The replacement deliberately keeps `evaluated` referenced: an earlier version used `if true`, which made the variable unused and killed the mutation with a COMPILE error, so no test ran and the kill was not evidence about any assertion. Only reachable where an occurrence IS due but the producer does not run - a cold-start baseline or a duplicate claim - so a test exercising ordinary due/not-due windows cannot detect it.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "TestColdStartWindowReportsNoEvaluation",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M32",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\t\t\tif errors.Is(err, ErrScheduledReportUndeliverable) {",
      "replace": "\t\t\tif false && errors.Is(err, ErrScheduledReportUndeliverable) {",
      "rationale": "Lets ErrScheduledReportUndeliverable propagate out of Produce, which is the H1 regression itself: the engine rolls the transaction back, so ONE tenant's stranded run discards every other tenant's freshly materialized run and handoff \u2014 on every tick, permanently, because last_run_at never advances while a run is pending. This pins the ISOLATION half; M26 pins detection. Python is strictly more available here, dispatching each row independently and bypassing the outbox entirely.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "12m",
          "-run",
          "TestOneTenantsStrandedRunDoesNotBlockAnotherTenant",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M33",
      "file": "internal/scheduler/fixed/engine.go",
      "find": "\treturn 1, 0, total, 0, degraded, true, nil",
      "replace": "\treturn 1, 0, total, 0, \"\", true, nil",
      "rationale": "Drops the degraded reason from the MATERIALIZED branch, so a condition reported alongside produced work never reaches telemetry. This survived before the seam test existed: one test injected synthetic results into Loop.record without ever calling Step, another drove the real engine but asserted only returned values, so both halves were covered and the join between them was not.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "12m",
          "-run",
          "TestProducedWorkWithDegradationReachesTheExportedGauge",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "M34",
      "file": "internal/scheduler/fixed/engine.go",
      "find": "\tif status == OccurrenceSkipped {\n\t\treturn 1, 0, 0, 1, degraded, true, nil\n\t}",
      "replace": "\tif status == OccurrenceSkipped {\n\t\tif degraded == \"\" {\n\t\t\tdegraded = reason\n\t\t}\n\t\treturn 1, 0, 0, 1, degraded, true, nil\n\t}",
      "rationale": "Restores the promotion of an ordinary SkipReason into Degraded \u2014 a defect this change introduced into SHARED engine behaviour and that reached a producer it did not write. Because Degraded now persists until a schedule next evaluates, promoting a skip makes it latch for a full period: on the weekly remaining-metrics fan-out, 'no_active_organizations' stays raised for a week after organizations are added. A skip is 'nothing to do'; degraded is 'something is wrong'. The ledger is unaffected either way \u2014 Complete still receives the original SkipReason. Must be proved THROUGH the engine: the promotion lives in runOccurrence, so a test handing Loop.record a synthetic result never crosses the boundary the defect is on.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "12m",
          "-run",
          "TestSkipReasonDoesNotReachTheGaugeThroughTheEngine",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "CHAOS-3150-M1-audit-duplicates",
      "file": "src/dev_health_ops/alembic/versions/0096_enforce_unique_saved_report_schedule.py",
      "find": "    if duplicate_schedules:\n        raise RuntimeError(_format_duplicate_error(duplicate_schedules))",
      "replace": "    if duplicate_schedules and False:\n        raise RuntimeError(_format_duplicate_error(duplicate_schedules))",
      "rationale": "The pre-DDL audit must fail with exact schedule and report identities. Disabling only its positive-result clause would fall through to a raw UniqueViolation, which is the mid-migration failure CHAOS-3150 requires operators to avoid.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/alembic/versions/0096_enforce_unique_saved_report_schedule.py"
      ],
      "proof": [
        [
          "pytest",
          "--noconftest",
          "-q",
          "tests/test_saved_report_schedule_unique_postgres_migration.py::test_migration_refuses_existing_duplicates_with_exact_offending_rows"
        ]
      ]
    },
    {
      "id": "CHAOS-3150-M2-audit-write-lock",
      "file": "src/dev_health_ops/alembic/versions/0096_enforce_unique_saved_report_schedule.py",
      "find": "LOCK TABLE public.saved_reports IN SHARE ROW EXCLUSIVE MODE",
      "replace": "LOCK TABLE public.saved_reports IN SHARE UPDATE EXCLUSIVE MODE",
      "rationale": "The audit and ALTER must observe one stable write set. SHARE UPDATE EXCLUSIVE does not conflict with an INSERT's ROW EXCLUSIVE lock, so a duplicate can arrive after the audit and turn the planned operator error into a raw DDL failure.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/alembic/versions/0096_enforce_unique_saved_report_schedule.py"
      ],
      "proof": [
        [
          "pytest",
          "--noconftest",
          "-q",
          "tests/test_saved_report_schedule_unique_postgres_migration.py::test_audit_lock_prevents_a_duplicate_insert_between_check_and_constraint"
        ]
      ]
    },
    {
      "id": "CHAOS-3150-M3-null-schedules",
      "file": "src/dev_health_ops/alembic/versions/0096_enforce_unique_saved_report_schedule.py",
      "find": "    WHERE schedule_id IS NOT NULL",
      "replace": "    WHERE TRUE",
      "rationale": "Only non-null schedule ownership is unique. Multiple manual reports intentionally have NULL schedule_id; treating NULL as a duplicate would block valid installations before PostgreSQL's unique constraint, which itself permits multiple NULLs.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/alembic/versions/0096_enforce_unique_saved_report_schedule.py"
      ],
      "proof": [
        [
          "pytest",
          "--noconftest",
          "-q",
          "tests/test_saved_report_schedule_unique_postgres_migration.py::test_unique_constraint_allows_multiple_unscheduled_reports_and_downgrades"
        ]
      ]
    },
    {
      "id": "CHAOS-3150-M4-storage-column",
      "file": "src/dev_health_ops/alembic/versions/0096_enforce_unique_saved_report_schedule.py",
      "find": "    op.create_unique_constraint(_CONSTRAINT, _TABLE, [\"schedule_id\"])",
      "replace": "    op.create_unique_constraint(_CONSTRAINT, _TABLE, [\"id\"])",
      "rationale": "The storage rule is one saved report per non-null schedule_id. A named unique constraint on the already-unique primary key would let the exact ambiguous join state survive while making schema inspection look superficially complete.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/alembic/versions/0096_enforce_unique_saved_report_schedule.py"
      ],
      "proof": [
        [
          "pytest",
          "--noconftest",
          "-q",
          "tests/test_saved_report_schedule_unique_postgres_migration.py::test_application_head_rejects_a_second_report_for_one_schedule"
        ]
      ]
    },
    {
      "id": "CHAOS-3150-M5-model-column",
      "file": "src/dev_health_ops/models/reports.py",
      "find": "        UniqueConstraint(\"schedule_id\", name=\"uq_saved_reports_schedule_id\"),",
      "replace": "        UniqueConstraint(\"id\", name=\"uq_saved_reports_schedule_id\"),",
      "rationale": "The SQLAlchemy metadata used by tests and schema tooling must declare the same schedule_id invariant as Alembic. Reusing the migration's constraint name on id would create a false parity signal.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/models/reports.py"
      ],
      "proof": [
        [
          "pytest",
          "--noconftest",
          "-q",
          "tests/models/test_reports.py::test_saved_report_model_declares_one_report_per_schedule"
        ]
      ]
    },
    {
      "id": "CHAOS-3159-M1-due-page-predicate",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "      OR job.next_run_at <= $2",
      "replace": "      OR job.next_run_at > $2",
      "rationale": "Only schedules whose durable marker is due may consume the bounded SQL page. Inverting this clause lets one tenant's 20,001 future rows fill the page and hide another tenant's due report, recreating the exact cross-tenant availability failure without the old allocation error.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "8m",
          "-run",
          "^TestOneTenantsRowsBeyondTheFormerReadGuardDoNotBlockAnotherTenant$",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "CHAOS-3159-M2-lock-page-bound",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "\tmaximumScheduledReportCandidatesPerOccurrence = maximumScheduledReportsPerOccurrence + 1",
      "replace": "\tmaximumScheduledReportCandidatesPerOccurrence = maximumScheduledReportsPerOccurrence + 2",
      "rationale": "The transaction may lock exactly the 500-item work budget plus one row proving a remainder. Growing that page changes the operator-facing lock-footprint contract; the proof uses an independent literal 501 rather than copying this production constant as its oracle.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "5m",
          "-run",
          "^TestSweepReadsOneBoundedPageAndObservesTheRemainder$",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "CHAOS-3159-M3-new-before-replay",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "ORDER BY replay.occurrence_id IS NOT NULL,",
      "replace": "ORDER BY replay.occurrence_id IS NULL,",
      "rationale": "Never-materialized work must sort ahead of durable replays. Reversing this boolean lets the same replay population refill every bounded page under continuous arrival, so a specific carried-over report can remain due forever.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "8m",
          "-run",
          "^TestCarriedOverReportLandsUnderContinuousArrival$",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "CHAOS-3159-M4-replay-status-authority",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "    FROM public.scheduled_report_occurrences AS occurrence\n    WHERE occurrence.report_id = report.id",
      "replace": "    FROM public.scheduled_report_occurrences AS occurrence\n    JOIN public.report_runs AS run\n      ON run.id = occurrence.report_run_id\n     AND run.status IN ('pending', 'running')\n    WHERE occurrence.report_id = report.id",
      "rationale": "The bounded selector must not pre-decide replay semantics from run status. Terminal and link-corrupt occurrences still belong to replayExisting, which respectively stays quiet or fails visibly; filtering them here hides both states and falsely reports no due work.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "6m",
          "-run",
          "^(TestReplayIgnoresARunThatIsNoLongerPending|TestReplayFailsWhenTheOccurrenceNoLongerLinksItsRun)$",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "CHAOS-3159-M5-terminal-base-is-strict",
      "file": "internal/scheduler/fixed/reports.go",
      "find": "      AND occurrence.scheduled_for > COALESCE(report.last_run_at, report.created_at)",
      "replace": "      AND occurrence.scheduled_for >= COALESCE(report.last_run_at, report.created_at)",
      "rationale": "A terminal transition may advance last_run_at to the exact scheduled occurrence. Equality means that occurrence is finished and the next cron instant is owed; treating it as replay suppresses the following run behind an old terminal occurrence.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "5m",
          "-run",
          "^TestTerminalBaseEqualityIsNotSelectedAsReplay$",
          "./internal/scheduler/fixed/"
        ]
      ]
    },
    {
      "id": "CHAOS-3159-M6-new-schedule-marker",
      "file": "src/dev_health_ops/api/graphql/resolvers/reports.py",
      "find": "    setattr(job, \"next_run_at\", next_run_at)",
      "replace": "    setattr(job, \"next_run_at\", None)",
      "rationale": "A newly created report schedule must enter the SQL fairness order with the exact croniter-derived marker. Leaving it NULL makes rolling-upgrade repair consume the head of a bounded page and turns marker population into an unbounded backlog.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/api/graphql/resolvers/reports.py"
      ],
      "proof": [
        [
          "pytest",
          "-q",
          "tests/api/test_saved_reports.py::test_report_schedule_writes_the_exact_next_due_marker"
        ]
      ]
    },
    {
      "id": "CHAOS-3159-M7-updated-schedule-marker",
      "file": "src/dev_health_ops/api/graphql/resolvers/reports.py",
      "find": "            setattr(existing_job, \"next_run_at\", next_run_at)",
      "replace": "            setattr(existing_job, \"next_run_at\", None)",
      "rationale": "Changing an existing schedule's cron or report base must replace its paging marker without allocating a second job. A stale/NULL marker can misorder the report indefinitely relative to continuously arriving work.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/api/graphql/resolvers/reports.py"
      ],
      "proof": [
        [
          "pytest",
          "-q",
          "tests/api/test_saved_reports.py::test_report_schedule_writes_the_exact_next_due_marker"
        ]
      ]
    },
    {
      "id": "CHAOS-3159-M8-backfill-report-base",
      "file": "src/dev_health_ops/alembic/versions/0097_backfill_report_schedule_next_run.py",
      "find": "           COALESCE(report.last_run_at, report.created_at) AS marker_base",
      "replace": "           report.created_at AS marker_base",
      "rationale": "The backfill must derive a previously-run report's next marker from last_run_at, not creation. Using creation silently reintroduces an already-consumed occurrence and moves old reports ahead of genuinely overdue work.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/alembic/versions/0097_backfill_report_schedule_next_run.py"
      ],
      "proof": [
        [
          "pytest",
          "-q",
          "tests/test_0097_backfill_report_schedule_next_run_postgres.py::test_0097_backfills_exact_report_markers_and_leaves_other_jobs_unchanged"
        ]
      ]
    },
    {
      "id": "CHAOS-3159-M9-backfill-keyset-continuation",
      "file": "src/dev_health_ops/alembic/versions/0097_backfill_report_schedule_next_run.py",
      "find": "        after_job_id = str(rows[-1][\"job_id\"])",
      "replace": "        return processed",
      "rationale": "The migration's memory bound is a keyset loop, not a truncated first page. Stopping after one batch leaves every later report with its stale marker while still advancing the Alembic revision.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/alembic/versions/0097_backfill_report_schedule_next_run.py"
      ],
      "proof": [
        [
          "pytest",
          "-q",
          "tests/test_0097_backfill_report_schedule_next_run_postgres.py::test_0097_processes_more_than_one_bounded_marker_batch"
        ]
      ]
    },
    {
      "id": "CHAOS-3159-M10-five-field-backfill-guard",
      "file": "src/dev_health_ops/alembic/versions/0097_backfill_report_schedule_next_run.py",
      "find": "    if len(cron_expression.split()) != 5:\n        raise ValueError(\"cron must contain exactly five fields\")",
      "replace": "    if False:\n        raise ValueError(\"cron must contain exactly five fields\")",
      "rationale": "The backfill must fail transactionally on legacy expressions outside the scheduler's five-field dialect. Letting croniter interpret a six-field row can stamp a marker the Go evaluator can never reproduce, advance 0097, and leave a permanently fatal runtime row.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/alembic/versions/0097_backfill_report_schedule_next_run.py"
      ],
      "proof": [
        [
          "pytest",
          "-q",
          "tests/test_0097_backfill_report_schedule_next_run_postgres.py::test_0097_refuses_an_unevaluable_legacy_cron_without_partial_backfill"
        ]
      ]
    },
    {
      "id": "CHAOS3158-L1",
      "file": "internal/jobs/report/postgres.go",
      "find": "\t\tif leaseExpiresAt != nil && leaseExpiresAt.After(now) {",
      "replace": "\t\tif leaseExpiresAt != nil && !leaseExpiresAt.After(now) {",
      "rationale": "An expired running report must be reclaimable, while a live lease must remain exclusive. Inverting only the time comparison restores the permanent running-state stall and also makes an active holder reclaimable.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "^TestPostgresRunStoreReclaimsAnExpiredRunningRun$",
          "./internal/jobs/report/"
        ]
      ]
    },
    {
      "id": "CHAOS3158-L2",
      "file": "internal/jobs/report/postgres.go",
      "find": "\t\tif reclaimCount >= maxExecutionReclaims {",
      "replace": "\t\tif reclaimCount > maxExecutionReclaims {",
      "rationale": "The persisted reclaim count must terminalize at the declared bound. Moving the boundary by one permits an extra automatic execution holder and makes repeated crash recovery unbounded relative to the contract.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "^TestPostgresRunStoreReclaimsAnExpiredRunningRun$",
          "./internal/jobs/report/"
        ]
      ]
    },
    {
      "id": "CHAOS3158-L3",
      "file": "internal/jobs/report/postgres.go",
      "find": "  AND execution_claim_token = $2::uuid\n  AND execution_lease_expires_at > $4",
      "replace": "  AND execution_claim_token IS NOT NULL AND $2::uuid IS NOT NULL\n  AND execution_lease_expires_at > $4",
      "rationale": "Lease renewal must be fenced by the current claim token. Testing only lease time would let a worker paused before reclaim extend the new holder's lease and retain authority after it was replaced.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "^TestPostgresRunStoreReclaimsAnExpiredRunningRun$",
          "./internal/jobs/report/"
        ]
      ]
    },
    {
      "id": "CHAOS3158-L4",
      "file": "internal/jobs/report/postgres.go",
      "find": "  ON run.scheduled_occurrence_id = occurrence.occurrence_id",
      "replace": "  ON run.report_id = occurrence.report_id",
      "rationale": "Only the immutable occurrence linked to the terminal run may invalidate the schedule marker. Joining by report alone makes a manual run clear next_run_at whenever that report has any scheduled occurrence.",
      "proof": [
        [
          "go",
          "test",
          "-tags",
          "integration",
          "-count=1",
          "-timeout",
          "10m",
          "-run",
          "^TestPostgresRunStorePreservesArtifactCancellationAndNotificationSemantics$",
          "./internal/jobs/report/"
        ]
      ]
    },
    {
      "id": "CHAOS3158-L5",
      "file": "internal/jobs/report/report.go",
      "find": "\t\tticker := time.NewTicker(leaseDuration / 3)",
      "replace": "\t\tticker := time.NewTicker(leaseDuration * 100)",
      "rationale": "Long report work must renew well before the execution lease expires. Delaying the first renewal beyond the lease restores the crash-recovery race in which healthy long work loses its fence and a second holder starts.",
      "proof": [
        [
          "go",
          "test",
          "-count=1",
          "-timeout",
          "5m",
          "-run",
          "^TestRunWithLeaseRenewalRenewsBeforeLongWorkCompletes$",
          "./internal/jobs/report/"
        ]
      ]
    },
    {
      "id": "CHAOS3158-L6-migration-reclaim-index",
      "file": "src/dev_health_ops/alembic/versions/0098_add_report_run_execution_lease.py",
      "find": "        [\"status\", \"execution_lease_expires_at\"],",
      "replace": "        [\"status\", \"execution_reclaim_count\"],",
      "rationale": "The reclaim scan must use the status and lease-expiry access path. Indexing the count instead makes the migration look indexed but leaves expired-running discovery without its declared SQL-sortable key.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/alembic/versions/0098_add_report_run_execution_lease.py"
      ],
      "proof": [
        [
          "pytest",
          "--noconftest",
          "-q",
          "tests/test_report_run_execution_lease_postgres_migration.py::test_0098_adds_exact_execution_lease_schema"
        ]
      ]
    },
    {
      "id": "CHAOS3158-L7-migration-zero-reclaim-count",
      "file": "src/dev_health_ops/alembic/versions/0098_add_report_run_execution_lease.py",
      "find": "            server_default=\"0\",",
      "replace": "            server_default=\"1\",",
      "rationale": "A pre-0098 running row has not yet consumed a replacement holder. Giving it a reclaim count of one spends half of the bounded crash-recovery budget at migration time.",
      "build": [
        "python3",
        "-m",
        "py_compile",
        "src/dev_health_ops/alembic/versions/0098_add_report_run_execution_lease.py"
      ],
      "proof": [
        [
          "pytest",
          "--noconftest",
          "-q",
          "tests/test_report_run_execution_lease_postgres_migration.py::test_0098_makes_a_legacy_running_run_immediately_reclaimable"
        ]
      ]
    }
  ]
}
