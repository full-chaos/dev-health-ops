{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.budget-exhaustion.with-frame",
  "family": "deg",
  "group": 9,
  "status": "declared-blocked",
  "blocked_by": "CHAOS-3455",
  "proves": [
    "group 9 family deg.budget-exhaustion (provider-cost/token budget exhaustion after a partial frame already exists)"
  ],
  "question": "Did the assistant run out of budget for the repo \"meridian/web-app\" after it had already gathered some evidence?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": "subject.exact.meridian-web-app",
  "invariants": [
    {
      "category": "declared-blocked-placeholder",
      "check": "no_internal_error",
      "args": {},
      "assert": "Placeholder ONLY -- this case is status=declared-blocked (nothing runs against real production code/fixtures yet, see blocked_by). Lane 2a runner code (read directly, 2026-08-06) has no declared-blocked concept: case_schema.py requires every case file, unconditionally, to carry a non-empty invariants[] list, or the WHOLE corpus load raises (not a per-file skip). This entry exists purely to satisfy that mechanical requirement so the corpus stays loadable; it must NOT be read as an executed assertion about the blocked scenario.",
      "executable_today": false
    }
  ],
  "resolution_profile_ref": null,
  "fault_ref": null,
  "notes": "Real error-code mapping VERIFIED by reading terminal_frames.py directly (2026-08-06): its ORCHESTRATOR_ERROR_CODES/PUBLIC_OUTCOME_BY_ERROR_CODE totality-checked table maps 'cost_limit_reached' -> PublicOutcome.FAILED unconditionally, keyed purely by error code with no frame-state branch -- identical totality-table shape to the one that settled provider-fail.before-frame/after-frame's TEMPORARILY_UNAVAILABLE outcome (same code pattern, different code/outcome pair). orchestrator.py confirms 'cost_limit_reached' is a genuine, real production error code: BudgetExceeded (raised by ProviderBudget.require()/.add() when input/output token or provider-cost limits are exceeded) is caught and mapped via AgentProviderErrorCode.BUDGET_EXHAUSTED -> 'cost_limit_reached' (orchestrator.py's own code_map, ~line 3867). So the intended expected_public_outcome, once this id is unblocked, is FAILED -- for BOTH with-frame and without-frame instances identically, same lesson as provider-fail.before-frame/after-frame: frame timing does not change the outcome bucket, only whether a real frame survives in the transcript alongside the error terminal. Genuinely blocked today, though: checked provider_scripts.py's ScriptedDecision/HttpFaultError shapes and scripted_openai_service.py's fault-serving path directly -- the 6 real fault types (retry-storm-trigger, oversized-output, unsafe-error-text, slow-response, fail-before-frame, fail-after-frame) only ever inject an HTTP-error-shaped response or a delay; none sets AgentProviderErrorCode.BUDGET_EXHAUSTED, and ToolCallDecision/FinalAnswerDecision/DisambiguationDecision/RefusalDecision carry no settable token/cost 'usage' field a scripted decision could use to organically exceed ProviderBudget.require()/.add()'s real accounting either. There is therefore no scriptable path to cost_limit_reached today, distinct from and more fundamental than the deg.timeout family's 'no per-stage fault type' gap (that family at least has a plausible existing-mechanism stand-in for 5 of its 7 instances; this one has none). question/org_alias/user_alias/subject_class/subject_ref kept as documentation of the intended (currently-blocked) scenario -- a partial frame (e.g. a real query_metric.v1 result, same shape as provider-fail.after-frame's) already committed before the budget-exhaustion error fires. [blocked_by WIRED to CHAOS-3455 during Lane 2b ticket-filing pass, 2026-08-06 -- original free-text reason preserved verbatim here: 'runner: no budget/cost-limit fault type scripted yet'.]"
}
