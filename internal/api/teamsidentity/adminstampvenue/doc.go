// Package adminstampvenue holds the venue differential oracle for the naive
// created_at/updated_at timestamps of GET /admin/teams and GET
// /admin/identities (TestAdminTimestampVenueOracle): the real Python api and
// the dho api route set, compared as raw response text. It is its own
// package so the venue run has its own time budget, apart from the other api
// venues.
package adminstampvenue
