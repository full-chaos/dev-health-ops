{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "metric-compare.two-metrics.stale-source",
  "family": "metric-compare",
  "group": 1,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 1 bullet 11"
  ],
  "question": "Compare items completed against cyclomatic per kloc for the repo \"probe/source-stale\"",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": null,
  "expected_mention_texts": [
    "probe/source-stale"
  ],
  "invariants": [
    {
      "category": "no-silent-widening",
      "check": "scope_resolution_outcome_in",
      "args": {
        "from_profile": "expected_scope_resolution_outcome"
      },
      "assert": "A named subject mention that misses its intended target never silently widens to org-wide/broader scope; widening is only ever a disclosed, terminal answered_with_gaps/needs_clarification outcome.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker scope_resolution_outcome_in against this case's own resolution-profile expected_scope_resolution_outcome (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Pinned to sources.json's dedicated 'stale' matrix row (repo_full_name probe/source-stale, org primary) -- every row's last_synced aged past STALE_FALLBACK_GRACE (96h > 48h) via the post-generation ClickHouse mutation sources.json documents, so DataHealthService.inspect reads DataHealthState.STALE for this repo's commits/work_items sources. Not a subjects.json entry (subject_ref deliberately null, per this repo existing only as a sources.json fixture-matrix row, not a named subject) -- referenced directly by repo_full_name in the question instead, per REGISTRY-AMENDMENT.v1.md/CASE-SCHEMA precedent (case-scope.refused-with-grounding.json already reuses a sources.json-only row the same way). Must report per-source applicability distinguishing fresh vs stale rather than silently treating both metrics' underlying source as current -- the invariant's 'widening is only ever a disclosed... answered_with_gaps' clause generalizes cleanly from subject-scope widening to source-freshness widening: presenting a stale-backed metric with the same confidence as a fresh one IS a silent widening of what the data can actually support. [CORRECTED during Lane 2b codex round-1 remediation, 2026-08-06, from 'Compare items completed against cyclomatic per kloc for the probe/source-stale repo': question_interpreter.py's own module docstring states plainly 'a lowercase slug ... produces no mention' -- extract_mentions requires either a capitalized _NAME token or a QUOTED span adjacent to a closed kind noun (test_question_interpreter.py's proven pattern: 'How is repo \"dev-health-ops\" doing?'). The original bare, unquoted lowercase slug would extract ZERO typed mentions and silently degrade to org-wide/no-subject behavior -- exactly the false-pass risk codex's wrong-kind-collision finding named, generalized here to every other case that had the same bug.]"
}
