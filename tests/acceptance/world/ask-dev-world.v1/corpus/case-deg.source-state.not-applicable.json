{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.source-state.not-applicable",
  "family": "deg",
  "group": 9,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 9 family deg.source-state (sources.json per-source-class terminal-state matrix, state=not-applicable)"
  ],
  "question": "Is ACR evidence available for the repo \"meridian/web-app\"?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": "subject.exact.meridian-web-app",
  "invariants": [
    {
      "category": "no-silent-widening",
      "check": "scope_resolution_outcome_in",
      "args": {
        "from_profile": "expected_scope_resolution_outcome"
      },
      "assert": "A named subject mention that misses its intended target never silently widens to org-wide/broader scope; widening is only ever a disclosed, terminal answered_with_gaps/needs_clarification outcome.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker scope_resolution_outcome_in against this case's own resolution-profile expected_scope_resolution_outcome (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Grounded against sources.json's dedicated 'not-applicable' matrix row: NativeDataHealthReader.read hardcodes `if source == 'acr': SourceHealthObservation(source, False, False, warning='acr_optional')` for every org unconditionally -- realized_by is deliberately null/org-wide (no fixture rows needed, no repo-specific mechanism). The row's own mechanism text is explicit that this is 'org-wide-hardcoded', not tied to any one repo -- meridian/web-app is used here only as the NAMED SUBJECT the question is asked about (a real, resolvable, positive-control repo per subject.exact.meridian-web-app), while the not-applicable ACR signal itself applies identically regardless of which repo is named. Expected answered_with_gaps: a question specifically about ACR evidence must disclose that ACR is optional/not-applicable rather than silently omitting the fact or silently answering as if ACR were simply absent evidence like any other source."
}
