{
  "schema_version": "resolution-profile.v1",
  "profile_id": "deterministic-v1",
  "$comment": "CHAOS-3219 Wave 4 Phase 2. The active profile for today's deterministic (regex/substring + CHAOS-3388 alias-matching) resolution stack -- see README.md in this directory for the format and the CHAOS-3389 fold-in design this seam exists for. Lane 2a (this file's schema/loader owner) ships this empty; Lane 2b populates `cases` as each corpus case referencing `resolution_profile_ref: \"deterministic-v1\"` is authored. An empty `cases` map is valid (case_schema.load_resolution_profile accepts it) -- a case that cites this profile before its entry is added fails loud at expectation-resolution time (resolve_case_expectations), which is the correct, visible signal that authoring is incomplete, not a defect in this file.",
  "cases": {
    "adv.abuse.retry-storm": {
      "expected_public_outcome": "temporarily_unavailable",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": null,
      "notes": "Persistent 429 on every round exhausts the retry policy; rate_limited -> TEMPORARILY_UNAVAILABLE per terminal_frames.PUBLIC_OUTCOME_BY_ERROR_CODE. Same bare-slug resolution_path=null / expected_scope_resolution_outcome=exact split as provider-fail.before-frame/after-frame."
    },
    "adv.abuse.subject-set-mutation-attempt": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification",
      "notes": "Same fixture row and mechanism as tenant.cross-tenant-identifier-refused -- a cross-tenant identifier resolves to not_found regardless of whether it is a bare fresh mention or framed as an 'also add this' mutation attempt on an implied prior scope."
    },
    "adv.cross-tenant.organization-id": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification",
      "notes": "'Orbit' extracts as a single UNTYPED bare-name candidate (question_interpreter.py's untyped_name_candidates -- capitalized, does not open the sentence, so the single-word/sentence-opening discard rule does not apply); it is searched across every kind by subject_preflight (default requested_entity_kind=PROJECT is only a declared fallback label, per that module's own docstring) but primary org's own repo/project/team catalog has no row named 'Orbit' -- the sibling org's own display name is not itself a searchable catalog entity in the requester's org -- so the search comes back empty exactly like scope.no-match. Same triple as tenant.cross-tenant-identifier-refused / subject-label.unauthorized-candidate-excluded / scope.no-match, generalized from a repo-shaped identifier to an org-shaped one."
    },
    "adv.cross-tenant.project-id": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification",
      "notes": "Same fixture row and DataHealthService.inspect/ScopeResolutionService mechanism as tenant.cross-tenant-identifier-refused and adv.cross-tenant.repository-id, reused deliberately (see the case file's own notes) with the mention typed via the 'project' kind noun instead of 'repo'/'repository'. Triple copied verbatim from tenant.cross-tenant-identifier-refused's own profile entry: the authorization guard fires on the resolved scope's outcome, not on which closed kind-noun grammar typed the mention, so the expectation cannot differ by mention shape."
    },
    "adv.cross-tenant.repository-id": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification",
      "notes": "Direct group-8 mirror of tenant.cross-tenant-identifier-refused (group 1) -- identical fixture row, identical full-exact-identifier mention shape, identical DataHealthService.inspect/ScopeResolutionService guard, different question text only (provider_scripts.py's global question-uniqueness requirement, see the case file's own notes). Triple copied verbatim from that case's profile entry for the identical reasoning documented there."
    },
    "adv.injection-request.graphql": {
      "expected_public_outcome": "denied",
      "expected_scope_resolution_outcome": null,
      "resolution_path": "miss-clarification",
      "notes": "subject_class=n/a: refusal fires via the provider's scripted decision; expected_scope_resolution_outcome is null because no entity ever commits (the untyped, non-blocking mention candidates some of this family's questions mint never resolve to a real catalog entity -- see the case file's own notes)."
    },
    "adv.injection-request.mcp": {
      "expected_public_outcome": "denied",
      "expected_scope_resolution_outcome": null,
      "resolution_path": "miss-clarification",
      "notes": "subject_class=n/a: refusal fires via the provider's scripted decision; expected_scope_resolution_outcome is null because no entity ever commits (the untyped, non-blocking mention candidates some of this family's questions mint never resolve to a real catalog entity -- see the case file's own notes)."
    },
    "adv.injection-request.shell": {
      "expected_public_outcome": "denied",
      "expected_scope_resolution_outcome": null,
      "resolution_path": "miss-clarification",
      "notes": "subject_class=n/a: refusal fires via the provider's scripted decision; expected_scope_resolution_outcome is null because no entity ever commits (the untyped, non-blocking mention candidates some of this family's questions mint never resolve to a real catalog entity -- see the case file's own notes)."
    },
    "adv.injection-request.sql": {
      "expected_public_outcome": "denied",
      "expected_scope_resolution_outcome": null,
      "resolution_path": "miss-clarification",
      "notes": "subject_class=n/a: refusal fires via the provider's scripted decision; expected_scope_resolution_outcome is null because no entity ever commits (the untyped, non-blocking mention candidates some of this family's questions mint never resolve to a real catalog entity -- see the case file's own notes)."
    },
    "adv.injection-request.url": {
      "expected_public_outcome": "unsupported",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "subject_class=n/a: refusal fires via the provider's scripted decision; expected_scope_resolution_outcome is null because no entity ever commits (the untyped, non-blocking mention candidates some of this family's questions mint never resolve to a real catalog entity -- see the case file's own notes)."
    },
    "adv.injection-request.write": {
      "expected_public_outcome": "denied",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "subject_class=n/a: refusal fires via the provider's scripted decision; expected_scope_resolution_outcome is null because no entity ever commits (the untyped, non-blocking mention candidates some of this family's questions mint never resolve to a real catalog entity -- see the case file's own notes)."
    },
    "adv.malicious-content.links": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Same reasoning as adv.malicious-content.markdown's own profile entry -- ordinary healthy answer over the same real exact subject; the invariant under test is about link/URL rendering sanitization, not the terminal outcome bucket. [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- settled by the code (that functions own docstring), not a judgment call.]"
    },
    "adv.malicious-content.markdown": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Ordinary healthy answer over a real, unambiguous exact subject (subject.exact.meridian-web-app) via the proven-safe quoted/noun-adjacent mention pattern -- mirrors health.project.exact-subject / status.single-project.exact-subject's own triple. The invariant under test is about the CONTENT/rendering of that answer (markdown sanitization), not the terminal outcome bucket itself, exactly the same posture unrelated-evidence.named-subject's own profile entry documents for its own content-focused invariant. [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- settled by the code (that functions own docstring), not a judgment call.]"
    },
    "adv.no-person-level-output": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Quoted full team label reaches genuine EXACT resolution (same mechanism as workload.team.with-denominator). No production code computes per-engineer output at all, so the honest terminal is a real team-level answer with the individual-ranking request disclosed as an unanswerable-at-this-granularity limitation, not a hard denial."
    },
    "adv.oversized.question": {
      "expected_public_outcome": "unsupported",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "subject_preflight's MAX_MENTIONS bound rejects before any catalog round trip (diagnostic='oversized_mention_set_in_v1') -- ledger stays None, so both resolution_path and expected_scope_resolution_outcome are null."
    },
    "adv.oversized.result": {
      "expected_public_outcome": "failed",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": null,
      "notes": "The scripted oversized-output fault's >=262144-byte direct_summary exceeds contracts.py's LongText max_length=16_384 by roughly 16x -- contract validation fails, mapping to answer_validation_failed -> PublicOutcome.FAILED, not a graceful truncation."
    },
    "adv.oversized.subject-set": {
      "expected_public_outcome": "unsupported",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "subject_preflight's MAX_MENTIONS bound rejects before any catalog round trip (diagnostic='oversized_mention_set_in_v1') -- ledger stays None, so both resolution_path and expected_scope_resolution_outcome are null."
    },
    "adv.unsafe-error-text.provider": {
      "expected_public_outcome": "temporarily_unavailable",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": null,
      "notes": "safe_agent_provider_error's fallback classification (PROVIDER_UNAVAILABLE) discards the scripted fault's embedded secret/org-name canary entirely and substitutes a fixed catalog message -> provider_unavailable -> TEMPORARILY_UNAVAILABLE, same bare-slug resolution_path=null / expected_scope_resolution_outcome=exact split as the other Lane-1b-owned bare-slug questions in this corpus."
    },
    "adv.unsafe-error-text.source": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Real (non-scripted) DataHealthState.UNAVAILABLE for sibling's own repo, asked from sibling's own vantage -- resolves EXACT, degradation disclosed as a limitation, same answered_with_gaps shape as trust.source-health.stale's own already-adopted entry, just a more severe source-health state (unavailable vs stale)."
    },
    "attention.team.invalid-qualification-unknown": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "Real today-behavior, not a hedge: investments.py's investment_allocation_shift_observation reports attribution_present=False unconditionally (CHAOS-3394, confirmed open/Backlog), so any answer to this org-wide/unqualified question is honestly gapped by production code as it stands. No subject_ref (question spans the team catalog, not one named team). [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.]"
    },
    "deficiency.team.applicable-rule": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Same quoted-full-label EXACT-resolution mechanism as workload.team.with-denominator. Positive control: >=1 operational_deficiency_service.py rule genuinely fires against this team's fixture data. [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "deficiency.team.not-applicable-rule": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Team resolves cleanly; the gap is a deficiency rule scoped NOT_APPLICABLE for this team, disclosed explicitly rather than silently dropped as if it had passed clean."
    },
    "deg.acr-unavailable-native-usable": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Same not-applicable/acr_optional mechanism as deg.source-state.not-applicable, but asserting the OPPOSITE-emphasis outcome: because acr's required flag is False, its unavailability alone can never push DataHealthService.inspect's complete_eligible to False, so a general status question over meridian/web-app (DataHealthState.COMPLETE on every native source per sources.json's 'current' row) is fully answered, not degraded, by ACR's absence. [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- settled by the code (that functions own docstring), not a judgment call.] [CORRECTED to miss-clarification 2026-08-06 (CHAOS-3462 B6, adversarial round 3). COVERAGE WARNING -- the primary subject really does commit exact_match; the value is miss-clarification because resolution_path is a WHOLE-RUN scalar and this question also names an incidental untyped bare-name mention that never resolves. derive_resolution_path returns miss-clarification if ANY mention fails to commit, and the unresolved_untyped PROCEED branch persists that row alongside the committed one. So the run answers normally and is still stamped miss-clarification. This is a limitation of the derivation, not of the product -- see the class note in CASE-SCHEMA.v1.md. Recorded accurately rather than left to surface as a live-run mystery.] [RESTORED to deterministic-exact 2026-08-06 (CHAOS-3462 B6, team-lead ruling): this is the honest expectation -- the primary subject really does commit exact_match. It was briefly rewritten to miss-clarification, which was the 'rewrite an honest expectation to force green' anti-pattern. The reason the RUN derives miss-clarification is that resolution_path is a WHOLE-RUN scalar and this question also names an incidental untyped bare-name mention that never resolves; derive_resolution_path returns miss-clarification if ANY mention fails to commit. So the expectation stays true and the resolution_path_in invariant was REMOVED instead -- the same disposition B4 used for every other case whose path the derivation cannot honestly express. Every other invariant on the case is untouched. Re-assertable if the derivation ever becomes per-primary-subject.]"
    },
    "deg.optional-integration-not-mislabeled": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Subject resolves EXACT via the quoted noun-adjacent span 'the repo \"meridian/web-app\"'. data_health_service.py's NativeDataHealthReader hardcodes the acr source class to required=False, warning='acr_optional', for every org unconditionally -- DataHealthService.inspect's complete_eligible never gates on it, and operational_deficiency_service.py's Category 1 binding never raises a finding for it. The answer to the acr question itself is a clean, confident negative (no deficiency); expected_public_outcome=answered_with_gaps is unrelated to that negative and is instead the same universal legacy-stack wrapping fact every successful DevAnswer terminal in this corpus carries (terminal_frames.wrap_legacy_answer_as_frame)."
    },
    "deg.provider.unavailable": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": null,
      "resolution_path": "deterministic-exact",
      "notes": "Mechanistically identical fixture to readiness.capabilities.degraded (world.json degraded-profile via primary.degraded-readiness-user) -- a capability-readiness gate, not a subject/scope resolution attempt, hence resolution_path null (same basis as readiness.capabilities.degraded/unsupported-model). Mapping choice: readiness_failure_state('provider_unavailable') (readiness.py) maps to the 'degraded' ReadinessState bucket, the closest real fixture to a dedicated 'provider unavailable' readiness state (no such 7th ReadinessState member exists). Same honesty caveat carried from readiness.capabilities.degraded's own profile entry: the exact code path from a degraded-profile readiness_probe result to a live answered_with_gaps DevAnswer was not fully walked against a concrete implementation at authoring time -- provider_profile_override has no src/ references yet. Recorded here as the registry's asserted expectation, flagged for live confirmation once the corpus runner exists. [resolution_path DEFINED (CHAOS-3462 B4); the previous null was incorrect. Same evidence as readiness.capabilities.degraded: one correctly-quoted mention (repo 'meridian/web-app'), provider_profile_override unwired in src/, so the ordinary PROCEED path runs. Additionally decisive here: every append_resolution site in orchestrator.py sits inside the preflight block, strictly ahead of the model round loop where a provider failure surfaces -- a later provider_unavailable cannot retract rows already appended.] [COVERAGE WARNING, do not read this entry as resolved coverage (codex adversarial round-1, MEDIUM, accepted): the resolution_path value above is accurate -- the run really does write exact ledger rows -- but it is accurate BECAUSE this case currently takes the ORDINARY ready path. provider_profile_override has zero references in src/, so the degraded/unsupported readiness behaviour this case is named for is NOT exercised by any assertion here, and none of this case's invariants would notice if it regressed. Removing the resolution_path invariant would not close that gap either -- the case would still go green on its remaining invariants. The honest remedy is status=declared-blocked until the override is wired, which changes the active-case floor and is therefore a team-lead call, tracked rather than silently taken here.]"
    },
    "deg.provider.unsupported": {
      "expected_public_outcome": "unsupported",
      "expected_scope_resolution_outcome": null,
      "resolution_path": "deterministic-exact",
      "notes": "Mechanistically identical fixture to readiness.capabilities.unsupported-model (world.json unsupported-model-profile via primary.unsupported-model-user) -- resolution_path null for the same reason (a model-readiness gate, production_runtime.py's _resolve_provider_selection, is a pre-check independent of the named subject, not modeled as a subject/scope resolution attempt at all). UNSUPPORTED (not TEMPORARILY_UNAVAILABLE) because unsupported-model-profile's readiness value raises DevRuntimeUnavailable(code='model_not_supported'), and terminal_frames.py's PUBLIC_OUTCOME_BY_ERROR_CODE table maps 'model_not_supported' -> PublicOutcome.UNSUPPORTED explicitly. Same expectation as readiness.capabilities.unsupported-model, reused deliberately (same underlying code path, different registry vantage: group 9 degradation framing vs. group 1 readiness-capability framing). [SET to deterministic-exact 2026-08-06 (CHAOS-3462 adversarial round 2): executed interpret+preflight show the correctly quoted repo \"meridian/web-app\" mention takes the ordinary singular proceeded_committed_subject PROCEED and writes one row, committing an exact subject. The prior null was wrong. This value shares the SAME open blocker as the 44 sibling deterministic-exact entries: the docker-exec ledger read never supplies mention_text, so derive_resolution_path raises ResolutionPathError for a single-shot exact_match and the runner records a failed resolution_path_classifiable check. Recorded here as the honest expectation rather than falsified to miss-clarification to force green.]"
    },
    "deg.provisional-unapproved-rule": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Reaches EXACT via the quoted full team label 'Platform Reliability (Ground Control)', identical mechanism to workload.team.with-denominator/deficiency.team.applicable-rule. Every HEALTH_RULE_REGISTRY rule shipped today is calibration_state=provisional (operational_deficiency_service.py's own totality guarantee), so every categories-3-6/8 finding is shadow_only and never reaches the inventory -- disclosed as a scoping/coverage gap, not silently reported as either 'no deficiencies' (implying clean) or a fabricated approved finding. expected_public_outcome=answered_with_gaps for the same settled-by-code reason as the other cases in this fragment."
    },
    "deg.source-state.deleted": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification",
      "notes": "Cross-referenced, not re-derived: identical fixture (subjects.json subject.deleted.legacy-billing) and identical mechanism (projects.is_active=0 excludes it from the CHAOS-3380 identity catalog's is_active=1 filter) as the already-authored scope.deleted-subject, whose profile entry carries the exact same triple. Mirrored here verbatim rather than independently re-derived."
    },
    "deg.source-state.no-data": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Pinned to sources.json's dedicated no-data row (probe/source-no-data, org primary) -- deployments/work_items/ci_runs read DataHealthState.NO_DATA (watermark genuinely NULL, not merely old), distinguished from measured-zero (a metrics-layer DORAMetricsRecord value=0.0, not realized in this batch)."
    },
    "deg.source-state.not-applicable": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Pinned to sources.json's dedicated not-applicable row (acr source, org-wide-hardcoded via NativeDataHealthReader.read's unconditional `warning='acr_optional'` branch, realized_by null/no fixture rows) -- meridian/web-app is the named, resolvable subject the question is asked about; the ACR not-applicable disclosure itself applies org-wide regardless of repo. A question specifically about ACR evidence must disclose the not-applicable/optional status rather than silently omitting it. [CORRECTED to miss-clarification 2026-08-06 (CHAOS-3462 B6, adversarial round 3). COVERAGE WARNING -- the primary subject really does commit exact_match; the value is miss-clarification because resolution_path is a WHOLE-RUN scalar and this question also names an incidental untyped bare-name mention that never resolves. derive_resolution_path returns miss-clarification if ANY mention fails to commit, and the unresolved_untyped PROCEED branch persists that row alongside the committed one. So the run answers normally and is still stamped miss-clarification. This is a limitation of the derivation, not of the product -- see the class note in CASE-SCHEMA.v1.md. Recorded accurately rather than left to surface as a live-run mystery.] [RESTORED to deterministic-exact 2026-08-06 (CHAOS-3462 B6, team-lead ruling): this is the honest expectation -- the primary subject really does commit exact_match. It was briefly rewritten to miss-clarification, which was the 'rewrite an honest expectation to force green' anti-pattern. The reason the RUN derives miss-clarification is that resolution_path is a WHOLE-RUN scalar and this question also names an incidental untyped bare-name mention that never resolves; derive_resolution_path returns miss-clarification if ANY mention fails to commit. So the expectation stays true and the resolution_path_in invariant was REMOVED instead -- the same disposition B4 used for every other case whose path the derivation cannot honestly express. Every other invariant on the case is untouched. Re-assertable if the derivation ever becomes per-primary-subject.]"
    },
    "deg.source-state.stale": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Pinned to sources.json's dedicated stale row (probe/source-stale, org primary), same fixture as trust.source-health.stale -- >=1 source (commits/pull_requests/reviews/work_items/deployments) reads DataHealthState.STALE and must be disclosed as such in a source-state/freshness answer, not silently reported as current."
    },
    "deg.source-state.unauthorized-not-visible": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification",
      "notes": "Same fixture row and mechanism as tenant.cross-tenant-identifier-refused and deg.source-state.unavailable's sibling row, asked from primary about sibling-only/private-repo (cross-tenant) -- DataHealthService.inspect's UNAUTHORIZED short-circuit fires before the reader is even invoked. Kept in lockstep with tenant.cross-tenant-identifier-refused's expected_public_outcome/expected_scope_resolution_outcome/resolution_path triple deliberately: a cross-tenant identifier resolves to not_found regardless of which question angle (status vs. CI status) is asked over it."
    },
    "deg.source-state.unavailable": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Pinned to sources.json's dedicated unavailable row (sibling-only/private-repo, org sibling), asked from the sibling org's OWN vantage (not cross-tenant) -- scope resolves EXACT, the degradation is purely a source-availability disclosure (commits/pull_requests/reviews/deployments read DataHealthState.UNAVAILABLE org-wide for sibling)."
    },
    "deg.source-state.unconfigured": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Pinned to sources.json's dedicated unconfigured row (incidents source, primary org, attached to probe/source-current) -- incidents reads DataHealthState.UNCONFIGURED org-wide for primary (pagerduty/opsgenie absent from sync_providers), must be disclosed rather than silently omitted or reported as no-data."
    },
    "deg.timeout.data-health": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Subject resolves EXACT via the quoted noun-adjacent span 'the repo \"meridian/web-app\"'. A real data_health.v1 tool call succeeds at round 0; the slow-response fault only delays round 1's final_answer by 4000ms, producing a genuine DevAnswer terminal -- expected_public_outcome=answered_with_gaps for the same settled-by-code reason as deg.timeout.graph."
    },
    "deg.timeout.evidence": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Subject resolves EXACT via the quoted noun-adjacent span 'the repo \"meridian/web-app\"'. A real search_evidence.v1 tool call succeeds at round 0; the slow-response fault only delays round 1's final_answer by 4000ms, producing a genuine DevAnswer terminal -- expected_public_outcome=answered_with_gaps for the same settled-by-code reason as deg.timeout.graph."
    },
    "deg.timeout.graph": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Subject resolves EXACT via the quoted noun-adjacent span 'the repo \"meridian/web-app\"'. A real work_graph_neighbors.v1 tool call succeeds at round 0; the slow-response fault only delays round 1's final_answer by 4000ms, producing a genuine DevAnswer terminal, not an error terminal -- so expected_public_outcome is answered_with_gaps for the same settled-by-code reason as every other successful legacy-stack terminal in this corpus (terminal_frames.wrap_legacy_answer_as_frame), not because the delay itself is treated as an error."
    },
    "deg.timeout.metric": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Subject resolves EXACT via the quoted noun-adjacent span 'the repo \"meridian/web-app\"'. A real query_metric.v1 tool call succeeds at round 0; the slow-response fault only delays round 1's final_answer by 4000ms, producing a genuine DevAnswer terminal -- expected_public_outcome=answered_with_gaps for the same settled-by-code reason as deg.timeout.graph."
    },
    "deg.timeout.status": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "organization_fallback",
      "resolution_path": null,
      "notes": "Question text is the pre-existing, verbatim, Lane-1b-scripted 'Is the status snapshot for meridian/web-app ready yet?' -- a BARE, unquoted, lowercase 'meridian/web-app' slug with no adjacent closed-kind noun. Per question_interpreter.py's own module docstring and this batch's CRITICAL LESSON, extract_mentions recognizes ZERO mentions in this literal text, so resolution_path is null (no dev_run_resolutions entries are ever written) and scope resolution falls back org-wide (ScopeResolutionOutcome.ORGANIZATION_FALLBACK) despite the question conceptually being about subject.exact.meridian-web-app. expected_public_outcome=answered_with_gaps because a real DevAnswer terminal is produced (final_answer decision, no http_error) -- on today's deterministic/legacy stack every such terminal maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED (terminal_frames.wrap_legacy_answer_as_frame, settled by validate_outcome_consistency's own unconditional disclosed-limitations/no-completion-block clause, same fact already established by workload.team.with-denominator/deficiency.team.applicable-rule's own resolution-profile entries)."
    },
    "deg.unknown-denominator": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Reaches EXACT via the quoted full team label 'Platform Reliability (Ground Control)', identical mechanism to workload.team.with-denominator/deficiency.team.applicable-rule (see those cases' own resolution-profile entries for the live-verified reason the bare 'Ground Control' alias mention is not used). The gap is downstream: dimension_observation_adapters.py's investment_allocation_shift_observation reports attribution_present=False unconditionally (CHAOS-3331), so the finding lands in suppressed_findings/missing_attribution rather than a confidently-approved shift -- disclosed, not fabricated. expected_public_outcome=answered_with_gaps for the same settled-by-code reason as the other cases in this fragment."
    },
    "health.project.exact-subject": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Assumes UNCONFIGURED (not required) incidents does not by itself block a clean project-health answer for primary org; commits+CI carry the substantive signal. Flagged as an assumption, not an empirically-verified production fact. [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "health.project.not-applicable-fixture-only": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Registry sketch's 'Nightfall' is not a real subjects.json entity; substituted meridian/api-gateway (real PROJECT row, subject.bounded-set.web-app-and-api-gateway member). incidents UNCONFIGURED + acr not_applicable both realistically surface as gaps in a 'what health data exists' enumeration."
    },
    "health.project.unknown-and-not-applicable-abstract": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Scoped honestly: only the acr not_applicable and incidents UNCONFIGURED dimensions are claimed as realized. sources.json's own not-applicable matrix row explicitly states the PROJECT-scope change_failure_rate NOT_APPLICABLE reachability gap this id's name also gestures at is structurally unreachable today (no ticket filed) and is NOT re-claimed as realized here -- a runner should not require that specific reachability to pass this case."
    },
    "investment.mix.adequate": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "n/a-subject, org-wide investment-mix classification over the default (unmutated) generation profile -- no scope resolution mechanism is exercised (no subject mention to resolve). [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.] [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "investment.mix.inadequate": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "Same n/a-subject shape as investment.mix.adequate; fixture deliberately skewed so InvestmentMixin's classification reads inadequate. Generator knob not independently live-verified in this authoring pass -- see case file notes. [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.]"
    },
    "metric-compare.observed-change.single-metric": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Single-metric (items_completed) observed-change comparison scoped to meridian/web-app, real commit/work-item history across world.json's 10-day generation window. [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "metric-compare.two-metrics.current-both": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "n/a-subject, org-wide two-metric comparison (items_completed, cyclomatic_per_kloc) over the default (unmutated, all-current) generation profile. Supersedes wave31 matrix.multi-metric-comparison-organization-wide. [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.] [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "metric-compare.two-metrics.stale-source": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Pinned to sources.json's dedicated stale row (probe/source-stale, org primary) -- referenced by repo_full_name directly, not a subjects.json id. Must report per-source applicability distinguishing fresh vs stale rather than silently treating both metrics as current."
    },
    "pers.clarification-persistence": {
      "expected_public_outcome": "needs_clarification",
      "expected_scope_resolution_outcome": "ambiguous",
      "resolution_path": "miss-clarification",
      "notes": "Same profile shape as scope.ambiguous -- persistence-proving angle on the same mechanism."
    },
    "portfolio.multi-project.status": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "Unblocked by CHAOS-3393 (confirmed State: Done via linear-cli) -- runs through PlanExecutor/StepContext's multi-project portfolio path, not ScopeResolutionService's single/bounded-set subject resolution (hence null scope-resolution outcome, distinct from scope.bounded-subject-set's 'exact'). Both named repos are plain current-state fixtures with no known gap condition, so a clean answered is the honest expectation. [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.] [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.] [resolution_path DEFINED (CHAOS-3462 B4); the previous null was incorrect. The question names TWO correctly-quoted, kind-adjacent mentions (repo 'meridian/web-app', repo 'meridian/api-gateway') -- extract_mentions returns 2 when executed against this exact text -- and both are real, unambiguous org-primary catalog entities in subjects.json. subject_preflight returns PROCEED for the homogeneous 2-PROJECT cohort (committed_cohort_portfolio_v1) with the ledger still attached, and orchestrator appends EVERY ledger entry, so two dev_run_resolutions rows (entry_ordinal 0 and 1) exist and both commit exact_match. The old null was stale from an unquoted phrasing of this question that predates the current case text.] [REVERTED to null 2026-08-06 (CHAOS-3462 adversarial round 2): the deterministic-exact value set earlier in this branch was unproducible -- executed preflight shows this case terminates via committed_cohort_v1_only with no terminating_resolution_entry, so it writes zero dev_run_resolutions rows.]"
    },
    "portfolio.org-wide.plan-registry-gap-loud": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "Unblocked by CHAOS-3393. Chosen expectation: the plan-registry gap is disclosed as part of an otherwise-real answer (Wave 3.1 gate.plan-registry-gap-is-loud). Documented alternative: a terminal `failed` if the specific plan-registry-missing condition this case's fixture setup realizes is severe enough to block any answer at all, rather than degrade-and-disclose -- not silently picked, flagged for a runner/Lane 2a to confirm against the real gap-detection path. Shares literal question text with portfolio.org-wide.status; differentiated by plan-registry fixture condition, not phrasing. [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.]"
    },
    "portfolio.org-wide.status": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "Unblocked by CHAOS-3393. Spans the entire primary-org portfolio; primary org's incidents source reads DataHealthState.UNCONFIGURED org-wide (pagerduty absent, sources.json deg.source-state.unconfigured), giving a real fixture-grounded gap to disclose. Shares literal question text with portfolio.org-wide.plan-registry-gap-loud; differentiated by plan-registry fixture condition, not phrasing. [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.]"
    },
    "provider-fail.after-frame": {
      "expected_public_outcome": "temporarily_unavailable",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": null,
      "notes": "CORRECTED during Lane 2b merge/QA (2026-08-06), same basis as provider-fail.before-frame: PUBLIC_OUTCOME_BY_ERROR_CODE keys purely on the 'provider_unavailable' error code, not frame state, so the round-0 committed query_metric.v1 frame (protected by the CHAOS-3423 frame-write savepoint fix, d91fd769e) does not change the wire outcome bucket -- it survives as real transcript evidence (a frame), not as a client-facing answered_with_gaps DevAnswer, which is exactly the distinction this case exists to prove. [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.]"
    },
    "provider-fail.before-frame": {
      "expected_public_outcome": "temporarily_unavailable",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": null,
      "notes": "CORRECTED during Lane 2b merge/QA (2026-08-06), overriding the sub-agent's flagged-but-unresolved 'failed' draft: terminal_frames.PUBLIC_OUTCOME_BY_ERROR_CODE is a TOTALITY-CHECKED table (import-time guard over every ORCHESTRATOR_ERROR_CODES member, confirmed by reading it directly) mapping 'provider_unavailable' -> PublicOutcome.TEMPORARILY_UNAVAILABLE unconditionally -- keyed purely by error code, with no frame-state branch, so this is settled by the code, not an open judgment call. Deterministic subject preflight for the literal 'meridian/web-app' mention is asserted to complete (EXACT) independently of the provider fault, since preflight runs before the model loop and is not itself a tool call. [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.]"
    },
    "readiness.capabilities.degraded": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": null,
      "resolution_path": "deterministic-exact",
      "notes": "resolution_path CORRECTED to null during Lane 2b merge/QA (2026-08-06), same basis as readiness.capabilities.unsupported-model -- a capability-readiness gate, not a subject/scope resolution attempt. Fixed by the registry (not a matcher choice): a 'degraded' capability readiness is the transient/retryable half of llm.agent.readiness.readiness_failure_state's classification (timeout/rate_limited/provider_unavailable/unknown-default), structurally distinct from unsupported_model's INCOMPATIBLE bucket. Honesty caveat carried from the case file: the exact code path from a degraded-profile readiness_probe result to a live answered_with_gaps DevAnswer was not fully walked against a concrete implementation at authoring time -- `provider_profile_override` has no src/ references yet. Recorded here as the registry's asserted expectation, flagged for live confirmation once the corpus runner exists. [resolution_path DEFINED (CHAOS-3462 B4); the previous null was incorrect. The question names one correctly-quoted mention (repo 'meridian/web-app'), and this case's 'degraded readiness' premise rides on world.json's provider_profile_override, which has ZERO references anywhere in src/ -- it gates nothing in the current execution path. So this request takes the ordinary PROCEED path: preflight writes the ledger before the model round and the mention commits exact_match. NOTE for Lane 2b: because that override is unwired, this case does not yet exercise degraded readiness at all -- a separate authoring gap, not a reason to keep an unpassable invariant.] [COVERAGE WARNING, do not read this entry as resolved coverage (codex adversarial round-1, MEDIUM, accepted): the resolution_path value above is accurate -- the run really does write exact ledger rows -- but it is accurate BECAUSE this case currently takes the ORDINARY ready path. provider_profile_override has zero references in src/, so the degraded/unsupported readiness behaviour this case is named for is NOT exercised by any assertion here, and none of this case's invariants would notice if it regressed. Removing the resolution_path invariant would not close that gap either -- the case would still go green on its remaining invariants. The honest remedy is status=declared-blocked until the override is wired, which changes the active-case floor and is therefore a team-lead call, tracked rather than silently taken here.]"
    },
    "readiness.capabilities.unsupported-model": {
      "expected_public_outcome": "unsupported",
      "expected_scope_resolution_outcome": null,
      "resolution_path": "deterministic-exact",
      "notes": "resolution_path CORRECTED to null during Lane 2b merge/QA (2026-08-06): a model-readiness gate (production_runtime.py's `_resolve_provider_selection`) is a pre-check independent of the named subject and, per the case's own subject_class=n/a, is not modeled as a subject/scope resolution attempt at all -- 'miss-clarification' would wrongly imply a resolution attempt ran and missed, which is not this shape (the question's subject text is irrelevant to why this terminal fires). Same request-shape-short-circuit precedent as scope.unsupported-request/scope.prohibited-write. UNSUPPORTED (not TEMPORARILY_UNAVAILABLE) because unsupported-model-profile's readiness value ('unsupported_model') raises DevRuntimeUnavailable(code='model_not_supported', ...); terminal_frames.py's totality-checked PUBLIC_OUTCOME_BY_ERROR_CODE table maps 'model_not_supported' -> PublicOutcome.UNSUPPORTED explicitly (TEMPORARILY_UNAVAILABLE is reserved for provider_not_configured/provider_unavailable/rate_limited/tool_unavailable -- transient codes). Also matches llm.agent.readiness.role_state_for_safe_error_code's own COMPATIBLE/INCOMPATIBLE/FAILED split: 'unsupported_model' maps to INCOMPATIBLE (structural, non-retryable), never FAILED. [SET to deterministic-exact 2026-08-06 (CHAOS-3462 adversarial round 2): executed interpret+preflight show the correctly quoted repo \"meridian/web-app\" mention takes the ordinary singular proceeded_committed_subject PROCEED and writes one row, committing an exact subject. The prior null was wrong. This value shares the SAME open blocker as the 44 sibling deterministic-exact entries: the docker-exec ledger read never supplies mention_text, so derive_resolution_path raises ResolutionPathError for a single-shot exact_match and the runner records a failed resolution_path_classifiable check. Recorded here as the honest expectation rather than falsified to miss-clarification to force green.]"
    },
    "scope.ambiguous": {
      "expected_public_outcome": "needs_clarification",
      "expected_scope_resolution_outcome": "ambiguous",
      "resolution_path": "miss-clarification",
      "expected_candidates": [
        "meridian/atlas",
        "meridian-sandbox/atlas"
      ]
    },
    "scope.bounded-subject-set": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": " [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "scope.deleted-subject": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification"
    },
    "scope.no-match": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification"
    },
    "scope.outcome.filtered": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "filtered",
      "resolution_path": "deterministic-exact",
      "notes": " [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "scope.outcome.inherited": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "inherited",
      "resolution_path": "deterministic-exact",
      "notes": " [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "scope.outcome.organization-fallback": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "organization_fallback",
      "resolution_path": null,
      "notes": " [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.]"
    },
    "scope.outcome.unresolved": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification"
    },
    "scope.partially-resolved-subject": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "filtered",
      "resolution_path": "deterministic-exact",
      "notes": "Resolves to the parent 'platform' team only; gap flagged rather than silently claiming sub-team precision."
    },
    "scope.prohibited-write": {
      "expected_public_outcome": "denied",
      "expected_scope_resolution_outcome": null,
      "resolution_path": "miss-clarification",
      "notes": "subject_class=n/a: same as scope.unsupported-request -- refused before scope resolution runs. [CORRECTED to miss-clarification 2026-08-06 (CHAOS-3462 adversarial round 2): executed interpret+preflight show one untyped bare-name mention that never commits in this world, so exactly one row is written and the derived path is miss-clarification, not null.]"
    },
    "scope.refused-with-grounding": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Flagged/withheld ('Inconsistent result'), not a clean answered. [CORRECTED to miss-clarification 2026-08-06 (CHAOS-3462 B6, adversarial round 3). COVERAGE WARNING -- the primary subject really does commit exact_match; the value is miss-clarification because resolution_path is a WHOLE-RUN scalar and this question also names an incidental untyped bare-name mention that never resolves. derive_resolution_path returns miss-clarification if ANY mention fails to commit, and the unresolved_untyped PROCEED branch persists that row alongside the committed one. So the run answers normally and is still stamped miss-clarification. This is a limitation of the derivation, not of the product -- see the class note in CASE-SCHEMA.v1.md. Recorded accurately rather than left to surface as a live-run mystery.] [RESTORED to deterministic-exact 2026-08-06 (CHAOS-3462 B6, team-lead ruling): this is the honest expectation -- the primary subject really does commit exact_match. It was briefly rewritten to miss-clarification, which was the 'rewrite an honest expectation to force green' anti-pattern. The reason the RUN derives miss-clarification is that resolution_path is a WHOLE-RUN scalar and this question also names an incidental untyped bare-name mention that never resolves; derive_resolution_path returns miss-clarification if ANY mention fails to commit. So the expectation stays true and the resolution_path_in invariant was REMOVED instead -- the same disposition B4 used for every other case whose path the derivation cannot honestly express. Every other invariant on the case is untouched. Re-assertable if the derivation ever becomes per-primary-subject.]"
    },
    "scope.stale-context-subject": {
      "expected_public_outcome": "needs_clarification",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification",
      "notes": "Degraded to needs_clarification/answered_with_gaps rather than silently reusing the stale commit."
    },
    "scope.unsupported-request": {
      "expected_public_outcome": "unsupported",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "subject_class=n/a: request-shape rejection fires before any scope/subject resolution attempt -- no resolution_path is forced per team-lead decree (2a's receipt makes this field nullable for n/a-subject cases)."
    },
    "status.single-project.exact-subject": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Plain 'What's the status of X?' phrasing over the same exact subject as positive-control-v1; exercises status-answer composition, not the heritage-oracle metric contract. [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "status.single-project.positive-control-v1": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Wave 3.1 heritage oracle re-anchored against subject.exact.meridian-web-app -- mirrors ask-dev-oracle.v1.json's expected_metric_id items_completed / expected_evidence_entity_fragment 'meridian/web-app'. [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "status.single-project.readiness-completion": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "No fixture pins a deterministic 'fully complete, zero remaining work' state for meridian/web-app; consistent with status.single-project.remaining-work legitimately finding outstanding work, a clean release-ready 'answered' would be dishonest."
    },
    "status.single-project.remaining-work": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "work_items generation reliably produces open items in the 10-day window for meridian/web-app, so the enumeration has real content. [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "subject-label.acronym-mention": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification"
    },
    "subject-label.five-word-truncation": {
      "expected_public_outcome": "needs_clarification",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification",
      "notes": "Exact terminal not pinned pending CHAOS-3407; invariant floor only."
    },
    "subject-label.parenthetical-quoted-control": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": " [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "subject-label.parenthetical-unquoted": {
      "expected_public_outcome": "needs_clarification",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification",
      "notes": "Exact terminal not pinned -- only the invariant floor (graceful, non-widening) is asserted under deterministic-v1 pending CHAOS-3407."
    },
    "subject-label.typo-tolerance": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification"
    },
    "subject-label.unauthorized-candidate-excluded": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification"
    },
    "subject-label.valid-prior-turn-reference": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "inherited",
      "resolution_path": "deterministic-exact",
      "notes": " [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "subject-label.word-order-variation": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification"
    },
    "subject-label.wrong-kind-collision": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "ambiguous",
      "resolution_path": "miss-clarification",
      "notes": "CHAOS-3422 (kind-unfiltered close-match search) not yet fixed -- exact single-candidate resolution NOT asserted under deterministic-v1; invariant floor only."
    },
    "tenant.cross-tenant-identifier-refused": {
      "expected_public_outcome": "not_found",
      "expected_scope_resolution_outcome": "unresolved",
      "resolution_path": "miss-clarification",
      "notes": "Same fixture row and mechanism as subject-label.unauthorized-candidate-excluded (whose profile entry uses the identical expected_public_outcome/expected_scope_resolution_outcome/resolution_path triple) -- kept in lockstep deliberately so a cross-tenant identifier resolves to not_found regardless of whether the mention is fuzzy (that case) or the full exact literal identifier (this case)."
    },
    "trust.conflicting-evidence": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Grounded against sources.json's dedicated conflicting row (probe/source-conflicting-ci: two contemporaneous ci_pipeline_runs for the same repo_id, opposite conclusions) -- same fixture row case-scope.refused-with-grounding.json also reuses, but at a lower severity: a single contemporaneous pair surfaces as a disclosed conflict within an answered_with_gaps terminal rather than the over-threshold withheld 'Inconsistent result' refusal that case tests. Per P0-B item 10, this trust section had zero real fixture coverage before probe/source-conflicting-ci existed. [CORRECTED to miss-clarification 2026-08-06 (CHAOS-3462 B6, adversarial round 3). COVERAGE WARNING -- the primary subject really does commit exact_match; the value is miss-clarification because resolution_path is a WHOLE-RUN scalar and this question also names an incidental untyped bare-name mention that never resolves. derive_resolution_path returns miss-clarification if ANY mention fails to commit, and the unresolved_untyped PROCEED branch persists that row alongside the committed one. So the run answers normally and is still stamped miss-clarification. This is a limitation of the derivation, not of the product -- see the class note in CASE-SCHEMA.v1.md. Recorded accurately rather than left to surface as a live-run mystery.] [RESTORED to deterministic-exact 2026-08-06 (CHAOS-3462 B6, team-lead ruling): this is the honest expectation -- the primary subject really does commit exact_match. It was briefly rewritten to miss-clarification, which was the 'rewrite an honest expectation to force green' anti-pattern. The reason the RUN derives miss-clarification is that resolution_path is a WHOLE-RUN scalar and this question also names an incidental untyped bare-name mention that never resolves; derive_resolution_path returns miss-clarification if ANY mention fails to commit. So the expectation stays true and the resolution_path_in invariant was REMOVED instead -- the same disposition B4 used for every other case whose path the derivation cannot honestly express. Every other invariant on the case is untouched. Re-assertable if the derivation ever becomes per-primary-subject.]"
    },
    "trust.source-health.current": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "meridian/web-app is never mutated by any sources.json staleness/failure postprocess -- presents the same DataHealthState.COMPLETE shape as sources.json's dedicated 'current' row (probe/source-current). Primary org's own always-unconfigured incidents source (no pagerduty configured) and the org-wide-hardcoded acr not_applicable are both expected/normal for this org, not gaps relative to what was asked. [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "trust.source-health.stale": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Pinned to sources.json's dedicated stale row (probe/source-stale, org primary) -- referenced by repo_full_name directly, not a subjects.json id. >=1 source (commits/pull_requests/reviews/work_items/deployments) reads DataHealthState.STALE and must be disclosed as such, the answered_with_gaps counterpart to trust.source-health.current."
    },
    "unrelated-evidence.named-subject": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": null,
      "notes": "Named, unambiguous exact-subject mention (meridian/web-app), delegate_default in provider-scripts (falls through to the existing default heuristic unchanged). ANSWERED because the named subject resolves cleanly with real evidence -- the invariant under test is about the CONTENT of that answer (zero cross-repository leakage), not the terminal outcome bucket itself. [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.] [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "unrelated-evidence.org-wide": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": null,
      "resolution_path": null,
      "notes": "No provider-scripts entry for this id -- falls through to the default heuristic untouched. expected_scope_resolution_outcome is null because this org-wide/portfolio-shaped question never exercises ScopeResolutionService's per-mention candidate-resolution machinery (no single subject is named to resolve); it is served by the org-wide/portfolio aggregation path instead, which this fragment does not assert a value for. resolution_path is recorded as deterministic-exact in the loose sense of 'no ambiguity/candidate matching needed, a direct answer is served' -- none of the five resolution_path vocabulary values map cleanly onto a no-subject-at-all request, flagged as an approximation rather than a precise fit. [resolution_path CORRECTED to null during Lane 2b codex round-1 deep remediation, 2026-08-06: resolution_path.py (Lane 2a, read directly) derives this field strictly from dev_run_resolutions entries, which only exist when extract_mentions actually named >=1 subject mention -- a question with zero extractable mentions (n/a-subject scenario, or a pre-existing bare/unquoted lowercase-slug question this lane cannot change because it must stay byte-identical to an already-landed Lane 1b provider-scripts/role-legacy_agent.json entry) never appends anything, so the honest value is null, not deterministic-exact.] [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "workload.team.small-cohort": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Team resolves cleanly; the gap is downstream cohort-too-small-for-confident-qualification, disclosed rather than silently presented as a confident verdict."
    },
    "workload.team.with-denominator": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Reaches EXACT via the quoted full team label 'Platform Reliability (Ground Control)', not the bare 'Ground Control' alias span -- see the case file's notes for the live-verified reason (CHAOS-3388's never-auto-commit-an-alias rule applies uniformly to every question_interpreter.py intent, including TEAM_WORKLOAD_BALANCE, via subject_preflight.py's SubjectPreflight.run and scope_service.py's commit rule at resolve_mention). A bare-alias phrasing of this same question would instead resolve ambiguous/needs_clarification, identically to scope.acronym-alias-subject.json. [expected_public_outcome CORRECTED to answered_with_gaps during Lane 2b cross-lane sweep, 2026-08-06 (2a codex round-2 finding, independently verified by reading terminal_frames.wrap_legacy_answer_as_frame directly): on todays deterministic stack a real DevAnswer terminal ALWAYS maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED -- the legacy model-tool-choice loop never computes a DevCompletionBlock, and validate_outcome_consistency requires either disclosed limitations or a non-calculable completion block for answered_with_gaps, which every wrapped answer satisfies unconditionally (per that functions own docstring). Settled by the code, not a judgment call.]"
    },
    "workload.team.without-denominator": {
      "expected_public_outcome": "answered_with_gaps",
      "expected_scope_resolution_outcome": "exact",
      "resolution_path": "deterministic-exact",
      "notes": "Team resolves cleanly (same quoted-full-label mechanism as workload.team.with-denominator); the gap is downstream in team_workload_service.py's NO-baseline-denominator path, disclosed rather than silently omitted."
    }
  }
}
