{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "adv.oversized.scope",
  "family": "adv",
  "group": 8,
  "status": "declared-blocked",
  "blocked_by": "CHAOS-3455",
  "proves": [
    "group 8 (\"Security and adversarial\", registry-ids.v1.json -- corpus-registry-v1.md's per-bullet 'proves' text was not accessible in this Lane 2b worktree; cited by registry group name/source file instead, documented honestly rather than guessed)"
  ],
  "question": "Please answer this using every one of these repos and teams in scope at once: the repo \"meridian/web-app\", the repo \"meridian/api-gateway\", and twenty more besides them, plus every team in the organization.",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "n/a",
  "subject_ref": null,
  "invariants": [],
  "resolution_profile_ref": null,
  "fault_ref": null,
  "notes": "BLOCKED: an oversized SCOPE (as opposed to an oversized set of question-text mentions, which adv.oversized.question/subject-set already cover via extract_mentions) means an oversized `DevScope.repositories`/`team_ids`/`entity_refs` list on the REQUEST's own structured scope field, not the natural-language question -- contracts.py (read directly) bounds `DevRequestedScope.repositories`/`team_ids`/`entity_refs` and `DevScope.entity_refs` each at `max_length=20`, a real, executable pydantic guard (a 21st item would raise a contract validation error before the interpreter even runs). CASE-SCHEMA.v1.md's case shape carries only `question`/`org_alias`/`user_alias` -- there is no field to author a raw `DevScope.repositories`/`team_ids`/`entity_refs` payload directly, and Lane 2a's runner (read via this doc's own description) builds the request's scope from org/user alias plus the question text, not from a case-authored structured scope object. Expressible only with a schema/runner extension letting a case author a raw oversized DevScope payload; the `question` field above is flavor-text documentation of the intended scenario only, not a mechanism that actually constructs an oversized DevScope today. [blocked_by WIRED to CHAOS-3455 during Lane 2b ticket-filing pass, 2026-08-06 -- original free-text reason preserved verbatim here: \"runner: no structured way to author an oversized DevScope.repositories/team_ids/entity_refs payload (case schema only carries a natural-language question, not a raw DevScope object; the real 20-item pydantic bound lives on the request contract, out of this case format's reach)\".]"
}
