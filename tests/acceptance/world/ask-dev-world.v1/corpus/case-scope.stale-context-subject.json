{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "scope.stale-context-subject",
  "family": "scope",
  "group": 1,
  "status": "declared-blocked",
  "blocked_by": "CHAOS-3454 runner cannot address a SEEDED conversation, so aged prior-turn history is unreachable",
  "proves": [
    "plan §3 subjects.json class"
  ],
  "question": "it",
  "org_alias": "primary",
  "user_alias": "primary.ordinary-6",
  "subject_class": "stale-context",
  "subject_ref": "subject.stale-context.rotated-service",
  "invariants": [],
  "resolution_profile_ref": null,
  "fault_ref": null,
  "notes": "DECLARED-BLOCKED against CHAOS-3520 (CHAOS-3490 Part 2 corpus-authoring lane, 2026-08-07). Live evidence: CHAOS-3219 Phase 2 exit run 7, world digest 2e1e254d3ee8b799fd58234e865ccf1361c2753c412d1b526bf4e79d1e445232, wave_3_1 preflight enabled=True -- receipt tests/acceptance/artifacts/wave4/scope.stale-context-subject.json recorded \"scope_resolution_outcome='exact', allowed=['unresolved']\". 'exact' is not a near-miss: it means the scenario this case is NAMED for never occurred on the run. The expectation is NOT rewritten to 'exact' -- that would be the 'rewrite an honest expectation to force green' anti-pattern, and it would delete this case's coverage claim while reading as green. WHY IT CANNOT BE REALIZED: 'unresolved' is produced by scope_service.py:649/:700/:714/:742 and requires a ref that fails to resolve. Producer executed: interpret('it') yields ZERO mentions, so nothing is ever looked up and nothing can fail; the run resolves the plain org scope to 'exact' via scope_service.py:755. The scenario the case describes -- a second turn whose 9-day-old prior DevRunSubjectSet commit must NOT silently apply -- also needs a first turn: the corpus runner sends one message per case with surface_context=null and entity_refs=[], the same runner gap tracked for adv.mid-conversation.* (CHAOS-3454). Nothing about the stale-context rotation window is exercised by a single-turn run. NO UNIQUE ASSERTION COVERAGE IS LOST BY BLOCKING: the invariants dropped here besides scope_resolution_outcome_in (no_internal_error and terminal_persists_assistant_row (both PASSED)) are asserted by many other active cases; only this case's own named-outcome claim goes uncovered, which is exactly what CHAOS-3520 records. Retired profile entry preserved verbatim: {\"expected_public_outcome\": \"needs_clarification\", \"expected_scope_resolution_outcome\": \"unresolved\", \"resolution_path\": \"miss-clarification\"}. Full authored invariant list before the block is in git history for this file. [ORIGINAL NOTES, preserved verbatim: Second turn of conversation ask-dev-world.v1:conversation:primary:stale-context; the earlier DevRunSubjectSet commit for 'rotated/service' is 9 days old, older than the rotation window, so it must NOT silently apply.]",
  "$comment_resolution_path_invariant": "resolution_path_in REMOVED (CHAOS-3462, adversarial round 2). Established by executing the REAL producer -- QuestionInterpreter.interpret plus SubjectPreflight.run with this runner's exact request shape -- not by reading the question. interpret returns ZERO mentions, so preflight takes the proceeded_organization_wide PROCEED with ledger=None, orchestrator's append_resolution sites require a non-None ledger, and zero dev_run_resolutions rows exist. derive_resolution_path([]) is None and resolution_path_in never matches None, so this check could not pass in ANY catalog world. The profile's declared value is left untouched: it is wrong for a separate, larger reason tracked as the mention_text blocker, and silently rewriting it here to force green would falsify the expectation rather than fix it.",
  "$comment_principal_pool": "CHAOS-3490: user_alias assigned by deterministic round-robin over the ordinary-primary pool (sorted case id, index 61 of 85). The pool is role-identical to primary.ordinary, so this changes request DISTRIBUTION only -- never what this case proves. Spreading exists because production caps one user at 20 requests/15min and 85 cases on one alias made a single-pass armed run impossible.",
  "$comment_blocked_by_correction": "CHAOS-3219 Phase 3 (2026-08-07) stale-blocker sweep. WAS: 'CHAOS-3520'. CHAOS-3520 (cited before) is CLOSED and was NEVER this case's blocker. The world DOES seed the scenario (fixtures/generators/retention_conversations.py:102 build_stale_context_conversation, wired fixtures/world.py:1239-1246). The runner creates a fresh conversation per case (test_wave4_corpus_runner_live.py:617-624) with no seam to target a seeded conversation id -- and this is distinct from multi-turn, since a two-turn runner starting fresh still cannot reach >9-day-old history. Appended to CHAOS-3454's scope 2026-08-07."
}
