{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "adv.abuse.retry-storm",
  "family": "adv",
  "group": 8,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 8 (\"Security and adversarial\", registry-ids.v1.json -- corpus-registry-v1.md's per-bullet 'proves' text was not accessible in this Lane 2b worktree; cited by registry group name/source file instead, documented honestly rather than guessed)"
  ],
  "question": "What's the status of meridian/web-app, checked repeatedly?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": "subject.exact.meridian-web-app",
  "invariants": [
    {
      "category": "terminal-persists-assistant-row",
      "check": "terminal_persists_assistant_row",
      "args": {},
      "assert": "Every clarification or error terminal persists a real transcript row (DevAnswer or the CHAOS-3423 dev_error.v1 row) -- never a silently-dropped turn.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker terminal_persists_assistant_row (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": "adv.abuse.retry-storm",
  "notes": "Question text copied verbatim from provider-scripts/role-legacy_agent.json's cases['adv.abuse.retry-storm'].question (already pre-existing there, kind=fault, type=retry-storm-trigger, fires_from_round=0, http_error 429 'scripted_fault_retry_storm' on EVERY provider call -- 'to trigger the caller's retry policy'). Subject resolution: same bare-lowercase-slug shape and reasoning as provider-fail.before-frame/after-frame (already-adopted precedent, read directly): 'meridian/web-app' has no capitalized letters and is not quoted or kind-noun-adjacent, so extract_mentions' typed grammar recognizes zero mentions and its untyped bare-name path also recognizes nothing (no capitalized/quoted span at all) -- resolution_path is honestly null (resolution_path.py derives it strictly from dev_run_resolutions entries, which are never written here), while expected_scope_resolution_outcome is asserted 'exact' following the SAME corrected precedent those two cases already established for this identical bare-slug-question shape, since deterministic subject preflight runs before the provider's decision loop and is independent of the fault. Public outcome: a persistent 429/rate-limit on EVERY round (unlike provider-fail's single/round-1 failure) exhausts the caller's retry policy; terminal_frames.PUBLIC_OUTCOME_BY_ERROR_CODE maps 'rate_limited' -> PublicOutcome.TEMPORARILY_UNAVAILABLE unconditionally (errors.py's safe_agent_provider_error classifies an LLMRateLimitError as AgentProviderErrorCode.RATE_LIMITED, retryable=True), which is the outcome asserted here. Honesty caveat: the exact retry-count/backoff budget before the caller gives up and surfaces this terminal was not traced end-to-end in this pass (out of Lane 2b's read scope) -- flagged as a documented judgment call, not a confirmed trace, matching this corpus's own established practice for this class of uncertainty.",
  "$comment_resolution_path_invariant": "resolution_path_in REMOVED (CHAOS-3462 B4). This case's question names ZERO mentions that production's extract_mentions recognizes -- verified by executing the REAL producer -- QuestionInterpreter.interpret plus SubjectPreflight.run with this runner's exact request shape -- against this question text. NOTE (adversarial round 2): an earlier version of this comment cited extract_mentions, which is the WRONG oracle: production additionally mints untyped bare-name mentions via _add_untyped_mentions, and three cases were mis-dispositioned on that basis. This case's zero-mention conclusion was re-confirmed against the real interpreter. CASE-SCHEMA.v1.md's own 'practical extraction gotcha' is the cause: a bare, unquoted, lowercase slug is not a mention. So no dev_run_resolutions row is ever written, derive_resolution_path returns None BY CONTRACT, and resolution_path_in deliberately never matches None -- the check failed by construction on every run, on every stack. Same trap and same disposition as the four scope_resolution_outcome_in cases in CASE-SCHEMA.v1.md S1: drop the unpassable check, keep the narrower floor the remaining invariants give, and say so out loud rather than claim enforcement this case never had. The profile's null resolution_path is CORRECT and stays.",
  "$comment_principal_pool": "CHAOS-3490: user_alias assigned by deterministic round-robin over the ordinary-primary pool (sorted case id, index 0 of 85). The pool is role-identical to primary.ordinary, so this changes request DISTRIBUTION only -- never what this case proves. Spreading exists because production caps one user at 20 requests/15min and 85 cases on one alias made a single-pass armed run impossible."
}
