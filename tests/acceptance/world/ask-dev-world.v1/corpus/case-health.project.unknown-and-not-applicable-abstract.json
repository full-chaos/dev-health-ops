{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "health.project.unknown-and-not-applicable-abstract",
  "family": "health",
  "group": 1,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 1 Product-question corpus; health family full dimension-breakdown probe including not-applicable dimensions"
  ],
  "question": "Show the health dimension breakdown, including any not-applicable dimensions, for the repo \"meridian/web-app\"",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": "subject.exact.meridian-web-app",
  "invariants": [
    {
      "category": "no-silent-widening",
      "check": "scope_resolution_outcome_in",
      "args": {
        "from_profile": "expected_scope_resolution_outcome"
      },
      "assert": "A named subject mention that misses its intended target never silently widens to org-wide/broader scope; widening is only ever a disclosed, terminal answered_with_gaps/needs_clarification outcome.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker scope_resolution_outcome_in against this case's own resolution-profile expected_scope_resolution_outcome (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Honesty scoping, important for this specific id: sources.json's own 'not-applicable' matrix row explicitly flags that 'the PROJECT-scope change_failure_rate NOT_APPLICABLE reachability gap corpus-registry-v1.md's health.project.unknown-and-not-applicable-abstract family already flags as structurally unreachable today (no ticket filed) is NOT re-claimed as realized here'. This case's assertions are therefore scoped only to what IS realized in the fixture world: the 'acr' dimension is a genuine not_applicable realization (NativeDataHealthReader's unconditional org-wide hardcode, no fixture row needed) and 'incidents' is a genuine UNCONFIGURED-dimension realization (pagerduty absent org-wide for primary) -- both legitimately populate a not-applicable/gapped breakdown. The PROJECT-scope change_failure_rate not-applicable path this id's name also gestures at ('unknown') is NOT claimed as covered by this case; a runner should not fail this case for lacking that specific reachability, since no production code path realizes it yet. expected_public_outcome answered_with_gaps reflects the dimensions that ARE realized (acr, incidents), not the unreached one. [CORRECTED during Lane 2b codex round-1 remediation, 2026-08-06, from 'Show the health dimension breakdown, including any not-applicable dimensions, for meridian/web-app': question_interpreter.py's own module docstring states plainly 'a lowercase slug ... produces no mention' -- extract_mentions requires either a capitalized _NAME token or a QUOTED span adjacent to a closed kind noun (test_question_interpreter.py's proven pattern: 'How is repo \"dev-health-ops\" doing?'). The original bare, unquoted lowercase slug would extract ZERO typed mentions and silently degrade to org-wide/no-subject behavior -- exactly the false-pass risk codex's wrong-kind-collision finding named, generalized here to every other case that had the same bug.]"
}
