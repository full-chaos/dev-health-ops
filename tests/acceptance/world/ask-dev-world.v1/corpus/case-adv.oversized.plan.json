{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "adv.oversized.plan",
  "family": "adv",
  "group": 8,
  "status": "declared-blocked",
  "blocked_by": "CHAOS-3455",
  "proves": [
    "group 8 (\"Security and adversarial\", registry-ids.v1.json -- corpus-registry-v1.md's per-bullet 'proves' text was not accessible in this Lane 2b worktree; cited by registry group name/source file instead, documented honestly rather than guessed)"
  ],
  "question": "Can you investigate this as thoroughly as possible, using as many separate investigation steps and tool calls as it takes, however many that is?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "n/a",
  "subject_ref": null,
  "invariants": [],
  "resolution_profile_ref": null,
  "fault_ref": null,
  "notes": "BLOCKED: an oversized PLAN (an oversized number of investigation/tool-call decision rounds) is decided by the PROVIDER's own decision sequence, not by question text -- the question above cannot, by itself, force a real or scripted provider to actually emit an oversized number of tool_call decisions. provider_scripts.py's fault taxonomy (read directly) has a real, scripted 'oversized-output' fault type (already reused verbatim by adv.oversized.result) for an oversized single ANSWER payload, but no equivalent scripted fault type for an oversized DECISION COUNT/plan-step sequence -- authoring that would require either a new provider-scripts fault type (Lane 1b territory) or a long, explicit `decisions: [...]` array of dozens of individual tool_call entries hand-authored in role-legacy_agent.json, which risks constructing an artificial, non-representative scripted sequence rather than testing a real production plan-size guard (orchestrator.py's own `tool_limit_reached`/RunState.FAILED path is real, but reaching it honestly needs either a real plan-executor step-count bound or a long scripted sequence neither this pass's read scope nor CASE-SCHEMA.v1.md's single-question shape can respectably construct). The `question` field above is flavor-text documentation of the intended scenario only. [blocked_by WIRED to CHAOS-3455 during Lane 2b ticket-filing pass, 2026-08-06 -- original free-text reason preserved verbatim here: \"runner: no scripted fault type for an oversized plan/tool-call-count decision sequence (provider_scripts.py's fault taxonomy covers oversized-output only, not an oversized decision/plan-step count)\".]"
}
