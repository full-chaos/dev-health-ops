{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "health.team.exact-subject",
  "family": "health",
  "group": 1,
  "status": "declared-blocked",
  "blocked_by": "CHAOS-3475 deterministic-alias is unproducible by a single-turn runner",
  "proves": [
    "group 1 Product-question corpus; health family team subject, real-fixture correction of the registry sketch's naive 'answered' expectation (mirrors scope.acronym-alias-subject precedent)"
  ],
  "question": "Is the Ground Control team healthy?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "acronym-alias",
  "subject_ref": "subject.acronym-alias.ground-control-team",
  "invariants": [],
  "resolution_profile_ref": null,
  "fault_ref": null,
  "notes": "Named 'exact-subject' in the id and sketched as subject_class 'exact' / outcome 'answered' in the higher-level registry row, but the only real team subject fixture backing 'Ground Control' is subjects.json's subject.acronym-alias.ground-control-team (class acronym-alias, literal_parenthetical alias form) -- there is no plain non-alias team-name subject in this world's catalog. subjects.json is authoritative over the registry sketch here, same discrepancy already documented by case-scope.acronym-alias-subject.json for the MWA case: subject.acronym-alias.mwa-project's own notes state 'acronym/literal-alias hits are never auto-commit eligible per alias_matching.py', and that phrasing explicitly covers BOTH acronym and literal-parenthetical alias forms. So under deterministic-v1 this deterministically surfaces for single-candidate confirmation (needs_clarification / ScopeResolutionOutcome.ambiguous / resolution_path deterministic-alias), not a direct answered -- documented discrepancy vs the registry sketch's original 'answered'/'exact' shorthand, not silently changed. subject_class set to 'acronym-alias' (not 'exact') to match subjects.json's real class field for this subject_ref, per CASE-SCHEMA.v1.md's definition of that field.",
  "$comment_resolution_path_invariant": "DECLARED-BLOCKED (CHAOS-3462 B6, team-lead ruling 2026-08-06). This case exists to prove CHAOS-3388 alias/acronym resolution and cannot, as authored: production never auto-commits an alias form (it must be offered as a candidate first), and the only in-run route from ambiguous_candidates to exact_match needs scope.entity_refs / surface_context, which this runner never sends -- and it sends one message per case, so the two-turn ask/offer/select/commit shape never occurs. The single-turn ledger is one ambiguous_candidates row and derive_resolution_path short-circuits to miss-clarification. Recording miss-clarification would make the case PASS while proving nothing about alias resolution -- manufactured green. Blocked loudly and countably instead, per the world.json/sources.json declared-blocked precedent. Unblocked by CHAOS-3475."
}
