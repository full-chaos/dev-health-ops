{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "subject-label.valid-prior-turn-reference",
  "family": "subject-label",
  "group": 1,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "CHAOS-3389 eval set (\"prior-turn references\")"
  ],
  "question": "Did anything change since then?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary-4",
  "subject_class": "exact",
  "subject_ref": "subject.exact.meridian-web-app",
  "invariants": [
    {
      "category": "no-internal-error-on-fuzzy-label",
      "check": "no_internal_error",
      "args": {},
      "assert": "No shape of subject label (parenthetical, >=5 words, typo, acronym, wrong word order) can crash the run to internal_error; worst case is a graceful terminal.",
      "executable_today": true,
      "$comment": "check=no_internal_error is a real, always-meaningful floor (the run must not crash to internal_error) but is NARROWER than this invariant's full 'assert' claim above -- Lane 2a's invariants.py registry (read directly, 2026-08-06) has no checker yet for the specific widening/unauthorized-candidate/persistence claim this category names. Tracked as a Lane 2a checker gap, not silently claimed as fully enforced."
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Positive counterpart to scope.stale-context-subject: turn 1 names meridian/web-app, turn 2 (<1h later, well within the rotation window) asks a bare follow-up and MUST correctly resolve to the turn-1 subject, distinguishing this from the >9-day stale case. Same mechanism as scope.outcome.inherited -- cross-referenced (P0-B outcome-value vs CHAOS-3389 eval-fixture angle), not duplicated. [Reworded during Lane 2b codex round-1 remediation, 2026-08-06, from 'Has anything changed since then?': production's provider_scripts.py requires GLOBALLY-UNIQUE question text per role script (test_a_question_fingerprint_collision_fails_to_load), and every corpus case now needs its own role-legacy_agent.json entry (script_inventory.py's full-coverage requirement) -- two cases sharing literal question text could never both get a script entry. Subject mention substring kept character-identical to subjects.json's declared 'mentions' so the deterministic extractor still recognizes it; only the surrounding phrasing changed.]",
  "$comment_resolution_path_invariant": "resolution_path_in REMOVED (CHAOS-3462, adversarial round 2). Established by executing the REAL producer -- QuestionInterpreter.interpret plus SubjectPreflight.run with this runner's exact request shape -- not by reading the question. interpret returns ZERO mentions, so preflight takes the proceeded_organization_wide PROCEED with ledger=None, orchestrator's append_resolution sites require a non-None ledger, and zero dev_run_resolutions rows exist. derive_resolution_path([]) is None and resolution_path_in never matches None, so this check could not pass in ANY catalog world. The profile's declared value is left untouched: it is wrong for a separate, larger reason tracked as the mention_text blocker, and silently rewriting it here to force green would falsify the expectation rather than fix it.",
  "$comment_principal_pool": "CHAOS-3490: user_alias assigned by deterministic round-robin over the ordinary-primary pool (sorted case id, index 73 of 85). The pool is role-identical to primary.ordinary, so this changes request DISTRIBUTION only -- never what this case proves. Spreading exists because production caps one user at 20 requests/15min and 85 cases on one alias made a single-pass armed run impossible."
}
