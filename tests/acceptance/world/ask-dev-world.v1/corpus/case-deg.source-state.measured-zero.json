{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.source-state.measured-zero",
  "family": "deg",
  "group": 9,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 9 family deg.source-state (sources.json terminal-state matrix, state=measured-zero); CHAOS-3219 launch threshold 'zero missing-data-as-zero/healthy conversions'"
  ],
  "question": "How often did the repo \"probe/source-measured-zero\" deploy in the last 30 days?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary-6",
  "subject_class": "exact",
  "subject_ref": null,
  "expected_mention_texts": [
    "probe/source-measured-zero"
  ],
  "invariants": [
    {
      "category": "no-silent-widening",
      "check": "scope_resolution_outcome_in",
      "args": {
        "from_profile": "expected_scope_resolution_outcome"
      },
      "assert": "A named subject mention never silently widens to org-wide scope; widening is only ever a disclosed terminal.",
      "executable_today": true
    },
    {
      "category": "terminal-outcome-matches-profile",
      "check": "public_outcome_in",
      "args": {
        "from_profile": "expected_public_outcome"
      },
      "assert": "The run reaches the declared terminal outcome, not merely a non-crash.",
      "executable_today": true
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "THE GAP THIS CLOSES: sources.json realizes a dedicated `measured-zero` row (probe/source-measured-zero) whose mechanism is deliberately distinct from no-data -- the deployments table is empty for the repo (so the data-health layer honestly reads NO_DATA) while fixtures/generators/pipelines.generate_dora_metrics writes an explicit DORAMetricsRecord value=0.0 for this repo/window. The metric layer (production_runtime.query_metric) reads those rows directly, NOT the per-source watermark, so 'deployment_frequency measured, value 0' and 'deployments: no_data' are both true of different repos and must never be conflated. Until now that fixture had NO dedicated case: its 9 appearances across the corpus were all incidental authorized_entity_ids lists. deg.source-state.no-data's own profile note says so out loud -- 'distinguished from measured-zero (a metrics-layer DORAMetricsRecord value=0.0, not realized in this batch)'. HONEST LIMIT: the three invariants above assert scope, ledger and terminal. They do NOT yet assert 'the reported value is a measured 0 rather than an absence' -- that needs a metric-value checker this registry does not have, and claiming otherwise would be exactly the over-claim this phase exists to remove. What the case does close today is that the measured-zero fixture is finally EXERCISED and its terminal pinned, so a regression that turned it into a no-data (or a crash) is caught; the value-level assertion is tracked, not silently implied.",
  "$comment_authored": "CHAOS-3219 Phase 3 (2026-08-07). expected_mention_texts GENERATED BY EXECUTING THE PRODUCER (QuestionInterpreter().interpret with the runner's real DevMessageRequest shape, taking each mention's normalized_lookup_text) -- never hand-authored, per CASE-SCHEMA.v1.md's binding method. Question fingerprint checked for collisions by EXECUTING question_fingerprint against all 130 case questions AND every role-legacy_agent.json entry: none.",
  "$comment_principal_pool": "CHAOS-3219 Phase 3: user_alias chosen for rate-limit headroom, not round-robin -- production caps one principal at 20 requests/15min and this alias was among the least loaded when the case was authored. Role-identical to primary.ordinary, so this changes request DISTRIBUTION only, never what the case proves.",
  "$comment_resolution_path_invariant": "resolution_path_in NOW DECLARED, and the pin that refused it is moved in this same change -- the sequence its own previous text demanded. That text refused the declaration because 'no case in this corpus has yet been OBSERVED producing a non-null resolution_path', which was true when written and is false now. EVIDENCE (.p3-logs/PIN-MOVE-RECEIPT.md, measured armed run 2026-08-07 11:11-11:19 PT, branch chaos-3219-phase3-corpus @150bcb432, WORLD_DIGEST 6dda65a6..., engine identity 92280f2b... role=legacy_agent, 134 collected / 0 skipped / 90 executed / 90 receipts / 0 missing): all EIGHT $comment_known_red_class_closed cases passed resolution_path_in with a non-null path, proving CHAOS-3533's ledger persistence live. Stronger still, THIS case's own receipt recorded resolution_path='deterministic-exact' while the check was still absent -- so the value asserted here is one this case was OBSERVED producing, not one predicted for it. The profile already declares resolution_path 'deterministic-exact', so from_profile asserts exactly the observed value. NOTE for anyone extending this: the eight class-closed cases resolve 'miss-clarification' BECAUSE THEY ARE MISSES; this case resolves its subject exactly and then reports a measured zero, so its path legitimately differs. Copying the class-closed value here would be a transcription error wearing the costume of a product regression."
}
