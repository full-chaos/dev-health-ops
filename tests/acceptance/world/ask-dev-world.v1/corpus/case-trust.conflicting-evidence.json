{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "trust.conflicting-evidence",
  "family": "trust",
  "group": 1,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "P0-B MISSING item 10 (conflicting-evidence trust section never rendered before this fixture existed)"
  ],
  "question": "Is the repo \"meridian/web-app\"'s CI passing?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": null,
  "invariants": [
    {
      "category": "no-silent-widening",
      "check": "scope_resolution_outcome_in",
      "args": {
        "from_profile": "expected_scope_resolution_outcome"
      },
      "assert": "A named subject mention that misses its intended target never silently widens to org-wide/broader scope; widening is only ever a disclosed, terminal answered_with_gaps/needs_clarification outcome.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker scope_resolution_outcome_in against this case's own resolution-profile expected_scope_resolution_outcome (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Question text names meridian/web-app, but the case is deliberately grounded against sources.json's dedicated 'conflicting' matrix row (repo_full_name probe/source-conflicting-ci, built by dev_health_ops.fixtures.generators.conflicts.build_conflicting_ci_runs: two ci_pipeline_runs rows for the same repo_id, queued minutes apart, opposite conclusions success/failed) -- same reuse pattern as case-scope.refused-with-grounding.json's identical fixture row, subject_ref intentionally null since ci_pipeline_runs is keyed by (repo_id, run_id) with no commit_sha/ref column (sources.json's own honesty note). Distinct outcome from scope.refused-with-grounding's over-threshold withheld 'Inconsistent result' terminal: this case's contemporaneous-conflict signal is a single pair of runs, not a persistent contradiction past the trust threshold, so it is expected to still answer (answered_with_gaps) while surfacing the conflict, rather than being withheld outright -- the two cases bound the same underlying mechanism at different severities. Per P0-B item 10, this trust section (evidence-conflict disclosure) had zero real coverage before probe/source-conflicting-ci existed as a fixture; this case closes that gap. inv.no-silent-widening's disclosure clause generalizes here too: a CI-passing answer must not silently pick one of the two contradicting runs and report it as the only signal. [CORRECTED during Lane 2b codex round-1 remediation, 2026-08-06, from \"Is meridian/web-app's CI passing?\": question_interpreter.py's own module docstring states plainly 'a lowercase slug ... produces no mention' -- extract_mentions requires either a capitalized _NAME token or a QUOTED span adjacent to a closed kind noun (test_question_interpreter.py's proven pattern: 'How is repo \"dev-health-ops\" doing?'). The original bare, unquoted lowercase slug would extract ZERO typed mentions and silently degrade to org-wide/no-subject behavior -- exactly the false-pass risk codex's wrong-kind-collision finding named, generalized here to every other case that had the same bug.]"
}
