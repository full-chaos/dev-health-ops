{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.provisional-unapproved-rule",
  "family": "deg",
  "group": 9,
  "status": "authored",
  "blocked_by": null,
  "proves": [
    "group 9 family deg.provisional-unapproved-rule (a health-rule finding whose calibration_state is provisional/shadow_only must never surface as an approved operational deficiency)"
  ],
  "question": "Do any of the \"Platform Reliability (Ground Control)\" team's flagged health-rule findings count as an approved deficiency yet, or are they still provisional?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "acronym-alias",
  "subject_ref": "subject.acronym-alias.ground-control-team",
  "invariants": [
    {
      "category": "no-provisional-finding-surfaced-as-approved",
      "check": "no_internal_error",
      "args": {},
      "assert": "A HEALTH_RULE_REGISTRY rule finding whose calibration_state is provisional (shadow_only) is never surfaced in the response as a live/approved operational deficiency -- shadow-only computation exists for calibration review, never as client-facing deficiency content. Applies to categories 3-6 (delivery flow / review-CI / deployment-reliability / ownership-code-risk) and category 8 (investment balance) uniformly, since every rule shipped today is calibration_state=provisional (operational_deficiency_service.py's own totality guarantee).",
      "executable_today": true,
      "$comment": "check=no_internal_error is a real, always-meaningful floor (the run must not crash to internal_error) but is NARROWER than this invariant's full 'assert' claim above -- Lane 2a's invariants.py registry (read directly, 2026-08-06) has no checker for the specific provisional-suppression claim this ad-hoc category names. Tracked as a Lane 2a checker gap, not silently claimed as fully enforced."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Grounded directly in operational_deficiency_service.py's own module docstring, read verbatim: 'Every rule shipped today is calibration_state=provisional (3302's own totality test), so every finding is shadow_only and NONE reaches this inventory ... the explicit guardrail: shadow_only findings never in status/counts.' This is a genuine, always-true production behavior today, not fixture-dependent -- true for ANY team/project subject, because the provisional-only state is a registry-wide fact (HEALTH_RULE_REGISTRY), not something a particular fixture's data determines. Distinct from deficiency.team.applicable-rule/deficiency.team.not-applicable-rule (already-authored group-1 siblings): those two exercise Category 1/2 findings, which ARE bound directly from DataHealthResult/StatusSnapshotResult.actual rather than through HEALTH_RULE_REGISTRY, so they genuinely fire or genuinely don't-apply today -- this case is specifically about the calibration_state=provisional guardrail those two never touch. Same quoted-full-label EXACT-resolution mechanism and subject as workload.team.with-denominator/deficiency.team.applicable-rule (see those cases' notes for the live-verified reason the bare 'Ground Control' alias mention is not used) -- resolution_path=deterministic-exact, expected_scope_resolution_outcome=exact per those already-established resolution-profiles/deterministic-v1.json precedents. expected_public_outcome=answered_with_gaps follows the same settled-by-code fact those cases' resolution-profile entries already document (terminal_frames.wrap_legacy_answer_as_frame: a real DevAnswer terminal on today's deterministic/legacy stack always maps to PublicOutcome.ANSWERED_WITH_GAPS, never plain ANSWERED)."
}
