{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "provider-fail.before-frame",
  "family": "provider-fail",
  "group": 1,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "CHAOS-3219 fault-type matrix (provider-fail, 1 of 6 scripted fault types); CHAOS-3423 no-answer-terminal persistence"
  ],
  "question": "Has anything changed in meridian/web-app during the selected period?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": "subject.exact.meridian-web-app",
  "invariants": [
    {
      "category": "terminal-persists-assistant-row",
      "check": "terminal_persists_assistant_row",
      "args": {},
      "assert": "Every clarification or error terminal persists a real transcript row (DevAnswer or the CHAOS-3423 dev_error.v1 row) -- never a silently-dropped turn. Applies here even though the provider failed before any frame or tool call ever executed (fires_from_round: 0): CHAOS-3423 Codex round-3/round-4 fixes exist precisely because a before-frame provider failure used to be reachable without persisting anything.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker terminal_persists_assistant_row (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "notes": "Question text copied verbatim from provider-scripts/role-legacy_agent.json's `cases[\"provider-fail.before-frame\"].question`. Fault script: kind=fault, type=fail-before-frame, fires_from_round=0, http_error 503 'scripted_fault_fail_before_frame' -- every round (including the very first provider.decide() call) fails; no tool has ever been requested for this case, so no DevAnswerFrame is ever committed. Subject resolution: `meridian/web-app` is a literal, unambiguous exact-subject mention; the deterministic server-side subject preflight (recorder.record_preflight, run before the model loop starts per orchestrator.py) is independent of the provider fault -- it is not a 'tool' and does not require a successful provider.decide() call -- so EXACT scope resolution is asserted to complete before the fault fires. This is an inference about SubjectPreflightResult's independence from provider.decide() faults, not something confirmed by running the (not-yet-built) corpus runner; flagged honestly. Public outcome CORRECTED during Lane 2b merge/QA (2026-08-06): terminal_frames.py's PUBLIC_OUTCOME_BY_ERROR_CODE is a totality-checked table (import-time guard over every ORCHESTRATOR_ERROR_CODES member) mapping 'provider_unavailable' -> PublicOutcome.TEMPORARILY_UNAVAILABLE unconditionally, keyed purely by error code with no frame-state branch -- errors.py's `safe_agent_provider_error` classifies a bare 503 as AgentProviderErrorCode.PROVIDER_UNAVAILABLE -> orchestrator.py's `_provider_error` code_map -> 'provider_unavailable' -> that table. This settles the outcome as TEMPORARILY_UNAVAILABLE, not FAILED (the sub-agent draft's guess, matched to the registry sketch's imprecise 'failed' proves-column language); inv.terminal-persists-assistant-row's persistence requirement holds regardless of which PublicOutcome bucket applies.",
  "$comment_resolution_path_invariant": "resolution_path_in REMOVED (CHAOS-3462 B4). This case's question names ZERO mentions that production's extract_mentions recognizes -- verified by executing the REAL producer -- QuestionInterpreter.interpret plus SubjectPreflight.run with this runner's exact request shape -- against this question text. NOTE (adversarial round 2): an earlier version of this comment cited extract_mentions, which is the WRONG oracle: production additionally mints untyped bare-name mentions via _add_untyped_mentions, and three cases were mis-dispositioned on that basis. This case's zero-mention conclusion was re-confirmed against the real interpreter. CASE-SCHEMA.v1.md's own 'practical extraction gotcha' is the cause: a bare, unquoted, lowercase slug is not a mention. So no dev_run_resolutions row is ever written, derive_resolution_path returns None BY CONTRACT, and resolution_path_in deliberately never matches None -- the check failed by construction on every run, on every stack. Same trap and same disposition as the four scope_resolution_outcome_in cases in CASE-SCHEMA.v1.md S1: drop the unpassable check, keep the narrower floor the remaining invariants give, and say so out loud rather than claim enforcement this case never had. The profile's null resolution_path is CORRECT and stays.",
  "$comment_principal_pool": "CHAOS-3490: user_alias assigned by deterministic round-robin over the ordinary-primary pool (sorted case id, index 49 of 85). The pool is role-identical to primary.ordinary, so this changes request DISTRIBUTION only -- never what this case proves. Spreading exists because production caps one user at 20 requests/15min and 85 cases on one alias made a single-pass armed run impossible."
}
