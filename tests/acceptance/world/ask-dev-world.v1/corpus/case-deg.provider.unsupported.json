{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.provider.unsupported",
  "family": "deg",
  "group": 9,
  "status": "declared-blocked",
  "blocked_by": "CHAOS-3588",
  "proves": [
    "group 9 family deg.provider (provider readiness layer -- structural unsupported_model bucket)"
  ],
  "question": "Would you be able to summarize the repo \"meridian/web-app\" for me right now?",
  "org_alias": "primary",
  "user_alias": "primary.unsupported-model-user",
  "subject_class": "n/a",
  "subject_ref": null,
  "invariants": [],
  "resolution_profile_ref": null,
  "fault_ref": null,
  "notes": "BLOCKED (CHAOS-3546 -> CHAOS-3588): this case's mechanism was world.json's user-level provider_profile_override (unsupported-model-profile via user_alias primary.unsupported-model-user), retired by CHAOS-3546's fixture-field-consumer guard -- it was declared on two world.json users and read by zero production code, so both cases it drove were passing while proving nothing about unsupported-model handling. The underlying readiness gate IS real and structurally correct (policy.py:151-158's denied_providers/denied_models -> MODEL_NOT_SUPPORTED -> production_runtime.py's DevRuntimeUnavailable('model_not_supported'), firing before BoundedDevRuntime construction, i.e. before any subject preflight -- confirmed by trace, not assumed), but nothing populates the denial lists: AgentProviderPolicy is built in exactly one place and never receives a `denied` value, and no acceptance-boot step writes the generic settings table a denial would live in. CHAOS-3588 is that ticket: choose org-scoped-settings-seeding vs. a real per-user threading mechanism (this case's own user needs a DIFFERENT provider outcome than other primary org users, which a bare org-level denial cannot express), build it, and re-author this case to drive the gate through it. Formerly reused readiness.capabilities.unsupported-model's exact mechanism/user rather than inventing a new profile, framing the same structural gate as a 'provider degradation' scenario (group 9) instead of a 'capability certification' one (group 1, readiness family) -- that framing is preserved for whoever re-authors this once CHAOS-3588 lands.",
  "$comment_resolution_path_invariant": "FALSIFIED BY EXECUTION (CHAOS-3490, 2026-08-06). The adversarial-round-2 reasoning recorded here previously claimed resolution_path_in was RESTORED because 'the correctly quoted repo \"meridian/web-app\" mention takes the ordinary singular proceeded_committed_subject PROCEED and writes one row.' The Phase 2 exit evidence run executed this case against the real stack and observed resolution_path = None -- zero dev_run_resolutions rows. The claim was established by READING the question rather than running it, which is the same failure mode CHAOS-3462 B4 exists to correct. The check is removed and this note is kept, not deleted, so the corrected record survives."
}
