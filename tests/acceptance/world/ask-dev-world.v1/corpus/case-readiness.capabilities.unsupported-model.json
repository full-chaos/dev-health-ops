{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "readiness.capabilities.unsupported-model",
  "family": "readiness",
  "group": 1,
  "status": "declared-blocked",
  "blocked_by": "CHAOS-3588",
  "proves": [
    "P0-B MISSING item 14"
  ],
  "question": "Please give me the status of the repo \"meridian/web-app\".",
  "org_alias": "primary",
  "user_alias": "primary.unsupported-model-user",
  "subject_class": "n/a",
  "subject_ref": null,
  "invariants": [],
  "resolution_profile_ref": null,
  "fault_ref": null,
  "notes": "BLOCKED (CHAOS-3546 -> CHAOS-3588): this case's mechanism was world.json's user-level provider_profile_override (unsupported-model-profile via user_alias primary.unsupported-model-user), retired by CHAOS-3546's fixture-field-consumer guard -- it was declared on two world.json users and read by zero production code, so both cases it drove were passing while proving nothing about unsupported-model handling. The underlying readiness gate IS real and structurally correct (policy.py:151-158's denied_providers/denied_models -> MODEL_NOT_SUPPORTED -> production_runtime.py's DevRuntimeUnavailable('model_not_supported'), firing before BoundedDevRuntime construction, i.e. before any subject preflight or scope resolution -- confirmed by trace, not assumed, so this case's own prior claim about gate ordering was correct), but nothing populates the denial lists: AgentProviderPolicy is built in exactly one place and never receives a `denied` value, and no acceptance-boot step writes the generic settings table a denial would live in. CHAOS-3588 is that ticket: choose org-scoped-settings-seeding vs. a real per-user threading mechanism (this case's own user needs a DIFFERENT provider outcome than other primary org users, which a bare org-level denial cannot express), build it, and re-author this case to drive the gate through it. Historical framing preserved for whoever re-authors: this id realized world.json's `provider_profiles.unsupported-model-profile` (readiness: unsupported_model, intent: readiness_probe) as a 'capability certification' scenario (group 1, readiness family), distinct from deg.provider.unsupported's 'provider degradation' framing (group 9) of the same underlying gate.",
  "$comment_resolution_path_invariant": "SELF-CONTRADICTION RESOLVED BY EXECUTION (CHAOS-3490, 2026-08-06). This case's own notes state the readiness/capability gate is evaluated BEFORE any subject preflight or scope resolution ever runs, so no ScopeResolutionOutcome is asserted -- while its resolution-profile entry simultaneously declared resolution_path 'deterministic-exact'. Those cannot both be true. The exit evidence run settled it: resolution_path = None, no resolution reached. The notes were right and the profile expectation was wrong."
}
