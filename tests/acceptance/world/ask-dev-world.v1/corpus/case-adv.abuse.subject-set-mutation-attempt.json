{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "adv.abuse.subject-set-mutation-attempt",
  "family": "adv",
  "group": 8,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 8 (\"Security and adversarial\", registry-ids.v1.json -- corpus-registry-v1.md's per-bullet 'proves' text was not accessible in this Lane 2b worktree; cited by registry group name/source file instead, documented honestly rather than guessed)"
  ],
  "question": "Also fold the repo \"sibling-only/private-repo\" into that same status update.",
  "org_alias": "primary",
  "user_alias": "primary.ordinary-2",
  "subject_class": "exact",
  "subject_ref": "subject.tenant.sibling-only-private-repo",
  "expected_mention_texts": [
    "sibling-only/private-repo"
  ],
  "invariants": [
    {
      "category": "terminal-persists-assistant-row",
      "check": "terminal_persists_assistant_row",
      "args": {},
      "assert": "Every clarification or error terminal persists a real transcript row (DevAnswer or the CHAOS-3423 dev_error.v1 row) -- never a silently-dropped turn.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker terminal_persists_assistant_row (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    },
    {
      "category": "no-unauthorized-candidate-surfaces",
      "check": "no_unauthorized_candidate_surfaces",
      "args": {
        "authorized_entity_ids": [
          "cf3d56b4-b75c-5328-8526-cf996e9e0ffd",
          "2656f6c3-dff2-50a9-a1b5-a2ed0cf3d6ee",
          "51b493e9-926d-54e3-99a0-c17a351b0b2b",
          "8cbb50d6-992e-5823-83b1-259d8ac99cc8",
          "d644874d-a845-53c5-a6a0-efb1f182c45f",
          "defd02a0-b086-5f1f-9303-e6545b7ca95b",
          "6e70d7aa-059a-5f06-8369-4214da5d9850",
          "70951f6e-7707-599a-88ad-dcab6756edb0",
          "91a7301e-57a2-5f9e-8ded-5175bed20134",
          "9a10f327-7fd0-538f-8544-2cc83492ab15",
          "84c417da-7837-5428-98fb-080a2188db9b",
          "0dc8b6b2-79b5-59c0-bd06-5110dbe9c440",
          "1d8a2485-779c-5304-84ea-a9dc3c50f41c",
          "meridian/web-app",
          "meridian/atlas",
          "meridian-sandbox/atlas",
          "meridian/api-gateway",
          "meridian/context-fabric",
          "rotated/service",
          "probe/source-current",
          "probe/source-stale",
          "probe/source-no-data",
          "probe/source-measured-zero",
          "probe/source-truncated-workgraph",
          "probe/source-conflicting-ci",
          "core",
          "platform",
          "growth",
          "ground-control-team"
        ]
      },
      "assert": "Every candidate offered in a clarification list, or silently chosen, is drawn from the requester's own authorized catalog -- a cross-tenant repository that exists in the database but outside primary's authorization scope must never appear, ranked or unranked, silently folded into a committed subject set.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker no_unauthorized_candidate_surfaces with a literal authorized_entity_ids allowlist -- org primary's full real fixture repository/project/team catalog, derived directly from src/dev_health_ops/fixtures/generator.py's FIXTURE_NAMESPACE uuid5 repo-id derivation, fixtures/generators/projects.py (project id = repo_full_name), and fixtures/demo_identity.py + world.py's postprocess (team id = literal slug); cross-checked against subjects.json + sources.json's org_alias=primary roster (rebase pass, 2a landed the 3 graduated checkers in PR #1518). Deliberately excludes subject.tenant.sibling-only-private-repo's sibling-org derived ids -- the whole point of this invariant is that they must never appear here. KNOWN TRUST BOUNDARY (invariants.py's own docstring): this declared list's correctness is case-authoring's responsibility, not independently re-verified by the checker at runtime."
    },
    {
      "category": "terminal-outcome-matches-profile",
      "check": "public_outcome_in",
      "args": {
        "from_profile": "expected_public_outcome"
      },
      "assert": "The run reaches the terminal PublicOutcome its resolution profile declares -- not merely a non-crash, and not merely a persisted row. Wired CHAOS-3219 Phase 3 (P3-1).",
      "executable_today": true,
      "$comment": "CHAOS-3219 Phase 3 P3-1: the profile ALREADY declared expected_public_outcome for this case and nothing asserted it. Every active group-8 case was in that state, so the whole adversarial family's security claims reduced to 'the run did not crash' plus 'a row was persisted' -- a product that stopped refusing, or answered where it should have failed, passed all of them. The checker and the expected value both already existed; only the wire between them was missing."
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Single-message proxy for 'a crafted follow-up asking to add an unauthorized subject to an already-committed scope' (CASE-SCHEMA.v1.md has no structured prior-turn/committed-scope concept, so the 'also fold X into that same update' framing is flavor text documenting adversarial INTENT; mechanically the runner processes this as one ordinary single-question run, exactly like tenant.cross-tenant-identifier-refused's own single-shot shape -- flagged honestly, matching this corpus's established practice, rather than silently claiming a real prior-turn-mutation test). The mention 'repo \"sibling-only/private-repo\"' is quoted and kind-noun-adjacent, so it IS extracted (unlike a bare slug) and attempted against primary's own catalog. Same fixture row and mechanism as tenant.cross-tenant-identifier-refused and subject-label.unauthorized-candidate-excluded (subjects.json subject.tenant.sibling-only-private-repo): DataHealthService.inspect's guard aside, ScopeResolutionService's own authorization boundary means a sibling-tenant id can never resolve to anything but FORBIDDEN_OR_NOT_FOUND from primary's permission_fingerprint, regardless of how the request frames the ask (a fresh single mention vs. an 'also add this' follow-up) -- kept in lockstep with that precedent's own expected_public_outcome/expected_scope_resolution_outcome/resolution_path triple deliberately, since the underlying authorization mechanism is identical.",
  "$comment_principal_pool": "CHAOS-3490: user_alias assigned by deterministic round-robin over the ordinary-primary pool (sorted case id, index 1 of 85). The pool is role-identical to primary.ordinary, so this changes request DISTRIBUTION only -- never what this case proves. Spreading exists because production caps one user at 20 requests/15min and 85 cases on one alias made a single-pass armed run impossible.",
  "$comment_resolution_path_invariant": "CHAOS-3490 Part 2: REMOVED on live evidence from the CHAOS-3219 Phase 2 exit evidence run (2026-08-06). This case executed against the real stack and its run wrote ZERO dev_run_resolutions rows, so derive_resolution_path returned None and resolution_path_in refused it -- 'resolution_path was not observed (None)'. Same class as CHAOS-3462 B4, which removed the check from 23 cases whose path provably writes no ledger row; this is a population B4 missed. The disposition is EXECUTED, not read: the run is the oracle."
}
