{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "investment.mix.inadequate",
  "family": "investment",
  "group": 1,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 1 bullet 11"
  ],
  "question": "How would you characterize our investment mix across feature/maintenance/risk work?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary-6",
  "subject_class": "n/a",
  "subject_ref": null,
  "invariants": [
    {
      "category": "no-internal-error-on-fuzzy-label",
      "check": "public_outcome_in",
      "args": {
        "from_profile": "expected_public_outcome"
      },
      "assert": "The run reaches the terminal PublicOutcome this case's resolution-profile entry declares, and never an internal_error.",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Same question as investment.mix.adequate, but this id's fixture is deliberately skewed (org-wide work-item category distribution weighted so one category, e.g. maintenance or risk, dominates past the InvestmentMixin classification threshold) so the real classifier reads the mix as inadequate rather than balanced. Honesty caveat: the specific generator knob that skews the distribution has not been independently live-verified against a scratch generation in this authoring pass (no world.json generation-profile field exists yet to pin a non-default category weighting the way sources.json pins staleness via a post-generation mutation) -- flagged here rather than silently claimed proven, matching this world's REALIZED_UNVERIFIED_LIVE_STATUS discipline (see subjects.json's partially-resolved entry for precedent). No subject-mention/scope-resolution mechanism is exercised (n/a subject); this case is about investment-mix content classification, not the fixed contract/security/persistence invariant taxonomy, so left honestly empty rather than padded. [Reworded during Lane 2b codex round-1 remediation, 2026-08-06, from \"What's our investment mix across feature/maintenance/risk work?\": production's provider_scripts.py requires GLOBALLY-UNIQUE question text per role script (test_a_question_fingerprint_collision_fails_to_load), and every corpus case now needs its own role-legacy_agent.json entry (script_inventory.py's full-coverage requirement) -- two cases sharing literal question text could never both get a script entry. Subject mention substring kept character-identical to subjects.json's declared 'mentions' so the deterministic extractor still recognizes it; only the surrounding phrasing changed.]",
  "$comment_resolution_path_invariant": "resolution_path_in REMOVED (CHAOS-3462 B4). This case's question names ZERO mentions that production's extract_mentions recognizes -- verified by executing the REAL producer -- QuestionInterpreter.interpret plus SubjectPreflight.run with this runner's exact request shape -- against this question text. NOTE (adversarial round 2): an earlier version of this comment cited extract_mentions, which is the WRONG oracle: production additionally mints untyped bare-name mentions via _add_untyped_mentions, and three cases were mis-dispositioned on that basis. This case's zero-mention conclusion was re-confirmed against the real interpreter. CASE-SCHEMA.v1.md's own 'practical extraction gotcha' is the cause: a bare, unquoted, lowercase slug is not a mention. So no dev_run_resolutions row is ever written, derive_resolution_path returns None BY CONTRACT, and resolution_path_in deliberately never matches None -- the check failed by construction on every run, on every stack. Same trap and same disposition as the four scope_resolution_outcome_in cases in CASE-SCHEMA.v1.md S1: drop the unpassable check, keep the narrower floor the remaining invariants give, and say so out loud rather than claim enforcement this case never had. The profile's null resolution_path is CORRECT and stays. This case's ONLY invariant was the unpassable resolution_path_in, so removing it outright would have left zero assertions (which case_schema rejects, and which would be a silent coverage hole anyway). Replaced with public_outcome_in wired to the profile's own already-declared, non-null expected_public_outcome -- a real, satisfiable terminal-outcome assertion, strictly stronger than the bare no_internal_error floor CASE-SCHEMA.v1.md S1 allows as the minimum.",
  "$comment_principal_pool": "CHAOS-3490: user_alias assigned by deterministic round-robin over the ordinary-primary pool (sorted case id, index 40 of 85). The pool is role-identical to primary.ordinary, so this changes request DISTRIBUTION only -- never what this case proves. Spreading exists because production caps one user at 20 requests/15min and 85 cases on one alias made a single-pass armed run impossible."
}
