{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "portfolio.org-wide.status",
  "family": "portfolio",
  "group": 1,
  "status": "authored",
  "blocked_by": null,
  "proves": [
    "CHAOS-3393 unblock evidence (was blocked on CHAOS-3393, now unblocked/authored per corpus/REGISTRY-AMENDMENT.v1.md \u00a71); group 1 Product-question corpus, portfolio family"
  ],
  "question": "What's the status of all our projects across the organization's portfolio?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "n/a",
  "subject_ref": null,
  "invariants": [
    {
      "category": "no-silent-widening",
      "check": "no_internal_error",
      "args": {},
      "assert": "A named subject mention that misses its intended target never silently widens to org-wide/broader scope; widening is only ever a disclosed, terminal answered_with_gaps/needs_clarification outcome.",
      "executable_today": true,
      "$comment": "check=no_internal_error is a real, always-meaningful floor (the run must not crash to internal_error) but is NARROWER than this invariant's full 'assert' claim above -- Lane 2a's invariants.py registry (read directly, 2026-08-06) has no checker yet for the specific widening/unauthorized-candidate/persistence claim this category names. Tracked as a Lane 2a checker gap, not silently claimed as fully enforced."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Shares literal question text with portfolio.org-wide.plan-registry-gap-loud by design -- the two ids exercise the SAME user utterance under two different plan-registry fixture conditions (the general/successful-registry path here vs. the registry-missing path there), not two different phrasings; differentiated by fixture/runtime setup outside this case JSON, documented on both sides rather than silently duplicated. Expected answered_with_gaps rather than a clean answered: this rollup spans the ENTIRE primary-org portfolio (not just the two curated exact/bounded-set repos), and world.json's primary org sync_providers deliberately omits pagerduty -> incidents reads DataHealthState.UNCONFIGURED org-wide for primary (sources.json deg.source-state.unconfigured) -- any true org-wide status/health rollup that would otherwise touch incidents has a real, fixture-grounded gap to disclose, not a hypothetical one. [Reworded during Lane 2b codex round-1 remediation, 2026-08-06, from \"What's the status of all our projects across the portfolio?\": production's provider_scripts.py requires GLOBALLY-UNIQUE question text per role script (test_a_question_fingerprint_collision_fails_to_load), and every corpus case now needs its own role-legacy_agent.json entry (script_inventory.py's full-coverage requirement) -- two cases sharing literal question text could never both get a script entry. Subject mention substring kept character-identical to subjects.json's declared 'mentions' so the deterministic extractor still recognizes it; only the surrounding phrasing changed.]"
}
