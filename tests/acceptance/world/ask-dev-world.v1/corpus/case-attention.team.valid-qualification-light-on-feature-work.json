{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "attention.team.valid-qualification-light-on-feature-work",
  "family": "attention",
  "group": 1,
  "status": "declared-blocked",
  "blocked_by": "CHAOS-3394",
  "proves": [
    "CHAOS-3394 open defect (Backlog, confirmed still open per corpus/REGISTRY-AMENDMENT.v1.md \u00a71 -- 'already correctly marked blocked ... no change'); group 1 Product-question corpus, attention family"
  ],
  "question": "Which teams, if properly qualified, are light on feature work?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "acronym-alias",
  "subject_ref": "subject.acronym-alias.ground-control-team",
  "invariants": [
    {
      "category": "declared-blocked-placeholder",
      "check": "no_internal_error",
      "args": {},
      "assert": "Placeholder ONLY -- this case is status=declared-blocked (nothing runs against real production code/fixtures yet, see blocked_by). Lane 2a runner code (read directly, 2026-08-06) has no declared-blocked concept: case_schema.py requires every case file, unconditionally, to carry a non-empty invariants[] list, or the WHOLE corpus load raises (not a per-file skip) -- one blocked case with invariants:[] would crash loading for all 57 cases. This entry exists purely to satisfy that mechanical requirement so the corpus stays loadable; it must NOT be read as an executed assertion about the blocked scenario. Flagged to team-lead/Lane 2a as a real runner gap: the loader/executor should check the case's own status field and skip execution (report blocked, not run) for any non-authored case, rather than relying on this placeholder to keep loading from crashing.",
      "executable_today": false
    }
  ],
  "resolution_profile_ref": null,
  "fault_ref": null,
  "notes": "Blocked, not authored: investments.py's investment_allocation_shift_observation reports attribution_present=False UNCONDITIONALLY today (CHAOS-3394) -- there is no code path, regardless of how validly a question qualifies/scopes down to one real team, that can currently produce a real attributed 'light on feature work' signal. This id's story is the validly-qualified counterpart to the sibling id attention.team.invalid-qualification-unknown: even if the question were properly scoped to a real, resolvable team subject (subject.acronym-alias.ground-control-team, chosen here as the representative valid qualification target -- the literal question text is intentionally identical to the sibling id's, since the blocking defect is qualification-independent, not a phrasing difference), the adapter's unconditional attribution_present=False means this case cannot be realized against real production code yet. Per CASE-SCHEMA.v1.md's declared-blocked shape: no invariants/resolution_profile_ref/fault_ref (nothing runs); question/org_alias/user_alias/subject_class/subject_ref kept as documentation of the intended (currently-blocked) scenario, matching the world.json/sources.json declared-blocked precedent of not silently omitting a case that cannot pass yet. [Reworded during Lane 2b codex round-1 remediation, 2026-08-06, from 'Which teams are light on feature work?': production's provider_scripts.py requires GLOBALLY-UNIQUE question text per role script (test_a_question_fingerprint_collision_fails_to_load), and every corpus case now needs its own role-legacy_agent.json entry (script_inventory.py's full-coverage requirement) -- two cases sharing literal question text could never both get a script entry. Subject mention substring kept character-identical to subjects.json's declared 'mentions' so the deterministic extractor still recognizes it; only the surrounding phrasing changed.]"
}
