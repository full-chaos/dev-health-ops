{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "portfolio.multi-project.status",
  "family": "portfolio",
  "group": 1,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "CHAOS-3393 unblock evidence (was blocked on CHAOS-3393, now unblocked/authored per corpus/REGISTRY-AMENDMENT.v1.md §1); group 1 Product-question corpus, portfolio family"
  ],
  "question": "How are the repo \"meridian/web-app\" and repo \"meridian/api-gateway\" doing together?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary-4",
  "subject_class": "bounded-set",
  "subject_ref": "subject.bounded-set.web-app-and-api-gateway",
  "invariants": [
    {
      "category": "no-silent-widening",
      "check": "no_internal_error",
      "args": {},
      "assert": "A named subject mention that misses its intended target never silently widens to org-wide/broader scope; widening is only ever a disclosed, terminal answered_with_gaps/needs_clarification outcome.",
      "executable_today": true,
      "$comment": "check=no_internal_error is a real, always-meaningful floor (the run must not crash to internal_error) but is NARROWER than this invariant's full 'assert' claim above -- Lane 2a's invariants.py registry (read directly, 2026-08-06) has no checker yet for the specific widening/unauthorized-candidate/persistence claim this category names. Tracked as a Lane 2a checker gap, not silently claimed as fully enforced."
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "The two named projects are the same real repos subject.bounded-set.web-app-and-api-gateway already names, but subject_ref is deliberately null / subject_class 'n/a' here (not subject.bounded-set.web-app-and-api-gateway) -- this case exercises the CHAOS-3393-landed PlanExecutor/StepContext multi-project portfolio aggregation path, a distinct code path from ScopeResolutionService's single-mention/bounded-set subject resolution that scope.bounded-subject-set already covers. Question deliberately substitutes 'meridian and atlas' (this family's original sketch) for the two unambiguous bounded-set repos: 'atlas' collides with subject.ambiguous.atlas (two same-named repos, meridian/atlas vs meridian-sandbox/atlas), which would pull this case into scope-ambiguity resolution (already covered by scope.ambiguous) instead of exercising the intended clean multi-project portfolio aggregation. Expected answered (not answered_with_gaps): both repos are plain current-state fixtures with no known gap condition attached (subjects.json: meridian/api-gateway is 'the same generation profile as the exact-subject repo'), so a clean two-project rollup is the honest expectation -- contrast with portfolio.org-wide.status, which spans the WHOLE portfolio and does carry a documented gap reason. [CORRECTED during Lane 2b codex round-1 remediation, 2026-08-06, from 'How are the meridian/web-app and meridian/api-gateway projects doing together?': question_interpreter.py's own module docstring states plainly 'a lowercase slug ... produces no mention' -- extract_mentions requires either a capitalized _NAME token or a QUOTED span adjacent to a closed kind noun (test_question_interpreter.py's proven pattern: 'How is repo \"dev-health-ops\" doing?'). The original bare, unquoted lowercase slug would extract ZERO typed mentions and silently degrade to org-wide/no-subject behavior -- exactly the false-pass risk codex's wrong-kind-collision finding named, generalized here to every other case that had the same bug.]",
  "$comment_resolution_path_invariant": "resolution_path_in REMOVED and the profile value REVERTED to null (CHAOS-3462, adversarial round 2 -- this supersedes an earlier, WRONG change in this same branch that set it to deterministic-exact). The earlier reasoning claimed subject_preflight returns the committed_cohort_portfolio_v1 PROCEED for this 2-project cohort. Executing the real preflight refutes that: the question's two mentions are typed REPOSITORY, and interpret assigns intent bounded_investigation -- so BOTH clauses of the PORTFOLIO_STATUS-and-PROJECT gate fail, control reaches _terminate(committed_cohort_v1_only), and TERMINATE persists only terminating_resolution_entry, which is None here. Zero rows in every catalog world.",
  "$comment_principal_pool": "CHAOS-3490: user_alias assigned by deterministic round-robin over the ordinary-primary pool (sorted case id, index 45 of 85). The pool is role-identical to primary.ordinary, so this changes request DISTRIBUTION only -- never what this case proves. Spreading exists because production caps one user at 20 requests/15min and 85 cases on one alias made a single-pass armed run impossible.",
  "expected_mention_texts": [
    "meridian/web-app",
    "meridian/api-gateway"
  ],
  "$comment_authored_3578": "CHAOS-3578 (2026-08-07). The measured armed run failed this case on resolution_path_classifiable + resolution_path_measured with 'unclassifiable-resolution-ledger': the ledger was queried and was NOT empty, but a mention resolved exact_match on its first entry with no mention_text/committed_label supplied, so nothing could confirm a direct match rather than an (impossible-by-contract but unverifiable) single-shot alias commit. The machinery behaved CORRECTLY -- it refused to certify an unverifiable resolution instead of guessing. The case was unmeasurable as written. expected_mention_texts GENERATED BY EXECUTING THE PRODUCER (QuestionInterpreter().interpret with the runner's real DevMessageRequest shape, taking each mention's normalized_lookup_text), per CASE-SCHEMA.v1.md's binding method -- never hand-authored. Both mentions come back requested_entity_kind=EntityKind.REPOSITORY. subject_class CORRECTED from 'n/a' to 'bounded-set', with subject_ref pointing at subjects.json's subject.bounded-set.web-app-and-api-gateway, whose members are exactly these two repos. BASIS: 'n/a' was factually wrong -- the question names two typed subjects and the producer extracts both. This case is the only member of the portfolio.* family that names typed subjects at all; its two siblings (portfolio.org-wide.*) ask about 'all our projects' and correctly stay n/a, which is how this one inherited a class it never fitted and slipped the span guard. ON THE APPARENT CONFLICT: that subjects.json entry's expected_terminal_resolution warns it is 'a bounded, filtered set, not org-wide portfolio aggregation (that's the separate, CHAOS-3393-blocked portfolio.* family)'. That warning distinguishes ORG-WIDE aggregation, which this question is not -- it names exactly two repos. It also carries no runtime force: subject_class and subject_ref are documentation, read by no checker, loader, or runner (case_schema.py requires subject_class only as a string), so this assignment imposes no terminal expectation on the case. NOT YET CROSS-CHECKED AGAINST A LIVE committed_label: the receipt records only the ABSENCE of one (that absence is the failure), and the resolution ledger was destroyed with the stack volumes. The next armed run is the oracle -- resolution_path_classifiable and resolution_path_measured fail loudly if these texts are wrong, which is exactly the check that caught the gap in the first place."
}
