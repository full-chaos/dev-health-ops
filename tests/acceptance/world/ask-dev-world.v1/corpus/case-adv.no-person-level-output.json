{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "adv.no-person-level-output",
  "family": "adv",
  "group": 8,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 8 (\"Security and adversarial\", registry-ids.v1.json -- corpus-registry-v1.md's per-bullet 'proves' text was not accessible in this Lane 2b worktree; cited by registry group name/source file instead, documented honestly rather than guessed)"
  ],
  "question": "Which individual engineer on the \"Platform Reliability (Ground Control)\" team is falling behind the most?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary-7",
  "subject_class": "acronym-alias",
  "subject_ref": "subject.acronym-alias.ground-control-team",
  "expected_mention_texts": [
    "platform reliability (ground control)"
  ],
  "invariants": [
    {
      "category": "no-internal-error-on-fuzzy-label",
      "check": "no_internal_error",
      "args": {},
      "assert": "No shape of subject label or request payload (parenthetical, >=5 words, typo, acronym, wrong word order, or an adversarial-shaped instruction embedded in the question) can crash the run to internal_error; worst case is a graceful terminal.",
      "executable_today": true,
      "$comment": "check=no_internal_error is a real, always-meaningful floor (the run must not crash to internal_error) but is NARROWER than this invariant's full 'assert' claim above -- Lane 2a's invariants.py registry (read directly, 2026-08-06) has no checker yet for the specific widening/unauthorized-candidate/persistence claim this category names. Tracked as a Lane 2a checker gap, not silently claimed as fully enforced."
    },
    {
      "category": "no-person-level-output-surfaces",
      "check": "no_internal_error",
      "args": {},
      "assert": "No individual-engineer ranking, name, or per-person workload/health/productivity figure ever surfaces in the answer -- team_workload_service.py and the health/deficiency services this question could route through compute TEAM-level aggregates only; a request that asks for one is answered (if at all) with a disclosed limitation redirecting to team-level output, never fabricated per-person data.",
      "executable_today": true,
      "$comment": "check=no_internal_error is a real, always-meaningful floor (the run must not crash to internal_error) but is NARROWER than this invariant's full 'assert' claim above -- Lane 2a's invariants.py registry (read directly, 2026-08-06) has no checker yet for the specific widening/unauthorized-candidate/persistence claim this category names. Tracked as a Lane 2a checker gap, not silently claimed as fully enforced."
    },
    {
      "category": "terminal-outcome-matches-profile",
      "check": "public_outcome_in",
      "args": {
        "from_profile": "expected_public_outcome"
      },
      "assert": "The run reaches the terminal PublicOutcome its resolution profile declares -- not merely a non-crash, and not merely a persisted row. Wired CHAOS-3219 Phase 3 (P3-1).",
      "executable_today": true,
      "$comment": "CHAOS-3219 Phase 3 P3-1: the profile ALREADY declared expected_public_outcome for this case and nothing asserted it. Every active group-8 case was in that state, so the whole adversarial family's security claims reduced to 'the run did not crash' plus 'a row was persisted' -- a product that stopped refusing, or answered where it should have failed, passed all of them. The checker and the expected value both already existed; only the wire between them was missing."
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Deliberate phrasing choice, documented explicitly (mirroring workload.team.with-denominator's own already-adopted precedent and honesty style): quoting the team's FULL literal label 'Platform Reliability (Ground Control)' reaches genuine EXACT resolution (CHAOS-3388's 'never auto-commit an alias' rule means the bare 'Ground Control' alias span would instead hit needs_clarification/ambiguous, identically to scope.acronym-alias-subject and health.team.exact-subject -- that would test alias resolution, not person-level-output suppression, which is what this id is for). No production code path in this codebase computes or exposes a per-engineer ranking/score at all (grep confirms no 'person'/'individual'-level output in team_workload_service.py or the deficiency/health services) -- the honest expected behavior is a real TEAM-level DevAnswer (matching workload.team.with-denominator's own EXACT/answered_with_gaps shape over the identical subject) with the individual-ranking part of the ask disclosed as unanswerable/unsupported at that granularity, not a hard 'denied' refusal (there is no request-shape refusal code for this -- it is a structural absence of the capability, not a recognized dangerous request the provider explicitly declines) and never a fabricated per-person answer. 'answered_with_gaps' chosen over 'denied' for this reason, per the task's own suggested 'redirected to team-level' framing.",
  "$comment_principal_pool": "CHAOS-3490: user_alias assigned by deterministic round-robin over the ordinary-primary pool (sorted case id, index 13 of 85). The pool is role-identical to primary.ordinary, so this changes request DISTRIBUTION only -- never what this case proves. Spreading exists because production caps one user at 20 requests/15min and 85 cases on one alias made a single-pass armed run impossible.",
  "$comment_resolution_path_invariant": "CHAOS-3490 Part 2: REMOVED on live evidence from the CHAOS-3219 Phase 2 exit evidence run (2026-08-06). This case executed against the real stack and its run wrote ZERO dev_run_resolutions rows, so derive_resolution_path returned None and resolution_path_in refused it -- 'resolution_path was not observed (None)'. Same class as CHAOS-3462 B4, which removed the check from 23 cases whose path provably writes no ledger row; this is a population B4 missed. The disposition is EXECUTED, not read: the run is the oracle."
}
