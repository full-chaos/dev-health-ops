// Package sessionvenue holds the session routes' venue differential oracle
// (TestSessionVenueOracle): the real Python api and the dho api route set,
// compared on one scenario. It is its own package so the venue run has its
// own time budget, apart from the other api venues.
package sessionvenue
