{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.timeout.data-health",
  "family": "deg",
  "group": 9,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 9 family deg.timeout (pipeline-stage timeout matrix -- data-health-check instance)"
  ],
  "question": "Is the data-health check for the repo \"meridian/web-app\" hanging?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": "subject.exact.meridian-web-app",
  "expected_mention_texts": [
    "meridian/web-app"
  ],
  "invariants": [
    {
      "category": "terminal-persists-assistant-row",
      "check": "terminal_persists_assistant_row",
      "args": {},
      "assert": "Every clarification or error terminal persists a real transcript row (DevAnswer or the CHAOS-3423 dev_error.v1 row) -- never a silently-dropped turn. Applies here even though the run is deliberately slow (an injected 4000ms delay immediately after the data_health.v1 tool call returns): a slow-but-successful terminal must persist exactly like a fast one.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker terminal_persists_assistant_row (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": "deg.timeout.data-health",
  "notes": "Same 'no dedicated per-stage timeout fault type exists today' honesty basis as deg.timeout.graph/metric/evidence. Stands in for a data-health-check-stage timeout using the EXISTING slow-response mechanism: round 0 issues a real, immediate tool_call to data_health.v1 (wire name data_health_v1, arguments {} -- production_runtime.py's data_health executor takes no request fields beyond the standard scope, unlike query_metric/search_evidence which do), then fires_from_round=1 applies the 4000ms slow-response delay to the round immediately after. Honesty caveat: identical to deg.timeout.graph/metric/evidence's -- the fault mechanism delays the provider's own decide() round-trip, not literal server-side tool execution latency, so this is a plausible, explicitly-flagged proxy for 'the data-health check itself timed out', not a literal one. Subject resolves EXACT via the quoted noun-adjacent span 'the repo \"meridian/web-app\"', independent of and prior to the fault."
}
