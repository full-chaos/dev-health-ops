{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "ops.kill-switch.global",
  "family": "ops",
  "group": 11,
  "status": "declared-blocked",
  "blocked_by": "CHAOS-3549 runner has no expected_http_refusal case shape, so a route-level 403 cannot be a measured pass",
  "proves": [
    "group 11 bullet 4"
  ],
  "question": "What's the status of the repo \"meridian/web-app\"?",
  "org_alias": "disabled",
  "user_alias": "disabled.ordinary",
  "subject_class": "n/a",
  "subject_ref": null,
  "invariants": [],
  "resolution_profile_ref": null,
  "fault_ref": null,
  "notes": "[SUPERSEDED IN PART -- read $comment_blocked_by_correction FIRST. The REGISTRY-AMENDMENT sec.10.4 premise referenced below (that an active case in the disabled-entitlement org fails the measured_wave_3_1_preflight_path assertion 'by construction') is FALSE on today's runner: that org set is derived from ACTIVE cases, so activating this case would add the org and the check would pass. The real blocker is earlier and cruder -- a 403 at conversation creation aborts the case with no receipt at all. Original text preserved unedited.] RE-AUTHORED as a FLAG-OFF BEHAVIOR case (CHAOS-3458, chris ruling 2026-08-06: \"single feature flag is the kill mechanism -- no per-scope kill switches. We don't need to kill pieces of a feature when it's not working, and it's simplicity here.\"). The org-level Ask Dev feature flag / entitlement gate IS the kill mechanism, so this case no longer waits on kill-switch enforcement code that will never be built; the four per-scope siblings (ops.kill-switch.{provider,role,surface,contextual-entry}) were RETIRED in the same changeset. WHAT THIS CASE NOW SPECIFIES: an ordinary member of world.json's 'disabled' org (ask_dev_entitlement='disabled', ask_dev_contextual_entrypoints_entitlement='disabled', ask_dev_wave_3_1_entitlement='disabled') asks an ordinary, well-formed, fully-resolvable question, and the route must REFUSE it (feature_not_enabled / UNSUPPORTED or equivalent) rather than answer it. The principal exists and is real: world.json seeds 'disabled.ordinary' with a working credential through password_for_alias, exactly like every other world principal, so the case needs no fixture or product change to be addressable. STILL BLOCKED, and the reason is now a PROVEN runner gap rather than the superseded 'no kill-switch code exists' premise: tests/acceptance/test_wave4_corpus_runner_live.py:825-833 records measured_wave_3_1_preflight_path on EVERY receipt with condition=(org_id in wave_3_1_enabled_orgs), unconditionally. The 'disabled' org is by construction not in that set, so any ACTIVE case in it fails a run-validity assertion whose whole purpose is to prove the run measured the CHAOS-3292 path -- an intentional flag-off case and an accidentally-legacy run are indistinguishable to it today. Unblocking needs a runner seam that lets a case declare 'this run is SUPPOSED to be on the disabled path' (Lane 2a), after which this case becomes active with no further authoring. Also note the case must stay declared-blocked to avoid a script-inventory entry: check_script_inventory (test_wave4_corpus_runner_live.py:270) requires a role-legacy_agent.json entry for active cases only. [blocked_by stays CHAOS-3458; original pre-ruling free-text reason preserved verbatim for provenance: 'runner: no production kill-switch enforcement code exists yet'.]",
  "$comment_blocked_by_correction": "CHAOS-3219 Phase 3 (2026-08-07) stale-blocker sweep. WAS: 'CHAOS-3458'. CORRECTION, and the mechanism recorded in REGISTRY-AMENDMENT sec.10.4 is FALSE on today's code. That section blocks this on the measured_wave_3_1_preflight_path assertion failing 'by construction' for a disabled-entitlement org. It would not: wave_3_1_enabled_orgs (test_wave4_corpus_runner_live.py:397-436) derives its org set from _representative_alias_per_org (:440-460), which iterates ACTIVE cases only (:204) -- making this case active would simply add the disabled org to that set and the check would pass. The real, earlier blocker is that _require_ask_dev 403s at POST /conversations (router.py:376-398, 1036-1045), AcceptanceApi converts any HTTPError to a failure (prepare_ask_dev_acceptance.py:70-74) and the runner re-raises (:636-641), so the case errors with NO receipt and ZERO assertions. CHAOS-3458 (cited before) is CLOSED as superseded by the single-flag ruling."
}
