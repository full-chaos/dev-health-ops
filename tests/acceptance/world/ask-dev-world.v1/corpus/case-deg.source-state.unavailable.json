{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.source-state.unavailable",
  "family": "deg",
  "group": 9,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 9 family deg.source-state (sources.json per-source-class terminal-state matrix, state=unavailable)"
  ],
  "question": "Can you pull up the current status of the repo \"sibling-only/private-repo\"?",
  "org_alias": "sibling",
  "user_alias": "sibling.ordinary",
  "subject_class": "exact",
  "subject_ref": "subject.tenant.sibling-only-private-repo",
  "expected_mention_texts": [
    "sibling-only/private-repo"
  ],
  "invariants": [
    {
      "category": "terminal-outcome-matches-profile",
      "check": "public_outcome_in",
      "args": {
        "from_profile": "expected_public_outcome"
      },
      "assert": "The run reaches the terminal PublicOutcome this case's resolution-profile entry declares, and never an internal_error.",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Grounded against sources.json's dedicated 'unavailable' matrix row (repo_full_name sibling-only/private-repo, org sibling): sibling org's 'local' SyncConfiguration is seeded active_failure=True org-wide -> commits/pull_requests/reviews/deployments all read DataHealthState.UNAVAILABLE. Deliberately asked FROM the sibling org itself (org_alias sibling, user_alias sibling.ordinary) about its OWN repo -- this is the same underlying fixture row subject.tenant.sibling-only-private-repo documents, but observed from its own tenant's vantage rather than cross-tenant, so ScopeResolutionService resolves it EXACT (never UNAUTHORIZED) and the degradation is purely a source-availability disclosure, not an authorization refusal. This is the deliberate counterpart to deg.source-state.unauthorized-not-visible and tenant.cross-tenant-identifier-refused, both of which ask about the SAME repo but FROM primary (cross-tenant, not_found) -- three distinct, individually true observations of one fixture row from three different vantage points, matching sources.json's own stated intent of not tripling it into three repos.",
  "$comment_resolution_path_invariant": "CHAOS-3490 Part 2: REMOVED on live evidence from the CHAOS-3219 Phase 2 exit evidence run (2026-08-06). This case executed against the real stack and its run wrote ZERO dev_run_resolutions rows, so derive_resolution_path returned None and resolution_path_in refused it -- 'resolution_path was not observed (None)'. Same class as CHAOS-3462 B4, which removed the check from 23 cases whose path provably writes no ledger row; this is a population B4 missed. The disposition is EXECUTED, not read: the run is the oracle.",
  "$comment_scope_resolution_outcome_invariant": "CHAOS-3490 Part 2: REMOVED on the same live evidence. The run emitted no scope.resolved event carrying a scope_resolution.outcome, so this check could not match any allowed value. Observed, not inferred.",
  "$comment_zero_invariant_justification": "CHAOS-3490 Part 2: stripping the two live-evidence-invalid checks left this case with ZERO invariants, which asserts nothing and would report green having measured nothing -- worse than no test, because it reads as coverage. It therefore takes public_outcome_in against the profile's existing non-null expected_public_outcome ('answered_with_gaps'), exactly the remedy CHAOS-3462 B4 applied to the three cases it left in the same state."
}
