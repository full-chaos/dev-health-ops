{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "status.single-project.exact-subject",
  "family": "status",
  "group": 1,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 1 Product-question corpus; status family plain exact-subject phrasing, distinct from positive-control-v1's heritage-oracle wording"
  ],
  "question": "How is the repo \"meridian/web-app\" doing?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": "subject.exact.meridian-web-app",
  "expected_mention_texts": [
    "meridian/web-app"
  ],
  "invariants": [
    {
      "category": "no-silent-widening",
      "check": "scope_resolution_outcome_in",
      "args": {
        "from_profile": "expected_scope_resolution_outcome"
      },
      "assert": "A named subject mention that misses its intended target never silently widens to org-wide/broader scope; widening is only ever a disclosed, terminal answered_with_gaps/needs_clarification outcome.",
      "executable_today": true,
      "$comment": "Wired to the real graduated checker scope_resolution_outcome_in against this case's own resolution-profile expected_scope_resolution_outcome (rebase pass, 2a landed the 3 graduated checkers in PR #1518) -- no longer a narrower no_internal_error placeholder."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Same subject as positive-control-v1 (subject.exact.meridian-web-app), deliberately a different, plainer question phrasing (matches the 'What's the status of X?' convention already used by scope.no-match / scope.acronym-alias-subject) so this case exercises the status family's own answer composition over commits+work-items+CI, not the heritage-oracle metric/evidence contract. Minimal invariant set: clean answered terminal over an exact, unambiguous subject. [Reworded during Lane 2b codex round-1 remediation, 2026-08-06, from \"What's the status of meridian/web-app?\": production's provider_scripts.py requires GLOBALLY-UNIQUE question text per role script (test_a_question_fingerprint_collision_fails_to_load), and every corpus case now needs its own role-legacy_agent.json entry (script_inventory.py's full-coverage requirement) -- two cases sharing literal question text could never both get a script entry. Subject mention substring kept character-identical to subjects.json's declared 'mentions' so the deterministic extractor still recognizes it; only the surrounding phrasing changed.] [CORRECTED during Lane 2b codex round-1 remediation, 2026-08-06, from 'How is meridian/web-app doing?': question_interpreter.py's own module docstring states plainly 'a lowercase slug ... produces no mention' -- extract_mentions requires either a capitalized _NAME token or a QUOTED span adjacent to a closed kind noun (test_question_interpreter.py's proven pattern: 'How is repo \"dev-health-ops\" doing?'). The original bare, unquoted lowercase slug would extract ZERO typed mentions and silently degrade to org-wide/no-subject behavior -- exactly the false-pass risk codex's wrong-kind-collision finding named, generalized here to every other case that had the same bug.]"
}
