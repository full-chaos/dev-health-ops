{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.provider.unavailable",
  "family": "deg",
  "group": 9,
  "status": "active",
  "blocked_by": null,
  "proves": [
    "group 9 family deg.provider (provider readiness layer, distinct from provider-fail.before-frame/after-frame's mid-run fault scripting)"
  ],
  "question": "Would you mind pulling the latest status on the repo \"meridian/web-app\" for me?",
  "org_alias": "primary",
  "user_alias": "primary.degraded-readiness-user",
  "subject_class": "n/a",
  "subject_ref": null,
  "expected_mention_texts": [
    "meridian/web-app"
  ],
  "invariants": [
    {
      "category": "no-silent-degraded-readiness",
      "check": "no_internal_error",
      "args": {},
      "assert": "A degraded provider/model readiness state (transient -- timeout, rate limit, temporary provider unavailability -- per llm.agent.readiness.readiness_failure_state, as distinct from the structural unsupported_model state) is always disclosed to the caller as a terminal answered_with_gaps outcome (or a graceful error terminal); it must never be silently presented as a full-confidence `answered` outcome, and must never crash to internal_error.",
      "executable_today": true,
      "$comment": "check=no_internal_error is a real, always-meaningful floor (the run must not crash to internal_error) but is NARROWER than this invariant's full 'assert' claim above -- Lane 2a's invariants.py registry (read directly, 2026-08-06) has no checker yet for the specific widening/unauthorized-candidate/persistence claim this category names. Tracked as a Lane 2a checker gap, not silently claimed as fully enforced."
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "This is the PROVIDER READINESS layer being unavailable at request start (evaluated before any question is asked, before subject preflight or scope resolution ever runs), DELIBERATELY DIFFERENT from provider-fail.before-frame/after-frame, which script a mid-run fault (a live provider error occurring partway through an already-in-progress agent turn, mapped via terminal_frames.py's PUBLIC_OUTCOME_BY_ERROR_CODE to TEMPORARILY_UNAVAILABLE for the 'provider_unavailable' error code specifically). world.json's provider_profiles has no readiness value literally named 'unavailable' -- the real ReadinessState literal (src/dev_health_ops/llm/agent/readiness.py) is exactly {ready, unsupported_model, missing_credentials, disabled, degraded, stale_readiness}, six members, no seventh 'unavailable' member. Grounded instead in the CLOSEST real fixture: readiness_failure_state('provider_unavailable') (readiness.py line ~313) maps explicitly to the 'degraded' ReadinessState bucket alongside timeout/rate_limited/unknown-default -- i.e. 'provider unavailable' IS one of the real, named sub-codes 'degraded' represents at the readiness-gate layer. Reuses world.json's already-wired degraded-profile / primary.degraded-readiness-user (the same profile readiness.capabilities.degraded grounds) rather than inventing a new user_alias/provider_profile entry (out of this batch's file-authoring scope) -- documented explicitly here as a MAPPING CHOICE, not a dedicated fixture: world.json's degraded-profile carries no distinguishing safe_error_code field, so this case and readiness.capabilities.degraded are mechanistically identical fixtures, differentiated only by question wording and by which specific readiness_failure_state sub-bucket (provider_unavailable vs. the general degraded bucket) each case's assertion focuses on. Same honesty caveat as readiness.capabilities.degraded's own notes: the exact code path from a degraded-profile readiness_probe result to a live answered_with_gaps DevAnswer was not fully walked against a concrete implementation at authoring time. Subject is n/a for the same reason as readiness.capabilities.degraded/unsupported-model: the readiness gate fires before any subject mention is ever parsed.",
  "$comment_resolution_path_invariant": "CHAOS-3490 Part 2: REMOVED on live evidence from the CHAOS-3219 Phase 2 exit evidence run (2026-08-06). This case executed against the real stack and its run wrote ZERO dev_run_resolutions rows, so derive_resolution_path returned None and resolution_path_in refused it -- 'resolution_path was not observed (None)'. Same class as CHAOS-3462 B4, which removed the check from 23 cases whose path provably writes no ledger row; this is a population B4 missed. The disposition is EXECUTED, not read: the run is the oracle."
}
