{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "deg.provider.unsupported",
  "family": "deg",
  "group": 9,
  "status": "authored",
  "blocked_by": null,
  "proves": [
    "group 9 family deg.provider (provider readiness layer -- structural unsupported_model bucket)"
  ],
  "question": "Would you be able to summarize the repo \"meridian/web-app\" for me right now?",
  "org_alias": "primary",
  "user_alias": "primary.unsupported-model-user",
  "subject_class": "n/a",
  "subject_ref": null,
  "invariants": [
    {
      "category": "terminal-persists-assistant-row",
      "check": "no_internal_error",
      "args": {},
      "assert": "Every clarification or error terminal persists a real transcript row (DevAnswer or the CHAOS-3423 dev_error.v1 row) -- never a silently-dropped turn.",
      "executable_today": true,
      "$comment": "check=no_internal_error is a real, always-meaningful floor (the run must not crash to internal_error) but is NARROWER than this invariant's full 'assert' claim above -- Lane 2a's invariants.py registry (read directly, 2026-08-06) has no checker yet for the specific widening/unauthorized-candidate/persistence claim this category names. Tracked as a Lane 2a checker gap, not silently claimed as fully enforced."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "Reuses readiness.capabilities.unsupported-model's exact mechanism/user (world.json's unsupported-model-profile via user_alias primary.unsupported-model-user, readiness: unsupported_model, intent: readiness_probe -- DevCapabilitiesReadiness.unsupported_model) rather than inventing a new profile, per this batch's instructions, but asks a DIFFERENT, non-duplicate question ('Would you be able to summarize...' vs. readiness.capabilities.unsupported-model's 'Please give me the status of...'). This id's contribution to the deg.provider family is framing the SAME structural readiness gate as a 'provider degradation' scenario (group 9, degradation and trust) rather than a 'capability certification' scenario (group 1, readiness family) -- same underlying code path (production_runtime.py's _resolve_provider_selection raises DevRuntimeUnavailable(code='model_not_supported'); terminal_frames.py's PUBLIC_OUTCOME_BY_ERROR_CODE table maps 'model_not_supported' -> PublicOutcome.UNSUPPORTED), two registry vantage points over one real fixture, deliberately not re-derived. Subject is n/a for the same reason as readiness.capabilities.unsupported-model: the readiness/capability gate is evaluated before any subject mention is ever parsed, so no ScopeResolutionOutcome value is asserted."
}
