{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "subject-label.wrong-kind-collision",
  "family": "subject-label",
  "group": 1,
  "status": "authored",
  "blocked_by": null,
  "proves": [
    "CHAOS-3389 eval set (\"wrong-kind collisions\"); CHAOS-3422 evidence"
  ],
  "question": "What's the status of the project \"api-gateway\"?",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "exact",
  "subject_ref": "subject.bounded-set.web-app-and-api-gateway",
  "invariants": [
    {
      "category": "no-silent-widening",
      "check": "no_internal_error",
      "args": {},
      "assert": "A named subject mention that misses its intended target never silently widens to org-wide/broader scope; widening is only ever a disclosed, terminal answered_with_gaps/needs_clarification outcome.",
      "executable_today": true,
      "$comment": "check=no_internal_error is a real, always-meaningful floor (the run must not crash to internal_error) but is NARROWER than this invariant's full 'assert' claim above -- Lane 2a's invariants.py registry (read directly, 2026-08-06) has no checker yet for the specific widening/unauthorized-candidate/persistence claim this category names. Tracked as a Lane 2a checker gap, not silently claimed as fully enforced."
    },
    {
      "category": "no-unauthorized-candidate-surfaces",
      "check": "no_internal_error",
      "args": {},
      "assert": "Every candidate offered (ranked or unranked) is drawn from the requester's own authorized catalog (permission_fingerprint-scoped); a candidate outside that scope must never appear.",
      "executable_today": true,
      "$comment": "check=no_internal_error is a real, always-meaningful floor (the run must not crash to internal_error) but is NARROWER than this invariant's full 'assert' claim above -- Lane 2a's invariants.py registry (read directly, 2026-08-06) has no checker yet for the specific widening/unauthorized-candidate/persistence claim this category names. Tracked as a Lane 2a checker gap, not silently claimed as fully enforced."
    },
    {
      "category": "no-internal-error-on-fuzzy-label",
      "check": "no_internal_error",
      "args": {},
      "assert": "No shape of subject label (parenthetical, >=5 words, typo, acronym, wrong word order) can crash the run to internal_error; worst case is a graceful terminal.",
      "executable_today": true,
      "$comment": "check=no_internal_error is a real, always-meaningful floor (the run must not crash to internal_error) but is NARROWER than this invariant's full 'assert' claim above -- Lane 2a's invariants.py registry (read directly, 2026-08-06) has no checker yet for the specific widening/unauthorized-candidate/persistence claim this category names. Tracked as a Lane 2a checker gap, not silently claimed as fully enforced."
    },
    {
      "category": "resolution-path-matches-profile",
      "check": "resolution_path_in",
      "args": {
        "from_profile": "resolution_path"
      },
      "assert": "The run's derived wave4_case_result.v1 resolution_path equals this case's resolution-profiles/deterministic-v1.json entry's own resolution_path (null is a valid, asserted value for n/a-subject cases that never attempt mention resolution).",
      "executable_today": true
    }
  ],
  "resolution_profile_ref": "deterministic-v1",
  "fault_ref": null,
  "notes": "fixtures/generators/work_items.py generate_work_items titles every work item f\"[{project}] {category}/{sub_category} {item_type} {i}\" -- meridian/api-gateway's own issues/PRs already produce real substring collisions against its own project-name mention (realized from existing generated data, no generator change). CHAOS-3422: candidate ranking is kind-unfiltered today, so a typed project mention can rank behind these issue/PR substring hits -- invariant-only: must not surface a WRONG-KIND or unauthorized result; clean single-candidate resolution is NOT asserted. [CORRECTED TWICE during Lane 2b codex round-1 remediation, 2026-08-06: (1) from \"What's the status of meridian/api-gateway?\" to a quoted, kind-noun-adjacent form (question_interpreter.py's own module docstring: 'a lowercase slug ... produces no mention'; extract_mentions needs a QUOTED span adjacent to a closed kind noun, per test_question_interpreter.py's proven 'How is repo \"dev-health-ops\" doing?' pattern) -- the original bare slug extracted ZERO typed mentions and silently degraded to org-wide behavior, exactly the false-pass risk codex's finding named. (2) from the FULL slug 'project \"meridian/api-gateway\"' to the PARTIAL slug 'project \"api-gateway\"': projects_gen.build_project_record sets a project's catalog id equal to its full repo_full_name ('meridian/api-gateway'), so a quoted, exact, fully-qualified mention hits the catalog directly and never reaches the close-match/fuzzy search path CHAOS-3422 is actually about -- only a mention that MISSES exact catalog lookup (the bare 'api-gateway', without the 'meridian/' org prefix) falls through to close-match search, where it can now genuinely collide against the '[meridian/api-gateway] ...' issue/PR titles (a real substring match) the way CHAOS-3422 describes."
}
