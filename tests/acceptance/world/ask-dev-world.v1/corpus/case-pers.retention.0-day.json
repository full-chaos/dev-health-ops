{
  "schema_version": "ask_dev_corpus_case.v1",
  "id": "pers.retention.0-day",
  "family": "pers",
  "group": 10,
  "status": "declared-blocked",
  "blocked_by": "CHAOS-3404",
  "proves": [
    "group 10 bullet 4"
  ],
  "question": "Purge Ask Dev conversations older than 0 days for my organization.",
  "org_alias": "primary",
  "user_alias": "primary.ordinary",
  "subject_class": "n/a",
  "subject_ref": null,
  "invariants": [
    {
      "category": "declared-blocked-placeholder",
      "check": "no_internal_error",
      "args": {},
      "assert": "Placeholder ONLY -- this case is status=declared-blocked (nothing runs against real production code/fixtures yet, see blocked_by). Lane 2a runner code has no declared-blocked concept: case_schema.py requires every case file, unconditionally, to carry a non-empty invariants[] list, or the WHOLE corpus load raises (not a per-file skip). This entry exists purely to satisfy that mechanical requirement so the corpus stays loadable; it must NOT be read as an executed assertion about the blocked scenario. Same interim exception documented in CASE-SCHEMA.v1.md declared-blocked section -- do not build on it.",
      "executable_today": false
    }
  ],
  "resolution_profile_ref": null,
  "fault_ref": null,
  "notes": "0-day retention sweep covering every intent/ledger/plan/source/frame/narrative/finding artifact class. DevPersistenceService.cleanup_expired exists (src/dev_health_ops/api/dev/persistence/service.py, confirmed by reading it directly) but has no beat schedule, CLI, or production caller (CHAOS-3404, confirmed open Backlog/High) -- retention sweeps never execute in production today, so this case cannot pass against real code yet. Matches registry-ids.v1.json group-10 blocked map (already recorded in REGISTRY-AMENDMENT.v1.md sec.1)."
}
